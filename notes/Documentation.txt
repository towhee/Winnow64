******************************************************************************************
WINNOW DEVELOPER DOCUMENTATION
******************************************************************************************

Format conventions
    • ***  (90 stars)      top-level section divider, followed by an ALL-CAPS title
    • ---  (90 dashes)     subsection bracket, with a Title-Case heading between lines
    • text                 insert line breaks to wrap text at 90 characters
    • • / -                bullet lists, indented from the left margin
    • `path/to/file:line`  file references; line number for a specific spot

------------------------------------------------------------------------------------------
Table of Contents
------------------------------------------------------------------------------------------

    MAINTENANCE: keep this Table of Contents in sync with the document. Whenever a
    top-level section (*** or === divider) or a subsection (--- divider) is added,
    removed, or renamed, update the matching entry here in the same pass.

    1. INITIALIZATION
         - Startup Sequence
         - Dock Window State (versioning)
         - Responsive dock tab titles (text <-> graphic)
         - Dropped dock lands last (rightmost tab)  [WORK IN PROGRESS - DISABLED]

    2. FOLDER LOADING PIPELINE
         - Overview
         - New Folder Selected (Call Sequence)
         - Concurrent Flow - New Folder Selected
         - Concurrent Flow - New Image Selected
         - Flow Flags
         - Current Model Row
         - Icons
         - Icon Chunk Size
         - Thumbnail (Icon) Decoding
         - Tracking Icon Parameters
         - Other Global Flags

    3. DATAMODEL CHANGES
         - Folder Change (legacy — needs rewrite)
         - Image Change (legacy — needs rewrite)
         - Filter Change
         - Sort Change
         - Combine Raw+JPG
         - Folder Tree Auto-Refresh
         - Insertions, Deletions, Modifications
         - Instance

    4. CACHING
         - Overview
         - Cache Status
         - Icon Caching
         - Load Responsiveness (keeping the GUI usable during a large/recursive load)
         - How to Test Icon Caching
         - Memory Governor (process footprint, decode concurrency)

    5. SCROLLING
         - Painting Delays
         - Scrollbar Change Flow
         - Resizing Icons or the Icon Viewport

    6. FOCUS PREDICTOR
         - Architecture
         - Process
         - Implementation

    7. HELP SYSTEM
         - Architecture
         - Adding a New Help Page
         - Editing Help Formatting Globally
         - VS Code Workflow
         - Entry Points
         - Orphaned Help Documents

    8. ACTION ENABLE/DISABLE RULES
         - Overview
         - Conditions and Triggers
         - The Menu vs Shortcut Pitfall
         - Disabled Shortcut Feedback
         - Develop Mode Local Shortcuts
         - Rules by Action

    9. MEMORY CARD EJECT
         - Overview
         - Entry Points
         - Drive Root Normalization
         - Auto-Eject After Ingest

    10. CHECK FOR UPDATES
         - Overview
         - Mechanism
         - Publishing version.json

    11. FIND DUPLICATES
         - Candidates (A list) and the model mapping
         - Targets (B list)
         - Comparison and thumbnail normalization
         - Video frames (async sync)
         - Responsiveness
         - Output
         - Dialog layout

    12. RAW DECODING (SENSOR DEMOSAIC)
         - Architecture

    13. OPERATION MODE (Preview vs Develop)

    14. DEVELOP / IMAGE EDIT (PARAMETRIC ADJUSTMENTS)
         - Architecture
         - Panel focus & slider arrow-nudge
         - Layer & masking model (planned)
         - Fill Replace (spot / fill / object heal)  [SHIPPED: SPOTS ONLY]
         - Develop dock (UI)
         - Develop scopes (histogram + vectorscope)
         - Develop Transform panel (crop + perspective)  [UI PROTOTYPE]
         - Develop presets (save)


******************************************************************************************

INITIALIZATION

    Persistent settings are saved between sessions using QSettings. Persistent settings
    form two categories at runtime: action settings and preference settings. Action
    settings are boolean and maintained by the action item's setChecked value.
    Preferences can be anything and are maintained as public variables, mostly in MW
    (MainWindow) and managed in the PrefDlg class.

    Not all settings can be read immediately, as the classes that use them may not yet
    have been created. Examples are IconView parameters and the recent folders menu
    list, since IconView and the menus have not been created yet. Therefore some
    settings are assigned up front and the rest are assigned as needed.

------------------------------------------------------------------------------------------
Startup Sequence
------------------------------------------------------------------------------------------

    • Load QSettings
    • Set default values if no settings available (first time run)
    • Set preference settings including:
        • General (previous folder etc.)
        • Slideshow
        • Cache
        • Full screen docks visible
        • List of external apps
        • Bookmarks
        • Recent folders
        • Ingest history folders
        • Workspaces
    • Create actions and set checked based on persistent values from QSettings
    • Create bookmarks with persistent values from QSettings
    • Update external apps with persistent values from QSettings
    • Load shortcuts (based on being able to edit shortcuts)
    • Execute updateState function to implement all persistent state settings

    • Select a folder
        • Load datamodel with QDir info on each image file
        • Add the rest of the metadata to datamodel (incl. thumbs as icons)
        • Update the image cache

------------------------------------------------------------------------------------------
Dock Window State (versioning)
------------------------------------------------------------------------------------------

    The QMainWindow dock layout is persisted with saveState()/restoreState() under QSettings
    "WindowState" (MW::settings writes it; MW::showEvent restores it). Both calls pass
    MW::winnowStateVersion.

    RULE: BUMP winnowStateVersion whenever the SET of docks changes (a dock added or removed).
    A WindowState saved by an older build does not contain the new dock, and restoring it
    leaves that dock's group orphaned as empty "zombie" tab bars; the dock layout then never
    converges and the tabbed docks flicker continuously. The version tag makes restoreState()
    reject the stale state (returns false, changes nothing) so the layout built in
    MW::createDocks/initialize() stands; the state is re-saved at the current version on exit,
    so it self-heals after one launch (cost: a one-time reset of dock sizes/positions).

    Because that fallback layout is what shows after a reject, keep it sane: do NOT tabify a
    dock in one area with a dock in another area (e.g. the left-area metadataDock with the
    right-area embelDock) -- a cross-area tabify drags both into one area and crams every dock
    into a single tab group, where the raised tab oscillates against the squeezed central
    widget. embel + develop tab together on the RIGHT (see initialize() tabifyDockWidget calls).

    developDock (added with the raw editor) was the first dock to trip this: the pre-existing
    saved state predated it AND, once saved during a flicker, the corrupt state perpetuated
    itself across launches until the version bump discarded it.


------------------------------------------------------------------------------------------
Responsive dock tab titles (text <-> graphic)
------------------------------------------------------------------------------------------

    When several docks are tabbed together in a narrow area, their text titles
    ("Folders", "Bookmarks", ...) may not all fit. MW::updateDockTabGraphics (gated
    by G::useDockTitleGraphic) measures the width the text titles need against the
    width of the visible dock: if they fit the tabs show text, otherwise EVERY tab
    in the group switches to a small white glyph (a QLabel centred over each tab,
    since QMainWindow does not paint a dock's windowIcon on tabs and macOS paints
    the tab natively).

    THE FAILURE MODE: if any dock in the group has NO registered glyph, the bar can
    never resolve to graphics -- there is no width at which all tabs are satisfied --
    so the layout thrashes and the tabs flicker continuously. (This is separate from
    the versioning flicker above; same symptom, different cause.) developDock hit
    this because it was added to the group without a glyph.

    RULE: EVERY dock that can be tabbed into the group MUST register a glyph. When
    adding a new dock, do all of:
      1. Create images/icon16/<name>_white.png  -- a 16x16 white-on-transparent glyph
         matching the weight of the existing ones (foldertree/bookmarks/filters/
         metadata/embellish/develop).
      2. Register it in winnow.qrc.
      3. Add <dockTabText> -> ":/images/icon16/<name>_white.png" to the graphicFor
         hash AND <dockTabText> -> <dock> to the dockFor hash, both in
         MW::updateDockTabGraphics.
      4. Append <dockTabText> to dockTextNames in initialize() (dock-bar detection).
      5. Add a tooltip case in MW::dockTabToolTip (dock name + shortcut).
      6. Bump winnowStateVersion (see "Dock Window State" above -- the set of docks
         changed).

    A missing glyph resource is handled gracefully at runtime (the tab keeps its
    text title), but that just reproduces the flicker when the group is too narrow,
    so treat steps 1-3 as mandatory, not optional.


------------------------------------------------------------------------------------------
Dropped dock lands last (rightmost tab)   [WORK IN PROGRESS - DISABLED]
------------------------------------------------------------------------------------------

    STATUS: disabled. The dockLocationChanged connection in initialize() is commented
    out because it broke dock-tab selection (clicking a tab no longer raised its dock).
    The code (MW::moveDroppedDockLast) is left intact for a later fix. The rest of this
    note describes the intended behavior.

    QMainWindow inserts a dock dropped into an existing tab group at the drop position
    (often the front). MW::moveDroppedDockLast (connected to every group dock's
    dockLocationChanged, next to scheduleDockTabUpdate) overrides this: after the drop
    settles it reads the group in visual tab order and, if the dropped dock is not
    already last, re-tabifies it after the current last dock (tabifyDockWidget(last,
    moved)) so it becomes the rightmost tab. The "already last" early-out makes the
    re-tabify's own dockLocationChanged a no-op, so there is no recursion. Tab identity
    uses the text title, falling back to the learned dockTabTitleByKey map in graphic
    mode. (Reordering by dragging a TAB within a group is left to Qt -- only drops that
    fire dockLocationChanged are forced last.) A new dock added to the connect loop in
    initialize() inherits this.


******************************************************************************************

FOLDER LOADING PIPELINE

    Use G::isFlowLogger or G::isLogger to trace program progress.

------------------------------------------------------------------------------------------
Overview
------------------------------------------------------------------------------------------

    Loading a folder runs in two phases that do NOT overlap with each other.
    Phase 1 populates the DataModel with one row per eligible file and runs on
    the GUI thread. Phase 2 reads metadata and icons (MetaRead, on worker
    threads) and caches full-size images (ImageCache, on worker threads); these
    two activities within Phase 2 DO overlap.

    Phase 1 - Build the DataModel (GUI thread)

    • A folder or bookmark click calls MW::folderSelectionChange. The mouse
      modifier decides whether the folder replaces the current selection
      (resetDataModel = true) or is added to it.

    • When resetDataModel is true the prior load is torn down first: MW::stop
      halts the worker threads, MW::reset clears state, and
      DataModel::clearDataModel empties the model. clearDataModel calls
      QCoreApplication::removePostedEvents(this, QEvent::MetaCall) BEFORE
      clear(). QStandardItemModel coalesces item edits into a posted
      _q_emitItemChanged meta-call; if that (or a stale worker setData/setIcon
      meta-call) fired after clear() freed the items it would dereference freed
      pointers and crash. DataModel::newInstance then bumps dm->instance so any
      surviving stale worker signals are ignored.

    • Subfolders are counted (Utilities::subFolderTree) and the folder plus,
      in the recursive case, every subfolder are queued by
      DataModel::enqueueFolderSelection / enqueueOp. macOS .photoslibrary
      bundles are filtered out (they hold huge numbers of derivative files).

    • DataModel::scheduleProcessing starts DataModel::processNextBatch, which
      drains the folder queue in time-sliced batches. It yields to the event
      loop every ~30 ms (kTimeBudgetMs), with a secondary cap of 64 folders per
      tick, rescheduling itself with a zero-delay timer. This time-slicing is
      what keeps the GUI responsive while a large recursive tree loads.

    • DataModel::addFolder handles one directory: dir.entryInfoList() lists
      the eligible files, they are sorted (DataModel::lessThan, or
      lessThanCombineRawJpg when raw+jpg combining is on), and each file is
      appended as a row (setRowCount + DataModel::addFileDataForRow, which fills
      the OS file columns and performs raw+jpg pairing).

      Cost note: the sort/filter proxy (dm->sf) inserts each new row into sorted
      position at O(N), so the whole load is O(N^2). Time-slicing bounds each
      GUI freeze to roughly one folder's insert time but does not change that
      total; a single very large folder loaded late can still block briefly.

    • When the queue empties DataModel emits folderChange, handled by
      MW::folderChanged. It re-enables the panels, sets the current index, and
      either finishes immediately (DataModel::isAllMetadataAttempted, e.g. a
      pure removal with no new rows) or starts Phase 2.

    Phase 2 - Read metadata/icons and cache images (worker threads)

    • MW::folderChanged starts MetaRead with a queued
      QMetaObject::invokeMethod(metaRead, "initialize") followed by
      MetaRead::setStartRow(startRow). MetaRead lives on metaReadThread.

    • MetaRead::dispatchReaders / dispatch assign rows to Reader workers,
      walking outward from startRow (the current image) so the user's position
      is served first. Each Reader reads metadata (Reader::readMetadata ->
      DataModel::addMetadataForItem) and a G::maxIconSize (256 px) icon
      (Reader::readIcon -> DataModel::setIcon1, stored in Qt::DecorationRole),
      delivered to the GUI thread over queued connections. Backpressure
      (dm->queuedReaderEvents, cap 4 x readerCount) defers dispatch when the GUI
      thread falls behind draining those events.

    • Once the start row itself has been read, MetaRead emits select, which
      routes Selection::setCurrentIndex -> MW::updateChange ->
      MW::fileSelectionChange. That loads the current image and starts
      ImageCache, so ImageCache runs concurrently with the rest of MetaRead.

    • ImageCache caches full-size images ahead of and behind the current
      position: ImageCache::setCurrentPosition -> updateTargets ->
      launchDecoders -> decodeNextImage -> ImageDecoder (decode / rotate /
      colorManage) -> ImageCache::fillCache -> cacheImage -> nextToCache.

    • MetaRead::done signals MW::folderChangeCompleted, which builds the
      filters, re-enables the sort/filter menus, applies a single sort pass, and
      resizes the table columns.

    Instances and aborting

    • Every new primary folder increments dm->instance
      (DataModel::newInstance). Worker signals carry the instance they were
      issued under; DataModel slots (setIcon1, addMetadataForItem, setValSf,
      ...) drop any call whose instance no longer matches, so lagging signals
      from an abandoned load are harmless.

    • G::stop and dm->abort are the cooperative stop flags. MW::stop sets
      them, signals the workers to abort, and waits (event-driven, 3 s cap)
      for them to acknowledge before resetting.

------------------------------------------------------------------------------------------
New Folder Selected (Call Sequence)
------------------------------------------------------------------------------------------

    - FSTree::mousePressEvent / FSTree::select / BookMarks item click
    - MW::folderSelectionChange             // prep based on mouse modifier
    - MW::stop                              // halt prior load (if reset)
    - MW::reset                             // reset parameters
    - DataModel::clearDataModel             // purge meta-calls, then clear()
    - DataModel::newInstance                // bump instance (new folder)
    - DataModel::enqueueFolderSelection
    - DataModel::enqueueOp
    - DataModel::scheduleProcessing
    - DataModel::processNextBatch           // time-sliced; ~30 ms yields
    - DataModel::addFolder                  // one directory per call
    - DataModel::addFileDataForRow          // one row per eligible file
    - DataModel::folderChange               // signal -> MW::folderChanged
    - MW::folderChanged                     // set current; start Phase 2
    - MetaRead::initialize                  // queued to metaReadThread
    - MetaRead::setStartRow
    - MetaRead::dispatchReaders
    - MetaRead::dispatch
    - Reader::read                          // prep + dispatch
    - Reader::readMetadata                  // -> addMetadataForItem
    - Reader::readIcon                      // -> setIcon1 (256 px)
    - MetaRead::select                      // signal, at startRow
    - Selection::setCurrentIndex
    - DataModel::setCurrentSF               // current indexes + fPath
    - MW::updateChange
    - MW::fileSelectionChange               // loads image; start cache
    - ImageView::loadImage
    - ImageCache::setCurrentPosition
    - ImageCache::updateTargets
    - ImageCache::launchDecoders
    - ImageCache::decodeNextImage
    - ImageDecoder::decode
    - ImageDecoder::rotate
    - ImageDecoder::colorManage
    - ImageCache::fillCache
    - ImageCache::cacheImage
    - ImageCache::nextToCache
    - MetaRead::done                        // signal, all rows read
    - MW::folderChangeCompleted             // filters, sort, resize cols

------------------------------------------------------------------------------------------
Concurrent Flow - New Folder Selected
------------------------------------------------------------------------------------------

    • Selecting a new folder in the Folders or Bookmarks panel calls
      MW::folderSelectionChange.

    • If the folder replaces the current selection, the prior load is torn
      down: all worker threads are stopped, the DataModel is cleared and all
      parameters are reset (MW::stop, MW::reset, DataModel::clearDataModel).

    • The DataModel is loaded from the folder queue by
      DataModel::processNextBatch, which calls DataModel::addFolder and
      iterates DataModel::addFileDataForRow to populate basic file info per
      row. processNextBatch is time-sliced (yields every ~30 ms) so the GUI
      stays responsive. MW::folderChanged then sets the current index.

    • MetaRead::setStartRow / dispatchReaders / dispatch send Reader workers
      outward from the start row, loading metadata
      (-> DataModel::addMetadataForItem) and 256 px icons
      (-> DataModel::setIcon1).

    • When the start row has been read, MetaRead emits select, which (via
      MW::updateChange -> MW::fileSelectionChange) starts ImageCache. From
      there ImageCache walks setCurrentPosition -> updateTargets ->
      launchDecoders -> fillCache, caching concurrently with MetaRead.

------------------------------------------------------------------------------------------
Concurrent Flow - New Image Selected
------------------------------------------------------------------------------------------

    A new image is selected, which triggers fileSelectionChange:

    • If starting the program, select the first image in the folder.
    • Record the current datamodel row and its file path.
    • Update the thumbView and gridView delegates.
    • Synchronize the thumb, grid and table views. If in loupe mode, load
      the current image.
    • Update window title, statusbar, info panel and classification badges.
    • Update the metadata and image caching.
    • If the metadata has not been cached yet for the selected image
      (usually the first in a new folder), load the thumbnail.
    • Update the cursor position on the image caching progress bar.

    A new image is selected which triggers a scroll event:

    • IconView selection change may trigger a scroll signal, based on the
      current view (thumb, grid or table). MW::thumbHasScrolled /
      gridHasScrolled / tableHasScrolled are signalled.
    • Update the icon range (firstVisible / lastVisible).

------------------------------------------------------------------------------------------
Flow Flags
------------------------------------------------------------------------------------------

    G::isInitializing
    G::stop
    G::dmEmpty
    G::allMetadataLoaded
    G::iconChunkLoaded
    G::isLinearLoadDone
    dm->loadingModel
    dm->basicFileInfoLoaded                 // not used
    G::isLinearLoading                      // not used
    G::ignoreScrollSignal
    isFilterChange

------------------------------------------------------------------------------------------
Current Model Row
------------------------------------------------------------------------------------------

    dm->currentDmRow                        // current row in the DataModel
    dm->currentSfRow                        // current row in the proxy model
    dm->currentDmIdx                        // QModelIndex into the DataModel
    dm->currentSfIdx                        // QModelIndex into the proxy
    dm->currentFilePath

------------------------------------------------------------------------------------------
Icons
------------------------------------------------------------------------------------------

    bool G::iconChunkLoaded
    bool G::loadOnlyVisibleIcons            // not used
    int  G::maxIconSize = 256;
    int  G::minIconSize = 40;
    int  G::maxIconChunk = 25000;
    bool G::useJitIconCache                 // false = brute force; true = JIT layers
    double G::jitIconCacheMemFraction = 0.5 // share of free-remainder for thumbnails
    std::atomic<qint64> G::imageCacheHeadroomMB  // image cache's remaining claim (MB)
    int  G::iconPressureTestLevel = -1      // Layer 3 test override (-1/0/1/2/3)

    int  dm->iconChunkSize                  // icons to cache (brute force = rows; JIT = budget/floor)
    int  dm->firstVisibleIcon;
    int  dm->lastVisibleIcon;
    int  dm->visibleIcons                   // visible icon count, shown views
    int  dm->startIconRange;
    int  dm->endIconRange;
    int  dm->scrollToIcon = 0;

    int  dm->iconCount();
    void dm->clearIconsOutsideChunkRange();
    bool dm->isAllIconChunkLoaded(int first, int last);
    bool dm->iconLoaded(int sfRow, int instance);
    bool dm->isIconRangeLoaded();
    void dm->setIconRange(int sfRow);
    void dm->setChunkSize(int chunkSize);
    void dm->resolveIconChunkSize();        // Layer 1: brute force vs JIT window
    void dm->refineIconChunkSize();         // Layer 2: re-decide with measured footprint
    void dm->applyIconCachePressure();      // Layer 3: shrink-only pressure valve
    int  dm->iconBudgetCount();             // icons that fit the memory budget
    int  dm->iconChunkFloor();              // 3x visible floor (overrides budget)
    double dm->avgIconMB();
    void dm->setIcon();

    bool MW::updateIconRange(QString src);
    void MW::reloadIconChunk();             // re-dispatch after a JIT chunk resize

    MetaRead:
        firstIconRow = dm->startIconRange;
        lastIconRow  = dm->endIconRange;
        bool needIcon;
        MetaRead::needToRead:
            bool isIcon = sf->index(sfRow, G::IconLoadedColumn).data().toBool();
        bool MetaRead::allMetaIconLoaded();
        emit cleanupIcons();   // -> DataModel::clearIconsOutsideChunkRange

------------------------------------------------------------------------------------------
Icon Chunk Size
------------------------------------------------------------------------------------------

    Icons or thumbnails, 256 px on the long side, are stored in the DataModel
    in column 0 under Qt::DecorationRole (the QPixmap); the icon's QRect is
    kept on the same cell under G::IconRectRole. The number of icons to cache is
    dm->iconChunkSize, set per folder in DataModel::resolveIconChunkSize: in brute
    force (G::useJitIconCache == false) it equals the row count (all icons); in JIT
    it is the memory budget (iconBudgetCount), clamped to the row count and to the
    3x-visible floor (iconChunkFloor). See the "Icon Caching" section below for the
    full brute-force vs JIT model (Layers 1-3). As the user moves through the images,
    either by selection or scrolling, the icon range is recalculated based on the
    midVisibleIcon, and iconChunkSize is held at the floor by MW::updateIconRange.

    When the user progresses through the images, the current image is centred
    in the visible views (thumbView, gridView and tableView) using the
    midVisibleCell.

------------------------------------------------------------------------------------------
Thumbnail (Icon) Decoding
------------------------------------------------------------------------------------------

    Thumb::loadThumb (Image/thumb.cpp), called by Reader::readIcon, decodes the
    icon for column 0. It prefers a small embedded preview over decoding the full
    image. The routing is:

        • video                          -> loadFromVideo
        • isEmbeddedThumb (offset+len)   -> loadFromJpgData
        • cr3 (if loadFromJpgData fails) -> loadFromImageIO   (CR3 HDR PQ, see below)
        • heic                           -> loadFromHeic
        • tif (G::useMyTiff)             -> loadFromTiff
        • else                           -> loadFromEntireFile (Qt, scaled)

    Embedded thumbnail format by file type (verified against external format
    documentation - ExifTool, the DNG spec, and lclevy/canon_cr3):

        • JPEG and the raw formats NEF, CR2, ARW, SR2, RAF, ORF, RW2: the embedded
          thumb is JPEG (TIFF tags 513/514 or makernote payloads).
        • CR3: standard CR3 stores JPEG in the THMB (thumbnail) and PRVW (preview)
          boxes. BUT a CR3 shot in HDR PQ mode encodes BOTH THMB and PRVW as HEVC
          (hvc1) - a headerless HEIF bitstream, not JPEG. See "CR3 HDR PQ" below.
        • DNG: the spec allows preview/thumbnail IFDs to be Uncompressed (RGB), JPEG
          or JPEG XL. Winnow's DNG parser scans for JPEG streams and selects the
          smallest JPEG, so m.offsetThumb/lengthThumb always point at a JPEG.
          The DNG parser deliberately does NOT scan the Photoshop IRB (tag 34377)
          for a thumbnail the way Tiff::parse does: DNG stores its previews in
          subIFDs (tag 330), which the parser already handles. An IRB thumb only
          exists if the DNG was round-tripped through Photoshop, so it would at most
          be a fallback when no subIFD JPEG preview is present (rare). ifdPhotoshopOffset
          is still read in dng.cpp so IRB::readThumb() can be added there as a
          !m.lengthThumb-gated fallback if ever needed.
        • TIFF: IRB (Photoshop) thumbs are JPEG (m.thumbFormat = Jpg); chained-IFD
          and subIFD thumbs are TIFF format (m.thumbFormat = Tif). The TIFF-format
          thumbs leave m.lengthThumb == 0, so isEmbeddedThumb is false and they are
          handled by loadFromTiff rather than loadFromJpgData.
        • HEIC / HIF (incl. Canon HEIF stills): handled natively by loadFromHeic
          (libheif on Windows, ImageIO on macOS).
        • The remaining supported types (bmp, png, gif, webp, jp2, tga, svg, etc.)
          have no embedded thumbnail; they are decoded whole by loadFromEntireFile.

    loadFromJpgData reads m.offsetThumb/m.lengthThumb and decodes with the fast
    "JPEG" hint, then falls back to Qt content-sniffing (loadFromData with no format
    hint) if that fails. The fallback makes the path tolerant of any Qt-decodable
    self-contained embedded thumbnail; undecodable buffers return false and fall
    through to the next loader (loadFromTiff / loadFromEntireFile).

    CR3 HDR PQ: the THMB payload is a bare HEVC bitstream (no HEIF file header), so
    neither the "JPEG" decode nor the content-sniff fallback can read it and
    loadFromJpgData fails. loadThumb handles this with a failure-driven fallback:
    when ext == "cr3" and the embedded path did not succeed (and is not retrying a
    locked file), it calls loadFromImageIO before the loadFromEntireFile catch-all.

        • macOS: loadFromImageIO -> macImageIOThumbnail (ImageIO natively supports
          CR3 and returns the embedded preview, or a downscaled decode of the raw).
          This is the supported, tested platform.
        • Windows: loadFromImageIO returns Fail (no safe path - libheif rejects CR3
          and Heic::decodeThumbnail does not guard read failures), so loadThumb
          falls through to loadFromEntireFile as before. A full Windows fix would
          need WIC plus the Canon raw codec.

    This is failure-driven by design: standard CR3 files decode in loadFromJpgData
    and never reach loadFromImageIO, so their behaviour is unchanged. It also does
    not depend on parser-side detection of the hvc1 codec, which keeps the CR3
    parser untouched.

------------------------------------------------------------------------------------------
Tracking Icon Parameters
------------------------------------------------------------------------------------------

    The number of visible icons (visibleCellCount) can change whenever:

        • the size of the view changes
        • the size of the icons changes

    When this happens:

        • IconView::updateVisible is called. A number of IconView operations
          use the parameters defined in IconView::updateVisible.
        • MW::updateIconRange updates the DataModel iconChunkSize based on
          which view has the most visible icons and the current iconChunkSize.

    The first, mid and last visible icons change whenever there is a scroll
    event. The scroll event can be triggered by the user scrolling or
    selecting another image. See also SCROLLING below.

        • MW::thumbHasScrolled, MW::gridHasScrolled or MW::tableHasScrolled is
          triggered.
        • MW::updateIconRange calls IconView::updateVisible.
        • Other visible views are scrolled to sync.

------------------------------------------------------------------------------------------
Other Global Flags
------------------------------------------------------------------------------------------

    G::isSlideShow;
    G::isRunningColorAnalysis;
    G::isRunningStackOperation;
    G::isProcessingExportedImages;
    G::isEmbellish;
    G::colorManage;
    G::modifySourceFiles;
    G::backupBeforeModifying;
    G::autoAddMissingThumbnails;
    G::useSidecar;
    G::renderVideoThumb;
    G::includeSubfolders;

    To remove:
        • MW::scrollToCurrentRow
        • Selection::updateVisible


******************************************************************************************

DATAMODEL CHANGES

    NOTE: The Folder Change and Image Change call sequences below describe the legacy
    MetadataCache architecture (`Cache/mdcache.cpp`), which is no longer compiled. The
    current architecture uses MetaRead + Reader (see FOLDER LOADING PIPELINE and
    CACHING). The
    sequences are retained here as historical reference and need a full rewrite.

------------------------------------------------------------------------------------------
Folder Change (legacy — needs rewrite)
------------------------------------------------------------------------------------------

    MW::folderSelectionChange
        Housekeeping                        // stop cache threads, slideshow, initializing
    DataModel::load                         // basic metadata for every eligible image
    MetadataCache::loadNewFolder            // legacy
    MetadataCache::setRange                 // legacy
    MetadataCache::readMetadataIconChunk    // legacy
    MW::loadMetadataCache2ndPass            // legacy (still declared in mdcache.h)
    IconView::bestAspect
    IconView::setThumbParameters
    MetadataCache::loadNewFolder2ndPass     // legacy
    MetadataCache::setRange                 // legacy
    MetadataCache::readMetadataIconChunk    // legacy
    MW::loadImageCacheForNewFolder          // no longer exists
    // need to repeat best fit?
    IconView::bestAspect
    IconView::setThumbParameters
    ImageCache::initImageCache
    ImageCache::updateImageCachePosition
    ImageCache::updateImageCacheList
    ImageCache::setPriorities
    ImageCache::setTargetRange
    ImageCache::run

------------------------------------------------------------------------------------------
Image Change (legacy — needs rewrite)
------------------------------------------------------------------------------------------

    MW::fileSelectionChange
    MW::updateMetadataCacheIconviewState    // no longer exists
    MetadataCache::fileSelectionChange      // legacy
    MetadataCache::setRange                 // legacy
    MetadataCache::readMetadataIconChunk    // legacy
    ImageCache::updateImageCachePosition
    ImageCache::updateImageCacheList
    ImageCache::setPriorities
    ImageCache::setTargetRange
    ImageCache::run

------------------------------------------------------------------------------------------
Filter Change
------------------------------------------------------------------------------------------

    MW::filterChange
    DataModel::newInstance
    ImageCache::stop
    SortFilter::filterChange
    BuildFilters::update
    IconView::refreshThumbs
    MetaRead::initialize
    ImageCache::filterChange
    thumbView->selectThumb
    MW::scrollToCurrentRowIfNotVisible

    Selection recovery and the central view:
    When no rows are filtered out, MW::filterChange restores the prior selection via
    Selection::recover(), which sets the current index with QItemSelectionModel::NoUpdate
    and therefore does NOT re-emit fileSelectionChange. The central view is thus not
    re-rendered for the current item. A still image keeps showing because imageView
    retains its decoded pixmap, but a video's first frame (rendered by videoView on the
    VideoTab) must be re-asserted explicitly. MW::refresh does this at its tail: for a
    video current item in Loupe mode it re-selects the VideoTab and reloads the first
    frame (guarded so a clip already shown paused is not reloaded).

------------------------------------------------------------------------------------------
Sort Change
------------------------------------------------------------------------------------------

    MW::sortThumbnails
    thumbView->sortThumbs
    ImageCache::rebuildImageCacheParameters
    MW::scrollToCurrentRow

------------------------------------------------------------------------------------------
Combine Raw+JPG
------------------------------------------------------------------------------------------

    When a folder holds raw+jpg pairs (same base name), the "Combine Raw+JPG" toggle
    decides whether each pair is shown as a single combined row (raw is primary, the jpg
    folded into it) or as two separate rows. The state lives in the bool combineRawJpg,
    which is mirrored to G::combineRawJpg, dm->sf, fsTree and bookmarks (the folder and
    bookmark panels show image counts that depend on the setting).

    Entry points (both route to MW::setCombineRawJpg):
        • combineRawJpgAction — File menu item, shortcut Opt/Alt + J. Being checkable,
          the action flips its own checked state before triggered fires.
        • rawJpgStatusBtn — status-bar button. Its lambda calls
          combineRawJpgAction->toggle() (which does not re-emit triggered) then calls
          setCombineRawJpg directly.

    combineRawJpgAction->isChecked() is the source of truth; setCombineRawJpg reads it
    into combineRawJpg. Because the trigger has already flipped the action before
    setCombineRawJpg runs, any early return must restore the action with
    setChecked(combineRawJpg) to keep the action and flag in sync — otherwise they
    desync and a later click is spent re-aligning them instead of toggling.

    Three load states:
        • Folder loaded (dm->rowCount() && G::allMetadataAttempted): full apply — persist
          to QSettings, rewrite the Type column for jpg duplicates ("JPG" vs "JPG+<raw>"),
          dm->rebuildTypeFilter, filterChange (re-pairs/splits the proxy) and
          buildFilters->updateAllCounts (the proxy baseline changed without a
          Filters-tree change, so unfiltered counts must be recomputed too).
        • No folder loaded (dm->rowCount() == 0): toggling is allowed; setCombineRawJpg
          records the setting and refreshes the folder/bookmark counts, then returns
          before the datamodel/proxy rebuild.
        • Folder loading (rows exist but !G::allMetadataAttempted): toggling is blocked.
          rawJpgStatusBtn and combineRawJpgAction are disabled in MW::folderChanged at
          load start and re-enabled in MW::folderChangeCompleted; updateStatusBar also
          enforces the disabled state. setCombineRawJpg additionally guards this case
          (restores the action and shows a "folder is still loading" popup) so a queued
          shortcut cannot slip through.

    Key files: Main/setandupdate.cpp (setCombineRawJpg), Main/status.cpp
    (updateStatusBar — button icon/tooltip/enabled state), Main/initialize.cpp
    (rawJpgStatusBtn wiring), Main/menusandactions.cpp (combineRawJpgAction),
    Main/mainwindow.cpp (folderChanged / folderChangeCompleted enable-disable).

------------------------------------------------------------------------------------------
Folder Tree Auto-Refresh
------------------------------------------------------------------------------------------

    The Folders panel (FSTree) updates itself when subfolders are added or removed on
    disk outside Winnow (e.g. in Finder/Explorer) — no manual Refresh is needed.

    Mechanism: a cross-platform QFileSystemWatcher (FSTree::folderWatcher) wired up in
    FSTree::setupFolderWatcher(). Qt maps this onto the native OS facility (FSEvents on
    macOS, ReadDirectoryChangesW on Windows, inotify on Linux), so one code path serves
    both platforms.

    Scope: QFileSystemWatcher only reports paths explicitly registered, so rather than
    watch the whole filesystem Winnow watches only the folders currently visible in the
    viewport. updateWatchedFolders() diffs the visible set against the watched set and
    applies the delta; scheduleWatchResync() re-runs it (debounced 150 ms) on scroll,
    expand, collapse, selection, resize and refresh.

    On a directoryChanged signal, onWatchedDirectoryChanged() debounces (300 ms) then
    calls FSFilter::refresh() — which uses invalidate() so the view re-queries the live
    FSModel::hasChildren() and the dropdown decoration appears/disappears — and re-syncs
    the watch set so a newly created subfolder is itself watched.

    Note: drive/volume mount/unmount is a separate path (Windows WM_DEVICECHANGE via
    WinNativeEventFilter); this section covers folder content changes only.

    Key files: File/fstree.cpp / File/fstree.h.

------------------------------------------------------------------------------------------
Insertions, Deletions, Modifications
------------------------------------------------------------------------------------------

    Images inserted:   (TBD)
    Images deleted:    (TBD)
    Images modified:   (TBD)

------------------------------------------------------------------------------------------
Instance
------------------------------------------------------------------------------------------

    The instance is a specific datamodel. Every time the datamodel changes — either a
    folder is added or removed — the instance is incremented. When the instance changes:

        • MetaRead must be called.
        • Filtering must be updated.


******************************************************************************************

CACHING

    Metadata caching has been replaced by `MetaRead` (`Cache/metaread.cpp`) dispatching a
    pool of `Reader` workers (`Cache/reader.cpp`). The earlier `MetadataCache` /
    `metadataCacheThread` system (`Cache/mdcache.cpp`, `Cache/mdcacher.cpp`,
    `Cache/mdcachemgr.cpp`) is no longer compiled — those files are not listed in
    `winnow64.pro` and are retained on disk for reference only.

------------------------------------------------------------------------------------------
Overview
------------------------------------------------------------------------------------------

    • When a new folder or a new file is selected, or when a scroll event occurs in
      IconView, MetaRead is started. MetaRead dispatches Reader workers to load
      metadata and icons into the DataModel.

    • As metadata accumulates, MetaRead signals ImageCache::addCacheItemImageMetadata
      so the image cache can begin loading full-size images. When MetaRead crosses
      imageCacheTriggerCount it signals the ImageCache to start.

    • If a new image is selected, MW::fileSelectionChange repositions MetaRead and the
      ImageCache around the new current row.

    • If the user scrolls in the thumbView or gridView, scroll events are sent to
      MW::thumbHasScrolled / MW::gridHasScrolled. A delay ensures that rapid scrolling
      coalesces — the next scroll event supersedes the prior, and MetaRead is only
      re-dispatched when there is a pause or the scrolling is slow. ImageCache is not
      restarted here because a new image has not been selected.

    See `Cache/metaread.cpp` and `Cache/reader.cpp` top-of-file comments for more detail.

------------------------------------------------------------------------------------------
Cache Status
------------------------------------------------------------------------------------------

    • Two status lights on the right side of the status bar (metadataThreadRunningLabel
      and imageThreadRunningLabel) turn from green to red to indicate datamodel metadata
      and image caching respectively. A status light on the lower right of each thumbnail
      is made visible when the associated image is cached.

    • An image cache progress bar shows the status of every image in the folder: target
      range, isCached and cursor position.

    • The progress display is the Progress class (Utilities/progress.h/.cpp), a self-
      contained container widget held by MW::progress and added to the status bar. It
      replaced the older ProgressBar (Utilities/progressbar.*, now removed). Unlike
      ProgressBar, Progress does not reference MW: it owns its own pixmaps, width, colors
      and palette (no MW::progressLabel/progressPixmap), and communicates
      with MW only through public methods and the heightChanged() / clicked() signals.
      Because of this it can later be reparented into a standalone window with no internal
      changes.

        • Progress holds any number of rows. Each row has a text column and a bar
          column, its own bar height, color, font and a Fill style. Each row owns its
          bar pixmap, so an incremental paint (e.g. the moving cache cursor) touches
          only that row.
        • Rows are added at runtime with addRow(name, barHeight, color, fill), which
          returns an int id; the owner holds the id and drives the row with the generic
          updateProgress(id, item, items, color) / clearProgress(id). A row's Fill style
          decides how updateProgress paints: Fill::FromStart fills 0->current item
          (sequential progress, e.g. FocusStack, RawDenoise); Fill::Cell paints only the
          current item's cell (items that complete out of order, e.g. MetaRead reading
          metadata across threads). An invalid color argument defaults to the row's own
          bar color. clearProgress refills the bar background and hides the row.
        • The ImageCache row is the one exception: its moving cursor and directional
          range fills are not expressible as a Fill, so Progress creates it in its ctor
          and keeps its own updateImageCacheProgress()/updateCursor()/
          clearImageCacheProgress(). Its id is exposed via imageCacheRow() for MW's gate.
        • All other rows are added by their owner (MW) at startup in
          MW::createDataModel: MetaRead "Metadata" (4 px, Fill::Cell), FocusStack "Focus
          Stack" (2 px, Fill::FromStart) and RawDenoise "Raw Denoise" (2 px,
          Fill::FromStart). MW stores their ids in progressMetaReadRow /
          progressFocusStackRow / progressRawDenoiseRow. Labels default to empty;
          setRowText() sets them.
        • All rows start hidden and are only shown while active, so at startup (before a
          folder is selected) the container is empty. updateProgress showRow(true) and
          clearProgress showRow(false). The ImageCache row is revealed by
          updateImageCacheProgress/updateCursor (it is not hidden by its clear, since the
          cache "All" repaint clears-then-draws every tick). Hiding a row collapses it
          entirely (text + bar), so the container height fits only the live rows. A row's
          effective visibility is active && enabled: update functions set "active", and
          MW::setCacheProgressEnabled(G::showCacheProgress) calls setRowEnabled() on the
          ImageCache + MetaRead rows so they honor the "Hide caching progress" preference
          even when the widget is shown for a focus stack (FocusStack is not gated). The
          rows block is vertically centered within the widget (paintEvent) with
          topPad/botPad reserved, so a single row lines up with the other status text and
          a filled Progress shows an equal top/bottom margin. MW floors the status-bar
          height at its pre-Progress height (MW::statusBarBaseHeight) so collapsing rows
          never clip the other status text.
        • Progress manages its own container visibility: it shows the widget whenever a
          row is visible and hides it when none are (updateContainerVisibility, called
          from relayout). Hosts therefore never call progress->setVisible() for a per-row
          concern -- doing so would hide a concurrent FocusStack/RawDenoise row when cache
          progress is turned off. Two host hooks remain: setSuppressed(true) force-hides
          the whole container regardless of rows (used for a random slideshow, which does
          no caching), and reset() clears + hides every row when the folder is closed
          (MW::reset / MW::nullFiltration). The cache "Hide caching progress" preference
          is applied only via MW::setCacheProgressEnabled -> setRowEnabled on the
          ImageCache + MetaRead rows; the container then follows automatically.
        • setContainerWidth() sizes the bars to the available status-bar width (driven
          by MW::updateProgressBarWidth via availableSpaceForProgressBar).
          setTextFontSize() is the separate, settable text-size variable.
          setBackgroundColor() keeps the container background matching the status bar.
        • The container height grows to fit the visible rows. Progress::sizeHint() reports
          that height so the status bar grows automatically; heightChanged() is also
          connected in MW to set the status-bar minimum height explicitly. Because
          QStatusBar centers its widgets and reserves its own vertical margin, the
          heightChanged handler requests a few extra pixels (h + overhead) so Progress is
          never compressed below its preferred height — otherwise the reserved top/bottom
          padding gets squeezed out and the margins collapse when Progress fills the bar.
        • clicked() opens Cache preferences (replacing the old StatusProgressLabel event-
          filter branch).

    • The single global flag G::showCacheProgress gates both ImageCache and MetaRead
      caching progress: when false, progress is neither calculated nor displayed (it
      replaces the former MW::isShowCacheProgressBar, ImageCache::isShowCacheStatus and
      MetaRead::showProgressInStatusbar). ImageCache::updateStatus forces InfoOnly,
      MetaRead::dispatch skips its progress emit, and MW::progress is hidden. It is set
      by the "Hide caching progress" preference (Productivity), persisted under the
      settings key "showCacheProgress" (written in MW::writeSettings, read in
      MW::loadSettings), and defaults to true.

    • isCached is stored in datamodel as
      dm->sf->index(row, G::IsCachedColumn).data().

    Functions:
        • MW::createStatusBar                      status labels, right of status bar
        • MW::refreshAfterImageCacheSizeChange      pref calls update cache status
        • MW::setThreadRunStatusInactive           caching status lights gray
        • MW::updateImageCachingThreadRunStatus    image caching light green/red
        • MW::updateMetadataCachingThreadRunStatus metadata caching light green/red
        • MW::setCacheStatusVisibility             updateState toggles progressBar
        • MW::updateImageCacheStatus               ImageCache updates cache status
        • MW::setCachedStatus                      sets isCached, refreshes views

------------------------------------------------------------------------------------------
Icon Caching
------------------------------------------------------------------------------------------

    • Each icon is a 256 px longside QIcon, so we want to balance how many we load when
      weighing performance vs memory footprint.

    • Icons are stored in the DataModel at dm->(row, 0).data(Qt::DecorationRole). The
      loaded status can be determined two ways:
        1. dm->itemFromIndex(dmIdx)->icon().isNull()      // used up to 2023
        2. dmIdx.data(Qt::DecorationRole).isNull()        // switched to in 2023

    • The number of icons to cache is governed by dm->iconChunkSize. How it is set is
      decided per folder in DataModel::resolveIconChunkSize (called from endLoad) and
      depends on the G::useJitIconCache flag.

    Brute force (G::useJitIconCache == false, the default)

        • dm->iconChunkSize is set to the DataModel row count, so every icon is cached
          regardless of folder size. Simplest behaviour; preferred for typical folders.
          (The G::memoryOverrunFlag bail in DataModel::setIcon1 remains the crash backstop.)

    Just-in-time, JIT (G::useJitIconCache == true)

        • A folder is cached fully only when its projected thumbnail footprint fits a memory
          budget; otherwise dm->iconChunkSize is clamped to a bounded sliding window and the
          existing machinery (setIconRange, MetaRead::needToRead, clearIconsOutsideChunkRange)
          loads and evicts icons as the user moves. JIT has three layers:

        Layer 1 - predictive budget (resolveIconChunkSize / iconBudgetCount)
            At folder load the per-icon footprint is estimated (worst case 256x256x4 bytes)
            and compared to a budget = free memory after a safety reserve and the image
            cache's own remaining claim (G::imageCacheHeadroomMB, published by
            ImageCache::memChk), times G::jitIconCacheMemFraction. Fits -> full; else ->
            bounded window. iconChunkSize is then clamped to the row count and to a hard
            floor (iconChunkFloor): max(256, 3 x visibleIcons). The floor OVERRIDES the
            budget — if the image cache has claimed most of memory and the budget would fall
            below 3x the visible icons, the floor wins and the thumbnails take the memory
            they need; the image cache self-throttles because its own ceiling is derived
            from G::availableMemoryMB, which drops as the thumbnails are held. The floor is
            re-applied by MW::updateIconRange whenever the visible count changes (e.g.
            switching to the denser Grid view). Note: sizing is purely budget + floor; there
            is no separate "visible x 3" target beyond this floor.

        Layer 2 - measured refinement (refineIconChunkSize)
            As real icons load, DataModel::setIcon1 accumulates their true size. After ~200
            icons the budget is recomputed with the measured average and the window may grow,
            or be promoted back to full, when thumbnails turn out smaller than the worst-case
            estimate. Grow-only and one-shot per folder.

        Layer 3 - defensive pressure valve (applyIconCachePressure)
            A 1 s GUI-thread timer watches memory pressure (DataModel::memoryPressureLevel,
            derived from G::availableMemoryMB). On warn it halves the window; on critical it
            clamps to the visible page and evicts. It never grows. A 60 s cooldown plus a
            2 GB available-memory high-water mark (hysteresis) gate when the latch releases,
            so the cache cannot thrash between shrink and re-grow. Releasing does not re-grow;
            Layer 1/2 re-decide on the next folder load. Brute force is not touched by the
            valve.

    • Independent of the above, MW::updateIconRange still grows dm->iconChunkSize to at least
      the number of visible icons when more thumbnails are visible (in thumbView, gridView or
      tableView) than the current chunk, e.g. after an app or thumbnail resize.

    • When dm->iconChunkSize changes or there is a scroll event, the metadata cache read
      process is rerun to update the icons.

------------------------------------------------------------------------------------------
Load Responsiveness (keeping the GUI usable during a large/recursive load)
------------------------------------------------------------------------------------------

    The cost of a folder load on the GUI thread is dominated not by decoding but by the
    flood of QStandardItemModel signals each row change emits, every one of which runs the
    proxy (SortFilter) plus all three views' (thumbView, gridView, tableView) handlers
    synchronously. On a ~9000-image recursive tree this was ~30 s of GUI-thread time,
    starving keystrokes/mouse for seconds. Three measures cut it to near zero:

    1. Batch per-row writes (always on). DataModel::setIcon1 and setValDm wrap their setData
       calls in a QSignalBlocker and emit ONE dataChanged, mirroring addMetadataForItem.
       Four/two signals per icon -> one.

    2. Notify only visible rows (G::useVisibleOnlyIconEmit, default true). setIcon1/setValDm
       emit their one dataChanged only when the row is within [firstVisibleIcon,
       lastVisibleIcon] (DataModel::iconRowVisible). Off-screen icons are stored without
       notification and paint correctly when scrolled to. ~9000 emits -> ~visible-count.

    3. Batch the folder insert (G::useBatchedFolderInsert, DEFAULT FALSE). DataModel::addFolder
       can add a folder's rows with a single setRowCount (one rowsInserted, one view relayout)
       and fill the cells with signals blocked + one dataChanged for the range, instead of
       one rowsInserted + ~20 dataChanged per file (the enumeration freeze).

       The Z-A reordering that previously made this unusable is now fixed: while a batched
       load runs, DataModel::scheduleProcessing turns the proxy's dynamic sort/filter OFF
       (sf->setDynamicSortFilter(false)), so the one wide dataChanged per folder no longer
       re-sorts the inserted block. Inserted rows map in source (lessThan / name) order;
       DataModel::restoreProxySortAfterLoad re-enables dynamic sort in one pass when the
       load finishes or aborts (every processNextBatch exit path), restoring the same final
       order the per-row path produces. Still DEFAULT FALSE pending A/B measurement on the
       recursive pictures tree (see G::isPerfProbe below); flip it to true to test.

    Items 1-2 are independent of brute force vs JIT — they help any mode. Set the flags
    false to restore the prior per-signal behavior.

------------------------------------------------------------------------------------------
How to Test Icon Caching
------------------------------------------------------------------------------------------

    Flags (Main/global.cpp) — flip at runtime via the debugger or a temporary code change:

      G::useJitIconCache          false = brute force (cache all); true = JIT layers active.
      G::jitIconCacheMemFraction  share of the free-memory remainder budgeted for thumbnails
                                  (default 0.5).
      G::iconPressureTestLevel    forces the Layer 3 pressure level for deterministic testing
                                  (see values below).
      G::useVisibleOnlyIconEmit   true = setIcon1/setValDm notify views only for visible rows.
      G::useBatchedFolderInsert   true (default) = addFolder inserts a folder's rows in one
                                  batch (one rowsInserted + one dataChanged); cuts Phase-1
                                  insert ~34%. Dynamic sort is disabled during the load and
                                  restored once at the end so order is preserved — see Load
                                  Responsiveness item 3.
      G::isPerfProbe              true = emit concise "[PERF]" lines (off in production):
                                    Phase1 load: rows, folders, enum(ms), sort(ms), msg(ms),
                                      insert(ms), other(ms) (= wall - measured buckets:
                                      event-loop yields / paint / end-of-load re-sort), wall(ms).
                                    Phase2 metaRead: rows, cycles, dispatch(ms) (metaReadThread
                                      busy), needToRead(ms) (proxy read hot loop, subset),
                                      ntrCalls, ntrAvg(us), thumbRetries (G::probeThumbRetryCount),
                                      wall(ms) (folderChanged -> done).
                                  To compare on the recursive pictures tree, clear the OS file
                                  cache between runs (`sudo purge`) and flip the flag under test.

      G::throttleFolderLoadMsg    true (default) = DataModel::addFolder throttles its
                                  "Searching for images…" progress message to ~50 ms instead
                                  of once per folder, removing ~1.3 s of synchronous repaints
                                  on a 1333-folder load. Set false to restore the per-folder
                                  emit (the msg(ms) A/B baseline).

      G::decodeRawEngine          which engine decodes RAW -> WorkingImage (demosaic
                                  + global colour/baseline luma NR + start WB/black/
                                  white). A/B knob:
                                    winnowDecodeRawEngine (default) = in-house
                                      decoder; portable, the only engine on Windows
                                      and the canonical focus-stacking engine
                                      (deterministic, CFA-level control).
                                    appleDecodeRawEngine = macOS Core Image
                                      (CIRAWFilter); GPU demosaic + NR, macOS-ONLY --
                                      callers fall back to winnow off-mac. Both
                                      converge on WorkingImage, so Develop / local
                                      luma NR / OutputTransform are engine-agnostic.
                                  Sticky: persisted in QSettings ("decodeRawEngine",
                                  saved/loaded next to "useRaw"), so the Develop
                                  "Demosaic" combo survives app restarts. The combo is
                                  rebuilt from G::decodeRawEngine (not a hardcoded
                                  default) so folder/image selection keeps the choice.
                                  Off-mac an appleDecodeRawEngine value is normalized
                                  to winnow on load.

    Load performance findings (~9000-image / 1333-folder recursive tree, cold cache)

      Phase 1 (build DataModel, GUI thread): baseline wall 2408 ms -> 542 ms (4.4x) after
      three fixes. Instrumentation overturned the prior assumption that the proxy/view
      insert dominated:
        • msg ~1299 ms (58%) — emit centralMsg per folder, each a synchronous repaint() in
          MW::setCentralMessage. Fixed by G::throttleFolderLoadMsg.
        • enum ~580 ms, cold≈warm (not I/O-bound) — QDir recompiled ~50 wildcard name-filter
          regexes per folder. Fixed by entryList(QDir::Files, NoSort) + a QSet<QString>
          suffix check (DataModel::supportedExtSet); ~230 ms after.
        • insert ~370 -> ~250 ms — batched per-folder insert (G::useBatchedFolderInsert).
        • sort ~9 ms — negligible (per-folder std::sort of ~7 files).

      Phase 2 (read metadata/icons, worker threads): wall ~9.6 s, and it is I/O/decode-bound
      in the Reader pool. The metaReadThread is ~1% busy (dispatch ~87 ms, needToRead
      ~10 ms), thumbRetries = 0 (the 100 ms loadThumb contention loop never fires), and the
      removed Reader::readIcon double-scale was redundant (loadThumb already scales to
      thumbMax). Consequence: the worker-thread QSortFilterProxyModel access (a latent
      thread-safety hazard) is NOT worth a snapshot rewrite for speed — it is ~0.1% of the
      load. The only real Phase-2 lever is reducing decode volume (G::useJitIconCache icon
      windowing) or reader-pool tuning, both bigger policy/architecture changes.

      Phantom sort column: a persisted sortColumn could be out of range (== G::TotalColumns)
      and reach the proxy via QTableView::setSortingEnabled (which calls sort() directly,
      bypassing MW::sortChange/IconView::sortThumbs). Sorting by an invalid column reverses
      the model under DescendingOrder (Z-A). SortFilter::sort() now clamps any out-of-range
      column to -1 (no sort / source order) — the single chokepoint covering every path; the
      workspace load also sanitizes a stale persisted sortColumn.

    Diagnostics — Help > diagnostics. The DataModel report shows the live state:
      iconChunkSize, icon cache mode (JIT window / brute force), icon loaded (row ranges),
      icon avg footprint (measured vs worst-case), icon budget (icons),
      icon chunk floor (3x vis), imageCacheHeadroomMB, iconChunkRefined, memoryPressureLevel,
      iconCachePressureLatched.

    Status indicator — the metadata thread "circle" (MW::updateMetadataThreadRunStatus)
    is colour-coded on success so the active icon-cache mode is visible at a glance:
      red     metadata thread running
      green   idle, brute force (G::useJitIconCache == false)
      blue    idle, JIT caching active (G::useJitIconCache == true)
      orange  idle, forced pressure (G::iconPressureTestLevel >= 2)  [precedence: orange > blue]
      yellow  idle, load failed

    Brute force
      • Set G::useJitIconCache = false and load any folder. Confirm "icon loaded" spans
        every row and "icon cache mode" = brute force (full).

    Layer 1 - predictive budget
      • Set G::useJitIconCache = true. A folder small enough to fit the budget caches fully;
        a large folder (or a lower G::jitIconCacheMemFraction) shows "icon cache mode" =
        JIT window with iconChunkSize < row count.

    Layer 2 - measured refinement
      • In JIT window mode, scroll/load past 200 icons. Watch the refineIconChunkSize log
        line; "icon avg footprint" switches to "(measured, n=...)" and iconChunkSize may grow
        or be promoted to full (iconChunkRefined = true).

    Layer 3 - pressure valve (use the test override so you need not starve the machine)
      G::iconPressureTestLevel values:
        -1  use the real signal (availableMemoryMB)
         0  normal AND memory recovered      -> latch releases after the 60 s cooldown
         1  warn                             -> window halved, latched
         2  critical                         -> window clamped to the visible page, latched
         3  normal BUT memory not recovered  -> reaches the release branch, held by high-water

      Suggested sequence (G::useJitIconCache = true, a windowed folder loaded):
        1. = 2   critical: iconChunkSize drops to the visible page, icons evicted, latched.
        2. = 3   pressure "cleared" but not recovered: after 60 s, still latched (roomy=false).
        3. = 0   recovered: latch releases on the next 1 s tick (iconCachePressureLatched=false).
        4. = -1  restore the real signal.
      Confirm each transition in the DataModel diagnostics and the applyIconCachePressure
      log lines.

------------------------------------------------------------------------------------------
Memory Governor (process footprint, decode concurrency)
------------------------------------------------------------------------------------------

    Winnow bounds its resident process footprint so a large folder cannot run the machine
    out of memory. There are three independent consumers of memory and three cooperating
    governors. The important thing to understand first: the ImageCache (imCache) is usually
    NOT the biggest consumer. When Decode Raw is on, in-flight raw decoders are.

    The three memory consumers
      • ImageCache (Cache/cachedata.h imCache): the decoded 8-bit display QImages, W*H*4
        bytes each. Byte-accounted by icd->sizeMB(). Bounded by the cache ceiling (below).
      • WorkingImageCache (Develop/workingimagecache.h): a SEPARATE, byte-budgeted LRU of
        the scene-linear float WorkingImage (W*H*3 floats = 3x the QImage) kept so a Develop
        slider re-render skips demosaic. Default budget 768 MB; setMaxBytes() is not called,
        so it stays at the default. It is NOT counted by icd->sizeMB().
      • In-flight decoders: each of decoderCount (= QThread::idealThreadCount) decoder
        threads, while decoding, transiently holds its working buffers. For an embedded-JPG
        or ordinary JPEG/HEIC decode that is ~one QImage. For a FULL-SENSOR RAW decode
        (UnpackCfa -> Demosaic -> RawColor) it is the float WorkingImage plus demosaic
        scratch -- about 8x the cached QImage. This is the key asymmetry: the CACHED result
        is identical for a raw and a same-resolution JPEG, but the raw DECODE working set is
        ~8x, and it is multiplied across all decoder threads.

    Why Decode Raw off is cheap
      With Decode Raw off (G::useRaw == false) a raw file is shown from its EMBEDDED JPG
      preview (ImageDecoder gates the sensor path on G::useRaw). That is one libjpeg pass,
      ~QImage-sized. So none of the raw-specific governing below engages, and that path runs
      at full decoder concurrency, unchanged. Raw-off performance/stability is the priority
      and is deliberately left untouched.

    Governor 1 - raw decode concurrency cap (Cache/imagecache.cpp, the primary control)
      Because N raw decodes at ~8x-QImage each can exceed the memory cap even while imCache
      is nearly empty, the number of CONCURRENT full-sensor raw decodes is bounded:

          rawDecodeLimit = clamp( (memoryAbortMB * 0.50) / (decodedQImageMB * 8),
                                  1, decoderCount )

      Gated at decodeNextImage (the single choke point reached from both launchDecoders and
      fillCache): if the row willUseSensorDecode() and activeRawDecodes >= rawDecodeLimit,
      the decoder is parked (cycling[id] = false) instead of starting another decode. A
      finishing sensor decode decrements the counter in fillCache and frees a slot; the
      remaining active decoders keep the pipeline full, so throughput holds at the limit.
      willUseSensorDecode() is false for JPEG/HEIC and for the Decode-Raw-off embedded path,
      so those are never gated. RawFormat::HasSensorDecoder(ext) is the cheap, no-alloc test
      (a hash lookup, no decoder constructed). It and RawFormat::Create() both derive from a
      single extension->decoder-factory table, rawFactories() in ImageFormats/Raw/rawformat.cpp
      -- so "which extensions have a sensor decoder" lives in exactly one place and the two
      cannot drift out of sync. Adding a new format's sensor decoder is a one-line change
      there. (Note ext membership is an over-approximation: e.g. a compressed ARW matches
      but its Decode() falls back to the embedded JPG. That is the safe direction for a
      memory guard -- the first decode must assume the heavy path -- and such fallbacks
      decode cheaply anyway, so throttling them costs nothing.)
      decoderIsRaw[id] marks a decoder on the sensor path so the decrement fires exactly once
      per decode; both are reset per folder in initialize(). On a big-RAM machine the larger
      memoryAbortMB opens the limit back toward full concurrency.

    Governor 2 - image cache ceiling clamp (ImageCache::memChk)
      maxMBCeiling (the cache's growth target) is derived from availableMemoryMB but then
      clamped so cache budget + measured non-cache footprint + a per-decoder reserve stays
      under memoryAbortMB. Because "non-cache footprint" is measured live, it already
      includes in-flight decoders, so the cache automatically plans fewer images while raw
      decodes are consuming memory.

    Governor 3 - memory watchdog (MW::memoryWatchdogTick, 50 ms GUI-thread probe)
      A backstop, graduated rather than a single hard abort. cap = G::memoryAbortMB
      (computeMemoryAbortMB scales it to host RAM, e.g. ~18.4 GB on 24 GB):
        • footprint >= cap        -> engage the ImageCache throttle: setMemoryThrottled(true)
          parks decoders at decodeNextImage, and memoryPause() (queued to the cache thread)
          shrinks/trims imCache. No dialog.
        • footprint <= 0.88*cap   -> release the throttle, memoryResume() relaunches decoders.
        • footprint >= cap + max(2 GB, cap/10) -> genuine runaway the throttle could not
          contain: fall back to the old hard onMemoryOverrun abort dialog (prevents OS OOM).
      With the concurrency cap in place the footprint should not approach the cap during raw
      traversal, so the watchdog rarely fires; it exists for the pathological case.

    Diagnostics
      ImageCache::diagnostics() prints a "Memory Footprint / Raw Decode" block (process
      footprint, imCache MB/count, WorkingImageCache MB/count/budget, G::useRaw, decoderCount,
      active/peak raw decodes, the computed limit and per-decode estimate) and a "Memory
      Pressure Warnings" ring buffer of timestamped PAUSE / RESUME / CRITICAL events. A large
      footprint with a small imCache and a WorkingImageCache still at ~budget confirms the
      memory is in the concurrent raw decoders, which the concurrency cap bounds.

    Tunables
      rawDecodeLimit()'s 0.50 budget fraction and x8 per-decode factor (Cache/imagecache.cpp);
      the watchdog's 0.88 resume hysteresis and critical headroom (MW::memoryWatchdogTick);
      computeMemoryAbortMB() (Main/global.cpp) for the host-scaled cap itself.


******************************************************************************************

SCROLLING

    When the user scrolls a datamodel view (thumbView, gridView or tableView), the
    scrollbar change signal is received by thumbHasScrolled, gridHasScrolled or
    tableHasScrolled. The other views are scrolled to sync with the source view's
    midVisibleRow. The first, mid and last visible items are determined in
    MW::updateIconRange and the metaReadThread is called to update the metadata and icons
    in the range (chunk).

    However, when the program syncs the views this generates more scrollbar signals that
    would loop. This is prevented by the G::ignoreScrollSignal flag. The same flag is also
    employed in fileSelectionChange, where visible views are scrolled to centre on the
    current selection.

    Shift + navigation key (extending a selection) does not move the current index, so the
    centre-on-current scroll above does not fire. Instead, Selection::select (Shift branch)
    follows the extending selection: it scrolls the visible view(s) just enough to expose
    the cell one beyond the new selection edge, in the direction of extension (the "look-
    ahead" cell). This keeps both the last selected item and the next item visible so the
    user can see where the selection is heading. The scroll uses EnsureVisible (not centre),
    so it is a no-op when the look-ahead cell is already on screen and is clamped to the
    start/end of the datamodel. See IconView::ensureRowVisible and TableView::ensureRowVisible.

------------------------------------------------------------------------------------------
Painting Delays
------------------------------------------------------------------------------------------

    When the user changes modes in MW (i.e. from Grid to Loupe), an IconView instance
    (either thumbView or gridView) can change state from hidden to visible. Since hidden
    widgets cannot be accessed, we need to wait until the IconView becomes visible and
    fully repainted before attempting to scroll to the current index.

    Also, when an IconView resize event occurs — triggered by a change in the gridView
    cell size (thumbWidth) that requires justification, a change in the thumbDock
    splitter, or a resize of the application window — we also need to wait until
    scrollbar repainting is completed.

    The last paint event is identified by calculating the maximum of the scrollbar range
    and comparing it to the paint event, which updates the range each time. With larger
    datasets (i.e. 1500+ thumbs) it can take a number of paint events and hundreds of ms
    to complete.

    NOTE: An earlier IconView::waitUntilScrollReady helper has been removed. The wait-
    for-paint logic now lives inline in the scroll-handling paths in IconView and MW.

------------------------------------------------------------------------------------------
Scrollbar Change Flow
------------------------------------------------------------------------------------------

    • The ScrollBar signal valueChanged triggers MW::thumbHasScrolled,
      MW::gridHasScrolled or MW::tableHasScrolled.

    • MW::updateIconRange updates the first, mid and last visible icons from
      all views to find the greatest range of visible icons by calling
      IconView::updateVisible and TableView::updateVisible. dm->iconChunkSize
      is adjusted if it is less than the visible icon range.

    • Concurrent metadata loading:
        MW::update(midVisibleIcon) is called and it loads any missing icons and cleans up
        any orphaned icons (not in the iconChunkRange).

------------------------------------------------------------------------------------------
Resizing Icons or the Icon Viewport
------------------------------------------------------------------------------------------

    When the IconView viewport is resized or the icon size is changed, the result is a
    scroll event. Since we want to keep the view centred on its original state, the middle
    or centre icon is tracked as dm->scrollToIcon. G::resizingIcons is set for the resize
    and the subsequent scroll event does not reset dm->scrollToIcon. MW::thumbHasScrolled
    or MW::gridHasScrolled resets G::resizingIcons to false.

    This allows the three datamodel views to be synced using dm->scrollToIcon.

    Persisted icon size (between sessions and workspaces):
        IconView::assignedIconWidth is the reference width that rejustify() uses to
        compute thumbs-per-row and then justify the cell width to exactly fill the row.
        The justified result lands in iconWidth, which can differ from assignedIconWidth.
        Critically, iconWidth is viewport-dependent: a rejustify() at a narrow width
        shrinks iconWidth toward ICON_MIN, while assignedIconWidth is unchanged.

        Rule: persist (save) assignedIconWidth, not iconWidth. iconWidth can be corrupted
        by a transient rejustify() — at startup the hidden gridView receives resize events
        at a narrow width, each scheduling a rejustify() ~500ms later (IconView::resizeEvent)
        that justifies iconWidth down to e.g. 62. Saving iconWidth persisted that transient
        size, so the grid reverted to ~62 on the next launch; assignedIconWidth survives the
        transient. (thumbView has an extra guard: rejustify() skips "Thumbnails" while
        G::isInitializing, and MW::showEvent applies its size via setThumbSize().)

        Rule: on restore, set assignedIconWidth = iconWidth BEFORE calling rejustify().
        rejustify() justifies from assignedIconWidth, so if only iconWidth is restored the
        saved size is discarded and rejustify() uses a stale/uninitialised reference.

        Where these rules are applied:
            • MW::writeSettings / MW::createGridView  - between-sessions save/restore.
              writeSettings saves gridView->assignedIconWidth as thumbWidthGrid;
              createGridView loads it into iconWidth then copies to assignedIconWidth.
            • MW::saveWorkspace / MW::invokeWorkspace  - per-workspace save/restore for
              both thumbView and gridView (save assignedIconWidth; on restore set
              assignedIconWidth = iconWidth before rejustify()).
            • MW::defaultWorkspace  - sets assignedIconWidth = iconWidth before rejustify()
              so the hard-coded defaults (thumb 100, grid 160) are honoured.


******************************************************************************************

FOCUS PREDICTOR

    Focus Predictor uses a machine learning model to predict where in the image the user
    will want to review the image focus — for example, the eye of the subject. When
    enabled, and the loupe view is zoomed, as the user advances each image is panned to
    the predicted focus review location.

------------------------------------------------------------------------------------------
Architecture
------------------------------------------------------------------------------------------

    Data
        All the training images and a CSV file that records the focus point, type and
        path:
            /Users/roryhill/Documents/Documents - Quark/FocusPointTrainer

    Python
        Code used to create the model:
            /Users/roryhill/Projects/FocusPointTrainer
        • config.py
        • model.py
        • train_focus_model.py              (creates the model)
        • validate_focus_model.py

    Model
        focus_point_model at /Users/roryhill/Projects/FocusPointTrainer in two versions:
        .pth and .onnx.

    FocusPointTrainer
        A C++ class that creates the "labeling" data from the training images in
            /Users/roryhill/Documents/Documents - Quark/FocusPointTrainer/focuspoint.csv.

    FocusPredictor
        A C++ class that uses the model to predict the focus point for the current image.

------------------------------------------------------------------------------------------
Process
------------------------------------------------------------------------------------------

    1. The training data is created using FocusPointTrainer to append the focus point,
       type and path for each training image to focuspoint.csv.

    2. In Code, train_focus_model is run (run Python in terminal). This creates
       focus_point_model.pth and focus_point_model.onnx. The .pth file is the native
       torch format and is converted to .onnx — the format that OpenCV uses. This enables
       C++ to use the model with the OpenCV library.

    3. Copy focus_point_model.onnx to /Users/roryhill/Projects/Winnow64/DetectionModels.

    4. Use FocusPredictor::predict to return the normalized image coordinates for the
       predicted focus location.

------------------------------------------------------------------------------------------
Implementation
------------------------------------------------------------------------------------------

    When focus prediction is enabled (Toggle Pan to Focus) and the loupe view is zoomed,
    the ImageView pans to the predicted focus point. See:
        • end of ImageView::loadImage
        • ImageView::predictPanToFocus              (get focus coord and pan)

    When this occurs, the location of the ImageView viewport within the scene is signalled
    to IconView, which tells IconViewDelegate to show a rectangle in the thumbView
    thumbnail matching the viewport in the scene. See:
        • ImageView::predictPanToFocus              emits the rectangle and image aspect
        • IconView::loupeRect                       receives rect/aspect, updates delegate
        • IconViewDelegate::setVpRect


******************************************************************************************

HELP SYSTEM

    All in-app help pages are HTML files loaded by HtmlWindow (Utilities/htmlwindow.cpp).
    Every page shares a single stylesheet, Docs/help.css, which is applied automatically
    by HtmlWindow via QTextDocument::setDefaultStyleSheet. To restyle every help page at
    once, edit help.css; no other files need to change.

------------------------------------------------------------------------------------------
Architecture
------------------------------------------------------------------------------------------

    Utilities/htmlwindow.cpp        HtmlWindow widget (a QScrollArea hosting a
                                    QTextBrowser). Loads the HTML and applies
                                    Docs/help.css as the document's default stylesheet,
                                    and sets a qrc base URL on the document so relative
                                    <img src="images/..."> resolves.

    Docs/help.css                   Shared dark-theme stylesheet for every help page.
                                    Defines body, h1-h3, a, code, pre, .highlight, .warn.
                                    Some values are fallbacks: at load time HtmlWindow
                                    overrides the body font-size with the app font size
                                    (G::strFontSize), scales the h1-h3 font-sizes by
                                    G::fontSize/12, and sets the h1-h3 colours from the
                                    theme-tied G::header1/2/3Color. The static values in
                                    help.css apply only when viewing the file outside the
                                    app (e.g. VS Code Live Preview).

    Docs/_template.html             Reference template demonstrating every supported
                                    element (headings, paragraphs, span colors, link,
                                    image, lists, code block, table). Copy this when
                                    creating a new page.

    Docs/<topic>.html               One file per help topic. The <head> only needs:
                                        <link rel="stylesheet" href="help.css">
                                    Qt ignores the <link> (it already loaded help.css
                                    as the default stylesheet); VS Code's Live Preview
                                    honors it, so the editor view matches Winnow.

    Docs/images/                    Image assets used by help pages. Reference with a
                                    relative path: <img src="images/Foo.png">.

    winnow.qrc                      Every .html and image referenced from help must be
                                    listed here so it is bundled into the binary.

    HtmlWindow lifecycle            Constructor sets Qt::Window (top-level with title bar)
                                    and Qt::WA_DeleteOnClose (self-cleanup on user close).
                                    Callers pass a parent (`window()` for embedded
                                    widgets, `this` for top-level dialogs) so the help
                                    window is destroyed with its owner. MW::closeEvent
                                    also sweeps findChildren<HtmlWindow*>() as a safety
                                    net.

------------------------------------------------------------------------------------------
Adding a New Help Page
------------------------------------------------------------------------------------------

    1. Copy Docs/_template.html to Docs/<yourpage>.html.

    2. Edit in VS Code (see "VS Code Workflow" below). Keep the existing
       <link rel="stylesheet" href="help.css"> in the <head>; only add a <style> block
       if you need per-page overrides.

       OR use BBedit.

    3. Place any images in Docs/images/ and reference them with a relative path,
       e.g. <img src="images/MyDiagram.png">.

    4. Register the new files in winnow.qrc under the existing /Docs prefix:
           <file>Docs/<yourpage>.html</file>
           <file>Docs/images/<YourImage>.png</file>     (if any)

    5. Open it from C++. Pattern (mirrors Filters::howThisWorks at filters.cpp:1767):

           #include "Utilities/htmlwindow.h"

           void MyWidget::someHelp()
           {
               QRect r = QRect(mapToGlobal(geometry().topLeft()), geometry().size());
               new HtmlWindow("Winnow - My Topic",
                              ":/Docs/yourpage.html",
                              QSize(700, 600), r, window());
           }

       The fifth argument (`window()` or `this`) sets the parent so the help window
       closes with its owner. Do not store the returned pointer or delete it manually;
       WA_DeleteOnClose handles cleanup.

    6. Build. No other wiring is required.

------------------------------------------------------------------------------------------
Editing Help Formatting Globally
------------------------------------------------------------------------------------------

    Edit Docs/help.css. Every help page picks up the change on the next launch (the .css
    is loaded fresh by HtmlWindow each time a help page opens).

    Runtime overrides: HtmlWindow::applyStyle() appends rules on top of help.css so help
    text tracks the app font and theme. These win over the help.css values (later rule,
    equal specificity), so editing them in help.css alone has no effect in the app:
      • body font-size  -> app font size (G::strFontSize)
      • h1/h2/h3 font-size -> help.css design size scaled by G::fontSize/12
      • h1/h2/h3 colour -> G::header1/2/3Color (theme-tied; set in WidgetCSS::css())
    applyStyle() also re-runs on every font/theme change (HtmlWindow::refreshOpenWindows,
    called from MW), so open help windows update live. To change these, edit applyStyle()
    (font-size/colours) or the G::headerNColor values in WidgetCSS::css() (colours).

    Constraint: QTextBrowser supports only Qt's HTML 4 / CSS 2.1 subset
        https://doc.qt.io/qt-6/richtext-html-subset.html
    Avoid flexbox, grid, CSS custom properties, and HTML5 semantic tags — Qt silently
    ignores them.

    For per-page overrides, add a <style> block to that page's <head>. It cascades on
    top of help.css.

------------------------------------------------------------------------------------------
VS Code Workflow
------------------------------------------------------------------------------------------

    Recommended editor: VS Code (free) with the Microsoft "Live Preview" extension.
    Reasons:
      • Plain-text HTML editing — will not silently inject CSS3 that Qt ignores
        (which is why WYSIWYG editors like Typora and Pinegrow are NOT recommended).
      • Side-by-side browser preview that refreshes on save.
      • The <link rel="stylesheet" href="help.css"> in each page makes the preview
        match Winnow's rendering closely.

    Mac-native alternative: BBEdit (free tier) — same plain-text philosophy.

    Expected differences between VS Code preview and Winnow:
      • Font sizes will not match pixel-for-pixel (Chromium vs QTextBrowser defaults).
        help.css sets explicit sizes on body/h1/h2/h3 to narrow the gap, but Winnow
        overrides them at runtime (body = app font size, headings scaled), so the
        preview shows help.css's static fallback sizes, not Winnow's actual sizes.
      • Heading colours differ: VS Code shows help.css's static h1/h2/h3 colours, while
        Winnow overrides them with the theme-tied G::header1/2/3Color.
      • Anything outside the CSS 2.1 subset (above) will render in VS Code but not in
        Winnow. When in doubt, check in Winnow.

------------------------------------------------------------------------------------------
Entry Points
------------------------------------------------------------------------------------------

    Each entry lists the UI trigger, then its caller and the HTML file opened:

    Help menu / "?" shortcut
        MW::help  ->  winnowhelp.html
    Folders panel "?" button
        FSTree::howThisWorks  ->  foldershelp.html
    Filters panel "?" button
        Filters::howThisWorks  ->  filtershelp.html
    Embellish panel "?" button
        EmbelProperties::coordHelp  ->  embelcoordsystem.html
    Filmstrip / status bar help
        MW::helpThumbViewStatusBarSymbols  ->  helpfilmstrip.html
    Find Duplicates "?" button
        FindDuplicatesDlg::on_helpBtn_clicked  ->  findduplicateshelp.html
    Find Duplicates delta info
        FindDuplicatesDlg::on_deltaThresholdToolBtn_clicked  ->  pixeldeltahelp.html
    Ingest dialog "?" button
        IngestDlg::on_helpBtn_clicked  ->  ingestautopath.html

    Not converted to HtmlWindow (intentionally):
      • MW::helpShortcuts (helpform shortcutsform.ui) — interactive QTreeWidget, not HTML.

------------------------------------------------------------------------------------------
Orphaned Help Documents
------------------------------------------------------------------------------------------

    The following files live under Docs/ but have no C++ references. Kept for now because
    they may contain useful source material; can be deleted when confirmed.

    Docs/ingesthelp.htm                Old ingest help, .htm extension. Still listed in
                                       winnow.qrc but no caller loads it. Replaced by
                                       Docs/ingestautopath.html.

    Docs/ingesthelp_files/             Word-export sidecar directory for ingesthelp.htm.

    Docs/filtershelp.fld/              Word-export sidecar directory left over from the
                                       original Word-authored filtershelp. Still listed
                                       in winnow.qrc (3 entries) but unused.

    Docs/versions.html                 200 lines of release-notes content. No C++ refs
                                       found. May be loaded dynamically somewhere worth
                                       verifying before deleting.

    Non-help files in Docs/ that don't belong here:
      • Docs/Border shapes.xlsx
      • Docs/QPixmap performance.xlsx
      • Docs/winnow.png
      • Docs/debug.log


******************************************************************************************
8. ACTION ENABLE/DISABLE RULES
******************************************************************************************

------------------------------------------------------------------------------------------
Overview
------------------------------------------------------------------------------------------

    Menu/toolbar actions that act on the loaded folder, the current selection, or the set
    of picked images are enabled/disabled centrally in MW::enableSelectionDependentMenus()
    (`Main/menusandactions.cpp`).  Pick-dependent actions are additionally kept in sync by
    MW::updatePickDependentActions() (`Main/mainwindow.cpp`), which is called from the
    pick-toggle paths.

    Actions that are always valid (app/window/help/about, dock-visibility toggles,
    workspaces, preferences, open folder, color manage, combine raw+jpg, diagnostics,
    folder-tree/USB context actions handled by the rename* slots, and "Manage External
    Applications") are never gated here.

------------------------------------------------------------------------------------------
Conditions and Triggers
------------------------------------------------------------------------------------------

    Four state conditions drive enabling.  Each must have a trigger that re-runs the
    relevant update when the condition can change, otherwise actions get stuck disabled:

    • dmHasRows  (dm->rowCount() > 0 — a folder is loaded)
         Triggers: fileSelectionChange (auto-select after load), selectionChanged (on
         clear), and view-mode switches — all call enableSelectionDependentMenus().

    • hasSelection / selection count  (sel->count())
         Trigger: the selection model's selectionChanged signal is connected directly to
         enableSelectionDependentMenus() in MW::createSelection()
         (`Main/initialize.cpp`).  This is essential: extending/shrinking a selection
         with shift/ctrl does NOT change the current index, so fileSelectionChange alone
         would never re-evaluate count-dependent actions (e.g. focusStack stuck disabled
         after growing the selection to two).

    • isAnyPick  (dm->isAnyPick())
         Triggers: updatePickDependentActions() is called from every pick-toggle path in
         `Main/pick.cpp` (togglePick, togglePickUnlessRejected, togglePickMouseOverItem,
         toggleReject, updatePickFromHistory, recoverPickLog) and after ingest in
         MW::setIngested() (`Main/setandupdate.cpp`).  enableSelectionDependentMenus()
         also calls it.

    • isUpDown  (gridView or tableView visible)
         Trigger: view-mode switches call enableSelectionDependentMenus().

------------------------------------------------------------------------------------------
The Menu vs Shortcut Pitfall
------------------------------------------------------------------------------------------

    Disabling a QMenu (menu->setEnabled(false)) only greys its entry in the menu bar.  It
    does NOT disable the window-level keyboard shortcuts of its child actions, because each
    child is added to MW via addAction() with its own shortcut (e.g. rate "1", label "6",
    filter "Shift+6").  Those shortcuts keep firing with no folder loaded.

    Therefore menus whose children carry shortcuts are gated with the local setMenuEnabled()
    helper, which disables the menu AND every child action individually:
    ratingsMenu, labelsMenu, sortMenu, filterMenu, utilitiesMenu, embelExportMenu.
    The Open With submenu is iterated the same way but stays enabled so manageAppAction
    remains reachable; only its per-app actions are gated.

------------------------------------------------------------------------------------------
Disabled Shortcut Feedback
------------------------------------------------------------------------------------------

    A disabled QAction does not fire and Qt does not consume its shortcut, so the key
    falls through as a normal KeyPress.  To tell the user why nothing happened, the qApp
    event filter (MW::eventFilter, `Main/mainwindow.cpp`) intercepts KeyPress events:

      • MW::disabledActionForShortcut(seq) scans MW::actions() for an action whose
        shortcut matches the pressed key sequence.  It returns the match only if it is
        disabled AND no enabled action owns the same shortcut (an enabled action would
        already have handled the key, so there is nothing to explain).
      • MW::actionDisabledReason(a) just reads the action's "disabledReason" dynamic
        property and formats it as "<action name> — <reason>."  The reason is recorded
        when the action is gated: enableSelectionDependentMenus() uses a gate() helper
        (and setMenuEnabled() for menu children) that sets both the enabled state and the
        property; updatePickDependentActions() does the same for ingest/nextPick/prevPick.
        Because the reason lives next to the gating, the two never drift.  A disabled
        action with no property set falls back to "not available right now".
      • On a match the reason is shown via G::popup and the key is consumed (return true).

    Guards: skipped during initialization, for auto-repeat, for modifier-only presses,
    when focus is in a text-entry widget (QLineEdit/QTextEdit/QPlainTextEdit), and when
    focus is in another top-level window (fw->window() != MW) so typing is never hijacked.

    Modeless dialog shortcut guard
    ..........................................................................................
    MW's single-key shortcuts use the default Qt::WindowShortcut context, which Qt
    activates whenever the action's window is anywhere in the parent chain of the active
    window.  A modeless dialog parented to MW (e.g. the Preferences dialog) therefore does
    NOT shield these shortcuts on its own: a digit typed into a Preferences spin box would
    fire rate "1" instead of entering text.  The enabled-action case fires via Qt's shortcut
    map (QEvent::ShortcutOverride) before the KeyPress feedback handler runs, so the
    eventFilter also intercepts ShortcutOverride: when focus is in another window
    (fw->window() != MW) and the key matches an MW-owned shortcut (MW::ownsShortcut(seq)),
    it accepts the override and consumes the event, delivering the key to the focused widget
    as normal input.  This scopes suppression to MW's own shortcuts, leaving the dialog's
    own shortcuts (typically Ctrl-modified) free to fire.

------------------------------------------------------------------------------------------
Develop Mode Local Shortcuts
------------------------------------------------------------------------------------------

    Eight keys mean something different in Develop mode than in Preview mode:

        R   Crop / Transform panel      developTransformAction   (Preview: unbound)
        N   New layer                   developNewLayerAction    (Preview: unbound)
        M   New mask                    developNewMaskAction     (Preview: unbound)
        O   Mask overlay tint on/off    toggleMaskOverlayAction  (Preview: Open folder)
        S   Spot removal tool           developSpotAction        (Preview: Slideshow)
        X   Export (STUB, see below)    developExportAction      (Preview: Reject)
        H   Histogram / vectorscope     developScopesAction      (Preview: unbound)
        P   Run/apply preset (STUB)     developRunPresetAction   (Preview: Pick)

    Why not just give these actions a shortcut
    ......................................................................................
    Qt allows exactly ONE QAction per key sequence.  Bind "S" to both slideShowAction and
    developSpotAction and QShortcutMap reports an ambiguous overload and fires NEITHER, so
    a mode-local key cannot be expressed as a second QAction.  Nor can the keys be swapped
    on/off the actions at mode change: that fights the user's shortcut customisations in
    the "Shortcuts" settings group, and any desync leaves keys bound to the wrong mode.

    Mechanism
    ......................................................................................
    The Develop actions are created with NO QKeySequence.  MW::loadDevelopShortcuts,
    called at the end of MW::loadShortcuts, fills developShortcuts -- a QHash<int,
    QAction*> of bare Qt::Key -> action.  MW::eventFilter routes both QEvent::ShortcutOverride and
    QEvent::KeyPress to MW::developShortcutIntercept while G::operationMode == Develop:

      • ShortcutOverride is accepted, which is what cancels the global QAction (Qt sends
        the override to the focus widget before the shortcut system runs).
      • The KeyPress that follows triggers the Develop action.  The work is done here and
        not on the override, because the override is only sent for keys a global action
        owns -- R/N/M/H own none -- and because consuming the KeyPress is what makes the
        selection rule below a real "ignore" rather than a fall-through.

    In Preview mode the arbiter is never called and every key behaves as the global table
    says.  This is the same two-step pattern ReplacePanel::eventFilter uses.

    Precedence: tool-local > mode-local > global
    ......................................................................................
    A key an armed tool owns ([ / ] brush size, A auto-mask, Cmd+Z undo stroke, Enter and
    Esc) is deliberately NOT in developShortcuts.  ImageView::event claims those from
    their global actions itself while its tool is live, so tool-local keys outrank
    mode-local ones without the table needing to know tools exist.  The converse also
    matters: the spot tool must NOT claim S, or S could arm the tool but never disarm it.

    Contextual mode-local keys (active only while a panel/tool is up)
    ......................................................................................
    Some Develop keys are only valid while a specific tool is active, so they cannot live
    in the static developShortcuts table (that would steal them mode-wide).  They are
    instead a guarded branch INSIDE developShortcutIntercept, ahead of rule 1, keyed off
    the tool's "active" flag.  Today the only case is the Transform panel: while
    developCropEditing is true, C/L/W/A beat their window-level shortcuts (A Run Droplet,
    C Compare) and call TransformPanel::handleTransformShortcut(key); Esc calls
    MW::cancelDevelopTransform (discard the session + hide the panel); Enter/Return calls
    toggleDevelopTransform (commit + hide, i.e. R again), EXCEPT while a warp quad is
    traced (imageView->cropIsWarp()) where the branch falls through so ImageView commits
    the quad (rectify) via its own Enter path.  See the Transform section.  Doing it in
    the
    arbiter -- not in ImageView::event as tool-local keys are -- is deliberate: the crop
    overlay usually does NOT hold keyboard focus, so an ImageView ShortcutOverride claim
    never fires (this was the first, wrong, fix).  The arbiter is a qApp filter, so it
    works regardless of focus.  ADD FUTURE panel/tool-scoped letter keys here the same way
    (guard on the tool's active flag), NOT to developShortcuts and NOT to
    ImageView::event.

    Space: temporary zoom/pan over a Develop tool
    ......................................................................................
    In Develop mode a mask / spot / crop tool owns the mouse (brush, lasso, eyedropper,
    spot pins, crop handles), so the loupe's click-to-zoom / drag-to-pan is unavailable
    while editing.  HOLDING Space borrows it back: the tool's mouse branches are skipped
    and the base loupe gesture runs (click toggles zoom, drag pans a zoomed image);
    releasing Space resumes the tool with no state lost.  It applies to every tool,
    including Crop.

    The wheel / two-finger scroll zooms too: in Develop mode ImageView::wheelEvent zooms
    the image under the cursor (ImageView::wheelZoom, cursor-anchored) instead of the
    Preview-mode prev/next navigation.  While a Spot or Brush/Object tool is armed the
    wheel still resizes that tool, so hold Space (spacePanOverride) to zoom with the wheel
    there -- the same flag that gates the mouse branches also bypasses the tool's
    wheel-resize.

    Space is bound to zoomToggleAction globally, and ImageView usually lacks keyboard
    focus in Develop, so this is driven from MW::eventFilter (a qApp filter that sees
    KeyPress AND KeyRelease), NOT developShortcutIntercept (which never sees KeyRelease
    and is not meant for held-state keys).  The ShortcutOverride pass is accepted to free
    the key from zoomToggleAction; KeyPress/KeyRelease call
    ImageView::setSpacePanOverride, which flips the spacePanOverride flag that every
    ImageView mouse handler is gated on.
    A value editor keeps Space as a literal character (same guard as the arbiter), auto-
    repeat is ignored, and ApplicationDeactivate clears the flag so a Space released in
    another app can't leave it stuck.  A change that arrives mid-stroke is deferred (see
    ImageView::applyDeferredSpacePan) and applied on the next mouse release so a partly-
    painted stroke isn't corrupted.

    Guards (MW::developShortcutIntercept, in order)
    ......................................................................................
      • Focus in a QDialog -> bail.  Mirrors the MODELESS DIALOG SHORTCUT GUARD, which
        cannot cover these keys because it matches via MW::ownsShortcut() and a Develop
        action has no sequence to match.  Without it a bare S typed into a Preferences
        field would arm the spot tool.  Floating QDockWidgets are separate top-level
        windows too but belong to the MW workspace, hence the QDialog test.
      • Focus in a value editor (QAbstractSlider / QAbstractSpinBox / QLineEdit /
        QComboBox) -> bail, so arrows still nudge a Develop slider and letters still type.
      • Auto-repeat -> bail; every Develop key is a toggle or pops a dialog.

    Selection keys are ignored in Develop mode
    ......................................................................................
    Arrows, PgUp/PgDn and Home/End (bare or Shift'd, plus the Ctrl+Shift+Alt pick-nav and
    Shift+Ctrl+Right random-image combos) are swallowed in Develop mode unless thumbView
    has the focus, so the keyboard cannot navigate off the image being developed.  Alt+key
    is EXCLUDED -- Alt+arrow / Alt+Home scroll and pan within the image, which is useful
    while developing -- but Ctrl+Shift+Alt+Left/Right IS pick navigation, hence the Ctrl
    test in isSelectionKey().  Focus is tested with MW::thumbViewHasFocus().

    Note the bare Left/Right navigation intercept further down MW::eventFilter bypasses
    the shortcut system entirely (it is faster than a menu shortcut).  It needs no mode
    guard of its own only because the arbiter runs first and consumes the arrow; keep that
    ordering.

    Discoverability
    ......................................................................................
    A key-less QAction renders no shortcut column, so each Develop action embeds its hint
    in its text after a tab ("New Layer\tN") -- the idiom pickAction and jumpAction
    already use for their multi-key hints.  The keys are surfaced in three places: the
    Utilities > Develop submenu (built in MW::createUtilMenu; MW::syncDevelopMenuEnabled
    greys the mode-local items outside Develop mode so they never advertise a key that
    does something else), the Develop editor-bar button tooltips, and Help > Shortcuts.

    The one exception is Save Develop Preset (Cmd+Shift+N): a MODIFIED combo owned by no
    other action, so it is a normal QAction with a real QKeySequence (not in the arbiter's
    bare-key table). It is kept Develop-only by its enabled state -- setOperationMode
    enables it only in Develop mode and syncDevelopMenuEnabled greys the menu item in
    Preview -- since a disabled QAction's shortcut does not fire. See the "Develop presets
    (save)" subsection in the DEVELOP section.

    Export is a stub
    ......................................................................................
    MW::developExport() only shows a "not implemented yet" popup: the render-to-file path
    does not exist yet.  Action, menu item, shortcut and help entry are wired, so the
    feature needs only that function body.

------------------------------------------------------------------------------------------
Rules by Action
------------------------------------------------------------------------------------------

    Requires dmHasRows (folder loaded):
      • File: revealFile(+context), reportMetadata, copyFolderPathFromContext,
        copyImagePathFromContext, rename, saveAsFile, refreshCurrent (always available
        while a folder is loaded — needed to pick up files added externally).
      • Edit: selectAll, invertSelection, shareFiles, copyFiles, copyImage,
        deleteImages, pick, pickMouseOver, reject, pickUnlessRejected, popPickHistory,
        tokenTemplateEditor, rotateRight/Left, ratingsMenu + rate0..5,
        labelsMenu + label0..5, utilitiesMenu (mediaReadSpeed, visCmpImages).
      • Go: jump, random, and all horizontal/home/end/page navigation (+AddToSelection)
        and the scroll family except up/down.
      • Filter: filterMenu + all filter* children.
      • Sort: sortMenu + all sort* children.
      • Embellish: embelExportMenu + children, embelNew/Read/SaveTemplate.
      • View: asLoupe, asGrid, asTable, copyInfoTextToClipboard, slideShow,
        zoomTo/In/Out/Toggle, thumbsEnlarge/Shrink.

    Requires selection count >= 2:
      • asCompare (compare view), meanStack, focusStack.

    Requires isAnyPick (at least one picked image):
      • ingest, nextPick, prevPick.

    Requires a view that scrolls vertically (isUpDown — grid or table visible):
      • keyUp/Down (+AddToSelection), keyScrollUp/Down.

    Other:
      • Open With per-app actions: require selection (hasSelection); manageAppAction
        always enabled.
      • pasteFiles: enabled when the clipboard holds file URLs
        (Utilities::clipboardHasUrls()).


******************************************************************************************

MEMORY CARD EJECT

------------------------------------------------------------------------------------------
Overview
------------------------------------------------------------------------------------------

    Ejecting a removable card (USB drive / memory card) safely unmounts it from the OS.
    The platform-specific work lives in UsbUtil (`Utilities/usbutil.cpp`):

      • UsbUtil::eject(rootPath) — Windows: DISMOUNT_VOLUME then STORAGE_EJECT_MEDIA via
        DeviceIoControl; macOS: "diskutil eject <mount point>".  Returns true only on a
        real success (the Windows path reports failure if either ioctl fails).
      • UsbUtil::isEjectable(rootPath) — true only when rootPath is a mounted volume that
        is valid (ready, writable) AND removable (DRIVE_REMOVABLE/DRIVE_CDROM on Windows,
        under /Volumes/ on macOS).  Both checks require rootPath to be the volume's mount
        root, not a subfolder.

    MW::ejectUsb(path) (`Main/mainwindow.cpp`) wraps this: if the drive being ejected is
    currently loaded in the datamodel it first calls stop() and clears the FSTree
    selection (reading files from a drive after ejection would crash), then ejects and
    shows a confirmation/failure popup.

------------------------------------------------------------------------------------------
Entry Points
------------------------------------------------------------------------------------------

    Eject is reachable ONLY from the context menus of the Folders (FSTree) and Bookmarks
    panels — there is no main-menu eject action.  The context action
    (ejectActionFromContextMenu -> MW::ejectUsbFromContextMenu) ejects the drive of the
    moused-over folder (mouseOverFolderPath).  MW::renameEjectUsbMenu() sets the action's
    label ("Eject Memory Card <drive>") and enabled state from that folder as the context
    menu is built.

------------------------------------------------------------------------------------------
Drive Root Normalization
------------------------------------------------------------------------------------------

    The moused-over (or ingest source) path is usually a subfolder, e.g. a DCIM folder,
    but isEjectable/eject require the volume mount root.  Both MW::ejectUsb() and
    MW::renameEjectUsbMenu() therefore normalize with QStorageInfo(path).rootPath() before
    calling UsbUtil, so eject works (and the menu item enables) for any folder on the card,
    not only when the volume root itself is selected.

------------------------------------------------------------------------------------------
Auto-Eject After Ingest
------------------------------------------------------------------------------------------

    The ingest dialog has an "eject" checkbox bound to the persisted setting autoEjectUsb
    ("autoEjectUSB").  When set, MW::ingest() ejects the ingest source card after a
    successful FOREGROUND ingest — both the gotoIngestFolder early-return path and the
    normal path, after the copy has finished (IngestDlg runs the foreground copy
    synchronously during exec()).  The eject targets dm->primaryFolderPath(), normalized
    to the drive root by ejectUsb().

    Auto-eject is foreground-only by design.  The ingest dialog disables AND clears the
    eject checkbox whenever background ingest is selected (IngestDlg::
    on_backgroundIngestChk_stateChanged, `Dialogs/ingestdlg.cpp`), and the checkbox is
    initialized after the background checkbox so a persisted autoEjectUsb=true is
    reconciled to false at construction.  As a result autoEjectUsb is always false for a
    background ingest; MW::ingest() additionally guards the eject with !isBackgroundIngest.
    A background ingest never auto-ejects (ejecting mid-copy would fail / corrupt the
    transfer).

******************************************************************************************
CHECK FOR UPDATES
******************************************************************************************

------------------------------------------------------------------------------------------
Overview
------------------------------------------------------------------------------------------

    Winnow checks whether a newer release is available for download.  Distribution is
    plain downloads (notarized DMG on macOS, Inno Setup installer on Windows) from the
    DigitalOcean server at https://winnow.ca, so the check only NOTIFIES the user and, on
    request, opens the platform download URL in the default browser — it does not
    auto-install.  (The old Windows-only Qt `maintenancetool` path is gone.)

    Two entry points, both calling MW::checkForUpdate(bool silent) (`Main/mainwindow.cpp`):
    • Help → "Check for updates" — silent == false (loud): also reports "up to date" and
      network errors via popups, and ignores the skip-version setting.
    • Startup, from MW::showEvent — a deferred QTimer::singleShot fires checkForUpdate(true)
      4 s after the window shows, gated by the checkIfUpdate preference and skipped when
      G::isTest (no network during headless / soak runs).  Silent: it only speaks when an
      update is available and has not been skipped.

------------------------------------------------------------------------------------------
Mechanism
------------------------------------------------------------------------------------------

    checkForUpdate() does an async GET (QNetworkAccessManager, no blocking wait) of a
    small per-platform descriptor:
        macOS:   https://winnow.ca/winnow_mac/version.json
        Windows: https://winnow.ca/winnow_win/version.json
    Shape: { "version": "2.05",
             "url":     ".../current/Winnow2.05.dmg",
             "notes":   ".../winnow/versions.html" }

    MW::onUpdateCheckReply() parses the JSON and compares with QVersionNumber (NOT string
    compare).  The running version is WINNOW_VERSION (`Main/mainwindow.h`, exposed as
    versionNumber), single-sourced from CMake project(Winnow VERSION ...).  If the remote
    version is greater it shows the UpdateAppDlg dialog (`Dialogs/updateAppDlg.*`):
    • Download → MW::downloadAndOpenUpdate(url): the installer is fetched in-app (no
      browser, progress shown in G::popup) to the Downloads folder, then opened — on
      macOS `open` mounts the DMG and brings its Finder window to the front (on top of
      Winnow); on Windows the downloaded Inno Setup installer is launched.
    • Skip this version → UpdateAppDlg::skipRequested() is true; the caller stores the offered
      version in updateSkipVersion ("updateSkipVersion") so the silent startup check no
      longer prompts for it (a loud manual check still does).
    • Not now → no state change; offered again next launch.

    Preference: checkIfUpdate ("checkIfUpdate", default true) — "Check for program updates
    at startup" in the preferences dialog (`Dialogs/prefdlg.*`, updateAppChk).

------------------------------------------------------------------------------------------
Publishing version.json
------------------------------------------------------------------------------------------

    The deploy scripts publish the descriptor (see packaging/README.md):
    • macOS deploy.command promote step 7b writes/uploads winnow_mac/version.json from the
      promoted version, and curl-checks it.
    • Windows deploy.ps1 writes/uploads winnow_win/version.json when $VersionJsonTarget is
      set.  Its url points at winnow_win/current/, so the Setup.exe must be in current/.
    Build dependency: the client needs Qt6::Network (added to CMakeLists.txt).


******************************************************************************************

FIND DUPLICATES

    FindDuplicatesDlg (`Dialogs/findduplicatesdlg.*`) finds duplicates of a set of
    candidate images among a set of target images. Launched from MW::findDuplicates,
    which runs it modally (exec) and, on accept, rebuilds the Compare filter so the
    flagged duplicates can be shown.

    Two columns of data drive everything:
    • The candidate "A" list — the images being checked. Held in the datamodel proxy
      dm->sf, addressed through the aSfRows mapping (see below).
    • The target "B" list — bItems, built from the include/exclude folders dragged into
      the dialog. Each B entry caches name, type, createdDate, aspect, duration and a
      thumbnail (im) for pixel comparison.

------------------------------------------------------------------------------------------
Candidates (A list) and the model mapping
------------------------------------------------------------------------------------------

    Candidates are the SELECTED images in dm->sf, not all filtered images. setupModel()
    builds aSfRows, a list mapping each candidate index (the loop variable 'a' / the row
    in the dialog's QStandardItemModel) to its dm->sf proxy row:
    • aSfRows is populated from dm->isSelected(sfRow) over the filtered proxy.
    • If nothing is selected it falls back to all filtered rows, so the dialog is never
      empty.

    Because the candidate list is no longer 1:1 with dm->sf, every candidate access to
    the datamodel goes through aSfRow(a) (returns -1 when out of range, so an invalid
    QModelIndex yields safe empty data rather than throwing). The dialog model row and
    results[a]/matches[a] stay keyed by the candidate index a; dm->sf is reached only via
    aSfRow(a).

------------------------------------------------------------------------------------------
Targets (B list)
------------------------------------------------------------------------------------------

    buildBList() expands the include folders (optionally recursing subfolders), subtracts
    the exclude folders, then appends a bItem per supported file found.
    • If no candidate is a video (candidatesHaveVideo() is false, tested via
      G::VideoColumn), videos are excluded from the targets too — there is no point
      comparing against video duplicates when no candidate is a video.
    getMetadataBItems() then fills each bItem's metadata and thumbnail.

------------------------------------------------------------------------------------------
Comparison and thumbnail normalization
------------------------------------------------------------------------------------------

    on_compareBtn_clicked() runs the search: clear → buildBList → getMetadataBItems →
    pixelCompare() (when "same pixels" is checked) or findMatches() (criteria match:
    name/type/date/aspect/duration). matched candidates are checked in the dialog table.

    Pixel comparison only makes sense if the two thumbnails share dimensions, so both the
    candidate (A, from the decoration-role icon) and target (B) thumbnails are run through
    normalizeBThumb(): scaled to 256 px on the long side (aspect preserved, the same
    256 box used to extract the A icon) and converted to QImage::Format_RGB32. For
    matching-aspect images this yields identical dimensions.

    compareRGB() sums the per-channel absolute differences over the overlapping
    rectangle. It reads pixels directly from constScanLine() as QRgb (both inputs are
    already RGB32) — QImage::pixelColor() per pixel was ~50x slower and, run over every
    A x B combination, was the original cause of the long main-thread stall (beachball).

------------------------------------------------------------------------------------------
Video frames (async sync)
------------------------------------------------------------------------------------------

    Video target thumbnails come from FrameDecoder asynchronously: getMetadataBItems()
    queues each video (addToQueue) and increments pendingVideoFrames; the first frame
    arrives later at setImageFromVideoFrame(), which stores the normalized thumbnail and
    decrements the counter. getMetadataBItems() then WAITS for pendingVideoFrames to
    reach 0 before returning (pumping the event loop, with a 30 s timeout and abort
    check), so pixelCompare() never runs against null video thumbnails. pendingVideoFrames
    is reset in clear().

------------------------------------------------------------------------------------------
Responsiveness
------------------------------------------------------------------------------------------

    The whole search runs synchronously on the GUI thread. The dialog's progress/abort
    hooks were originally gated on G::useProcessEvents, which is globally false, so the
    GUI froze for the entire run. The compute loops (getMetadataBItems, pixelCompare,
    findMatches) now pump the event loop unconditionally; pixelCompare throttles to one
    update/processEvents per percent to avoid flooding the loop. on_compareBtn_clicked
    guards re-entry (if (isRunning) return), disables compareBtn during the run, and
    resets isRunning / re-enables the button on every exit path.

------------------------------------------------------------------------------------------
Output
------------------------------------------------------------------------------------------

    on_updateDupsAndQuitBtn_clicked() writes the result back to the datamodel: it first
    clears G::CompareColumn on ALL dm->sf rows (candidates are only a subset, so a
    candidate-only pass would leave stale flags), then sets it true for each checked
    candidate via aSfRow(a), and accepts the dialog. MW::findDuplicates then refreshes
    the Compare filter.

------------------------------------------------------------------------------------------
Dialog layout
------------------------------------------------------------------------------------------

    findduplicatesdlg.ui lays the upper area out as leftFrame then rightFrame inside
    horizontalLayout_13. leftFrame (on the left) holds the candidate images table (tv);
    rightFrame (on the right) holds the target include/exclude folder lists and the
    comparison controls. The frame names reflect their on-screen positions.


==========================================================================================
RAW DECODING (SENSOR DEMOSAIC)
==========================================================================================

------------------------------------------------------------------------------------------
Architecture
------------------------------------------------------------------------------------------

    Full-sensor RAW decoding (demosaic), based on LibRaw, is being ported in phases. It is
    separate from the existing embedded-JPG preview path: a RAW file still shows its
    embedded JPG until a per-format sensor decoder exists.

    Pipeline (ImageFormats/Raw/):
        ImageDecoder::load()
          -> RawFormat::Create(ext)          factory; "arw" -> SonyRaw, else nullptr (embedded JPG)
          -> RawFormat::Decode()             shared, camera-agnostic pipeline:
               1. UnpackCfa()                 PER-FORMAT virtual (THE override point):
                                              vendor packed/compressed bitstream -> normalised
                                              CFA mosaic (RawImage). Lives in the vendor .cpp
                                              (canon.cpp, nikon.cpp ...) as a class SEPARATE
                                              from that file's metadata parser.
               2. SubtractBlack()            shared per-2x2-position black subtraction + clip.
               3. Demosaic::Run()            shared, camera-agnostic: Bilinear3x3 for Bayer,
                                              XTransWindow (5x5) for Fuji X-Trans; AHD/DCB later.
                                              Operates only on the CFA mosaic + pattern (Bayer) or
                                              raw.xtrans (6x6 map), so it is unit-testable.
               4. RawColor::ToWorking()      shared white balance + camera->sRGB matrix, STOPS
                                              AT LINEAR (no gamma) -> WorkingImage. Real colour
                                              (dcraw cam_xyz_coeff); see COLOUR below.
               5. Develop::Apply()           parametric edits in linear space, applied here for
                                              RAW (native float) when EditParams is non-identity.
               6. OutputTransform::ToImage() baseline tone curve (RAW only) + gamma -> QImage.

    Demosaic is deliberately NOT the override point: the algorithm is shared across cameras;
    only the unpack/decompress step is vendor-specific (mirrors LibRaw's real structure).

    Threading: one RawFormat instance is constructed per decode (per decoder thread). Unlike
    the Metadata-owned parser singletons, RawFormat instances are never shared, so the
    per-decode scratch carries no cross-thread races.

    SonyRaw::UnpackCfa (ImageFormats/Sony/sony.cpp) handles UNCOMPRESSED ARW (Compression 1,
    e.g. A9 II: 14-bit LE uint16, Bayer RGGB) AND lossy "ARW Compressed" (Compression 32767):
        UNCOMPRESSED: reads StripOffsets/Counts, ImageWidth/Length, BitsPerSample, CFAPattern
            (33422), WhiteLevel (50717), via the plumbed RawSensorInfo or a self-walk.
        ARW2 COMPRESSED (decodeSonyArw2, tried first): each 16-byte group encodes 16 photosites
            of one Bayer colour across 32 columns -- an 11-bit max+min, the indices holding them,
            then 7-bit deltas shifted to fit (max-min). The 11-bit values expand through a Sony
            tone curve from tag 0x7010 (4 control points): breakpoints at point>>2, slope starts
            at 4 and doubles each segment; sample = curve[pix<<1]>>2. Columns are de-interleaved
            even-then-odd within each 32-col group. Ported from dcraw's sony_arw2_load_raw and
            VERIFIED byte-identical to libraw on an A9 ARW. Levels/WB/matrix from the same SR2
            tags as the uncompressed path. The NEWER "Lossless Compressed RAW" (Compression JPEG,
            e.g. A1/A7R5) is lossless JPEG -> can reuse LosslessJpeg later; not handled yet.
    Other comp values -> Decode() returns false -> ImageDecoder falls back to the embedded JPG.

    METADATA PLUMBING (RawSensorInfo): the per-file facts UnpackCfa needs (CFA strip
    offset/length, geometry, bitsPerSample, compression, CFA pattern, levels, sample byte
    order) live in struct RawSensorInfo (Metadata/rawsensorinfo.h -- dependency-free), carried
    on ImageMetadata::rawInfo. Sony::parse fills it once during the normal metadata read (from
    the SubIFD0 it already parses -- no second walk). Persistence to the cache decode path:
    DataModel keeps a lock-guarded QHash<fPath, RawSensorInfo> (fPathRawInfo*), written in
    addMetadataForItem and cleared on folder reload; ImageDecoder fetches it via
    dm->fPathRawInfoGet() in cache mode (indMeta carries it directly in independent mode) and
    hands it to RawFormat::Decode.

    GATED ON G::useRaw: when raw decoding is OFF (previews only), both the parse-time fill and
    the addMetadataForItem store are skipped, so the MetaRead path pays nothing -- important
    because the parse-time extraction grows (makernote colour matrix etc.). If useRaw is toggled
    on after a folder is read, rawInfo will be absent from the store and UnpackCfa falls back to
    its self-walk, so correctness holds; the store repopulates on the next folder read.
    SonyRaw::UnpackCfa uses rawInfo when valid and otherwise falls back to its self-contained
    TIFF/EP walk (readSonyTiffSensorInfo). CRUCIAL: the self-walk fills the SAME fields as the
    parse path -- geometry AND colour (model->matrix via tag 272, WB_RGGBLevels 0x7313, BlackLevel
    0x7310). If it only filled geometry, a toggle-on after a useRaw-off load would render with an
    identity matrix / no WB (green tint). Both paths therefore produce identical colour. The
    CFAPattern->enum mapping is shared via cfaPatternFromPlaneColor() in rawimage.h.

    COLOUR (RawColor::ToWorking, real): follows dcraw/libraw cam_xyz_coeff. Input is the model's
    XYZ(D65)->camera matrix (DNG ColorMatrix / adobe_coeff convention), looked up by model in
    ImageFormats/Raw/cameramatrix.cpp -- a FULL port of libraw's adobe_coeff for the mainstream
    still-camera makers (776 models; phone/action/cinema/bare-sensor and 4-colour CMYG/RGBE
    entries omitted). Each row also carries adobe_coeff t_black/t_maximum, read via
    cameraLevelsForModel() and used as a fallback black/white where a decoder would otherwise
    guess (e.g. Canon, whose saturation is below the bit-depth max -- 7D Mark II clips at 13584,
    not 16383). Lookup is a case-insensitive longest-prefix match, so one "Nikon D800" row covers
    D800/D800E and "Sony ILCE-9" covers ILCE-9/9M2, while the most specific key wins (a short
    "Nikon D3" never shadows "D300"). Regenerate from a newer libraw colordata.cpp via
    tools/gen_cameramatrix.py when bodies are added; a wrong matrix is a hue shift, not a crash,
    so verify visually. From it RawColor
    builds camRgb = xyzToCam.rgbToXyz, row-normalises so neutral maps to neutral, derives the
    neutral white balance preMul = 1/rowSum, and inverts to get camera->linear-sRGB (rgbCam).
    It applies the white balance (as-shot raw.camMul when the parser filled it, else preMul;
    normalised to green) and the matrix, clamps to [0,1], stays linear. The matrix + camMul ride
    on RawSensorInfo (xyzToCam, camMul, hasColorMatrix), filled at parse time (model known) so
    they plumb to the cache path like the rest of rawInfo. An unknown model -> identity xyzToCam
    -> sRGB-assumed pass-through (approx, still renders).

    SONY AS-SHOT WB + BLACK: Sony::parse reads the as-shot white balance and black level from
    the SubIFD0 it already parses (the "SR2" tags, stored in the clear on ARW): WB_RGGBLevels
    (0x7313, SSHORT x4, R G1 G2 B) -> rawInfo.camMul (R,G,B,G2, applied directly as gains),
    BlackLevel (0x7310, SHORT x4) -> rawInfo.black, WhiteLevel (0xC61D) -> rawInfo.white. So
    RawColor uses the real as-shot WB (haveAsShot path), not the matrix-derived neutral, on the
    A9 II. (Found by instrumenting the file; exiftool was used only as a reference oracle.)
    LIMITATION: clipped highlights can show a slight tint until highlight recovery lands.

    ADOBE DNG (DngRaw, ImageFormats/Dng/dng.cpp): self-contained -- DngRaw::UnpackCfa walks the
    DNG's TIFF/EP IFDs (via the shared TiffWalk reader), finds the CFA image (Photometric ==
    CFA 32803, full-res, most pixels), and decodes it. Handles UNCOMPRESSED (Compression 1) and
    LOSSLESS-JPEG (Compression 7), in STRIP or TILE layout (tiles placed left-to-right,
    top-to-bottom, edge-cropped). DNG carries its OWN colour, so DngRaw reads it from the file:
    xyzToCam = ColorMatrix2 (D65; else ColorMatrix1), WB camMul = 1 / AsShotNeutral, plus the
    DNG BlackLevel (50714) / WhiteLevel (50717) / CFAPattern (33422) -- no per-model table and
    NOTHING plumbed at parse time (the decode is on-demand, so there is no MetaRead cost; it
    always self-walks). Registered for "dng" in RawFormat::Create. VERIFIED: C++ CFA output is
    byte-identical to the validated reference on a single-strip lossless DNG (Leica M10) and a
    726-tile lossless DNG (Nikon D850 / Lightroom), both of which render to coherent, correctly
    coloured images. LIMITATION: assumes ColorMatrix2's illuminant is D65 (no chromatic
    adaptation yet); packed sub-16-bit uncompressed storage not handled (16-bit storage only).

    FUJIFILM RAF (FujiRaw, ImageFormats/Fuji/fuji.cpp): RAF is a Fuji container ("FUJIFILMCCD-RAW "):
    a big-endian header with offsets (92 = CFA metadata dir, 100/104 = CFA data offset/length) to
    an embedded JPEG, a CFA directory (tag 0x100 = raw H/W; tag 0x131 = the X-Trans 6x6 colour map,
    36 bytes REVERSED -> [yy][xx], 0=R 1=G 2=B; tag 0xC000 = RAFData block with the WB), and the
    CFA data. The FujiIFD that prefixes the CFA block (cfaOff -> little-endian TIFF -> 0xf000 ->
    FujiIFD) carries BitsPerSample (0xf003 -> white), per-CFA BlackLevel (0xf00a) and the
    compressed-strip offset (0xf007). Two CFA encodings, routed on the 16-byte header at cfaOff +
    0xf007 (signature 0x4953 = compressed):
      - UNCOMPRESSED: the CFA block is rawW*rawH*2 bytes plus a small leading header (size =
        cfaLen - rawW*rawH*2, e.g. 2048), little-endian 16-bit samples.
      - COMPRESSED (lossless/lossy, most current bodies): FujiCompressed::decode
        (ImageFormats/Fuji/fujicompressed.cpp) -- a faithful port of libraw's fuji_compressed.cpp
        with all decoder state in a per-call Ctx struct (no globals, so concurrent cache-thread
        decodes are safe). The 16-byte BE header gives lossless flag, raw_type (16=X-Trans,
        0=Bayer), bits, geometry, block_size(0x300)/blocks_in_row/total_lines; each vertical
        block is a gradient-predicted, adaptive Golomb-Rice bitstream decoded into the rawW*rawH
        mosaic. Fuji Bayer bodies (GFX) are RGGB; X-Trans uses the 0x131 map.
    Active-area crop: the decoded mosaic is the FULL sensor (incl. optical-black margins, ~190
    black columns on the right of a GFX); crop to RawImageCropTopLeft (0x0110 = top,left) +
    RawImageCroppedSize (0x0111 = H,W) to drop them, else they show as a black edge. Bayer keeps an
    even origin (RGGB phase); X-Trans rolls the 6x6 map by (top%6,left%6) so the demosaicer, which
    tiles from (0,0), still gets the right colours.
    WB: the 0x2FF0 tag when present, else the as-shot WB_GRBLevels [G,R,B] in the 0xC000 RAFData
    block. That block ALSO holds WB presets (daylight/tungsten/...) that look identical to a WB
    triple, so a plain first-match scan can grab the tungsten preset and badly over-saturate; the
    as-shot WB is instead written as THREE identical consecutive triples, so we require a 3x repeat
    (G = green reference, smallest). Robust across bodies without libraw's per-model wb_section
    offset table; within ~1% of libraw on GFX, ~3% on X-T2 (libraw additionally does two-record CCT
    averaging -- visually negligible for the preview/develop path).
    Matrix by model (RAF header camera id at 0x1C -> "Fujifilm X-T50"). X-Trans is demosaiced by
    Demosaic::XTransWindow (5x5 same-colour average over the tiled 6x6 map; native channel exact --
    the X-Trans analogue of the bilinear Bayer path). VERIFIED byte-identical to libraw on the
    uncompressed X-T50 and the compressed X-Trans (X-T2) and Bayer (GFX 50S II) RAFs. LIMITATIONS:
    the lossy compressed path is ported but unexercised (no lossy test file); white level is the
    FujiIFD BitsPerSample max (e.g. 16383 at 14-bit) not libraw's tighter per-body saturation
    (e.g. GFX 15872), so highlights clip slightly late; WB is unaveraged (~3% on X-T2).

    PANASONIC RW2 (PanasonicRaw, ImageFormats/Panasonic/panasonic.cpp): RW2 is a TIFF-like file
    (magic 0x55) whose IFD0 carries Panasonic raw tags -- SensorWidth/Height (0x02/0x03),
    RawFormat version (0x2D), RawDataOffset (0x118), CFA (0x09: 1=RGGB 2=GRBG 3=GBRG 4=BGGR),
    per-channel WB (0x24/0x25/0x26 = R/G/B, green==256) and black (0x1C..0x1E, stored 15 below
    true). Decodes the classic RawFormat-4 compression (dcraw panasonic_load_raw): a 14-column
    group code with a per-pair nibble predictor, bits pulled from Panasonic's reversed 16 KB
    "pana_bits" buffer (refilled rotated by loadFlags = 0x2008; byte index vbits>>3 ^ 0x3FF0).
    Ported from dcraw, validated BYTE-IDENTICAL to libraw on a DC-GX9 RW2; renders to a coherent,
    correctly-coloured image. Matrix from the per-model table (DC-GX9). LIMITATION: RawFormat
    5/6/7 (newer cameras) use different loadFlags / a different codec -> returns false (fallback).

    OLYMPUS ORF (OlympusRaw, ImageFormats/Olympus/olympus.cpp): the ORF TIFF LIES (tags the raw
    strip Compression 1 / 16-bit) but the strip is Olympus's proprietary 12-bit compression: a
    Huffman magnitude code (a direct 4096-entry 12-bit-prefix table) + an adaptive bit length per
    sample + a median predictor (w,n,nw) with a 2-column running carry; the bitstream starts 7
    bytes into the strip. Ported from dcraw's olympus_load_raw, validated byte-identical to libraw
    on an E-M1 ORF. As-shot WB is read from the Olympus MakerNote (non-standard "OLYMPUS\0II\3\0"
    header -> TiffWalk::initEmbedded with base = makernote start, IFD at +12; ImageProcessing
    0x2040 -> WB_RBLevels 0x0100, two SHORTs R,B with green == 256) -- essential, matrix-neutral
    renders badly green for Olympus. Matrix from the per-model table (E-M1). FIRST CUT: white 4095
    (12-bit), E-M1-validated black 255 + BGGR pattern (ORF has no CFAPattern tag); per-model
    black/pattern/bit-depth is a refinement (the OM-1 may differ). NOTE: TiffWalk::init now
    accepts non-42 TIFF magics (ORF 0x4F52, RW2 0x55, CR2 0x4352) since the endian marker + a sane
    first-IFD offset is enough; standard TIFF (magic 42) still validates.

    NIKON NEF (NikonRaw, ImageFormats/Nikon/nikon.cpp): self-contained. Walks the NEF IFDs (via
    TiffWalk) to the CFA SubIFD (Compression 34713 = Nikon NEF Compressed) -- some bodies (Z9)
    expose SEVERAL such SubIFDs (the full-res frame plus smaller/0x0 placeholders), so the largest
    valid-geometry candidate is chosen, not the last seen (else the 0x0 IFD fails the geometry check
    and the whole decode falls back to the embedded preview), then reads the
    linearization/Huffman metadata from the Nikon type-3 MakerNote (tag 0x96). The MakerNote is
    an EMBEDDED TIFF ("Nikon\0" + 2 version bytes + "\0\0" + its own header at base = mnOffset+10,
    offsets relative to base) -- TiffWalk::init(file, base) handles that. Nikon's compression is
    NOT lossless JPEG: each sample is a Huffman-coded difference (one of dcraw's 6 trees, chosen
    by version byte + bit depth) against DUAL predictors -- a vertical predictor for the first two
    columns (per row parity), then a running horizontal predictor per column parity -- mapped
    through a linearization curve (identity for the 0x46 lossless mode; interpolated with a tree
    "split" row for the 0x44 lossy mode). Ported from dcraw's nikon_load_raw. Colour matrix from
    the shared per-model table (added D810/D7200/D850/D2H/D100/D800E/Z9; an unknown model falls back
    to an identity matrix, which renders with a colour cast -- seen on the Z9 before its row was
    added); white = (1<<bps)-1; black from MakerNote
    tag 0x3d (4-SHORT per-channel pedestal: D850 400, D810 600, Z9 1008 -- matches libraw), falling
    back to the decoded minimum only on older bodies that omit 0x3d (D2H/D100/D800E, ~0 pedestal).
    The minimum alone is fragile: one sub-pedestal hot/noisy pixel drags it below the true black, so
    the shared SubtractBlack under-subtracts and the frame renders washed-out (was seen on the D850,
    darkest pixel 78 vs true 400). CFA phase read from the raw SubIFD's
    CFAPattern tag (0x828E) via cfaPatternFromPlaneColor() -- most bodies are RGGB but older
    sensors differ (D100 GRBG, D2H GBRG), and decoding those as RGGB rendered greens as magenta;
    falls back to RGGB when the tag is absent. ACTIVE-AREA CROP: most NEFs store exactly the active
    area, but a few old bodies pad it with masked optical-black columns that fringe the left/right
    edges magenta when demosaiced; a per-model crop (libraw margins -- D2H: left 6, width 2482)
    trims them. Margins are even so the CFA phase is unchanged; add a row per affected model.
    VERIFIED: the C++ decode is
    BYTE-IDENTICAL to libraw on 14-bit (D810) and 12-bit (D7200) lossless NEF -- validated with a
    throwaway rawpy/libraw reference in scratch, since the bit-level scheme is too subtle to
    guess. As-shot WB: read from the UNENCRYPTED MakerNote tag 0x0C (WhiteBalanceRBLevels, 4
    RATIONALs in order R, B, G1, G2 -- green-normalised multipliers) -> raw.camMul, matching
    libraw's cam_mul exactly on D810/D7200/D100. (Some bodies, e.g. D100, store only R/B and leave
    the green entries 0, meaning green=1; substitute 1.0 rather than rejecting the tag, else a zero
    green multiplier drops the WB and leaves a heavy green cast.) Old bodies that lack 0x0C (e.g. D2H) instead carry
    WB in the ColorBalance block 0x97; its early pre-encryption versions ("0100".."0103") store the
    same RGGB levels as 16-bit ints at a fixed, version-dependent offset (layout ported from dcraw's
    parse_makernote) -- reproduces libraw's cam_mul on D2H (611,256,397,256). Without it the matrix-
    derived neutral WB was used, giving a warm cast. (The version-0223+ cipher is still unbroken but
    those bodies all expose 0x0C, so it is unneeded.) LIMITATION: the 0x44 lossy-with-curve path is
    ported but not yet exercised by a test file.

    CANON CR2 (CanonRaw, ImageFormats/Canon/canon.cpp): self-contained. Walks the CR2 IFD chain
    (via TiffWalk), finds the raw IFD (the one with Canon slice tag 0xC640), decodes its
    lossless-JPEG strip (Compression 6), then reassembles Canon's vertical SLICES: tag 0xC640 =
    [nFullSlices, sliceWidth, lastSliceWidth]; the component-interleaved JPEG raster maps back to
    sensor (row,col) by dcraw's formula. It then reads the Canon makernote (IFD0 -> ExifIFD
    0x8769 -> MakerNote 0x927C) for SensorInfo (0xE0 -> active-area crop L/T/R/B) and ColorData
    (0x4001 -> WB_RGGBLevelsAsShot; offset 63 for modern ColorData, 25/24/142 for older lengths).
    Black is self-calibrated from the masked optical-black columns (x < left) before cropping;
    white = (1<<precision)-1; colour matrix from the shared per-model table (added 7D Mark II);
    pattern RGGB; crop origin forced even to keep the Bayer phase. VERIFIED: C++ un-sliced CFA is
    byte-identical to the validated reference on a 7D Mark II CR2 (which renders to a coherent,
    correctly-coloured image), and the makernote crop/WB/model read back correctly. LIMITATIONS:
    lossless-JPEG CR2 only (sRAW/mRAW and very old CR2 layouts not handled); Canon black is a
    masked-column average, not per-channel; ColorData WB offset table covers the common versions.

    CANON CR3 / CRX (CanonCR3Raw, ImageFormats/Canon/crx.cpp): self-contained clean-room decode
    of Canon's CRX lossless codec (registered for "cr3"; CanonCR3 handles CR3 metadata, this is
    the sensor path). Walks the ISO-BMFF box tree for the full-resolution CRAW track, reads its
    CMP1 geometry (fW/fH, tile, nBits, planes, cfaLayout), the IAD1 active-area crop, and the CRX
    bitstream location (stbl co64 offset + stsz length). The CRX entropy stream is adaptive
    Golomb-Rice (escape 41 / 21 bits / kMax 15) + JPEG-LS-style MED prediction + T.87 run mode.
    KEY POINTS re-derived by byte-level instrumentation vs the libraw/rawpy oracle: (a) the tile's
    four Bayer planes share ONE CONTINUOUS bitstream -- do NOT re-seek per plane to the byte
    offsets (only R5 and R6II even planes land byte-aligned; odd planes start mid-byte); the reader
    runs continuously, only k and the run s_param reset at each plane's first line. (b) The
    non-top line adapts k from a LOOK-AHEAD-adjusted code bit_code=(bit_code+|2*(d-b)|)>>1 (the
    sample itself is decoded with adaptation OFF); the top line uses ordinary per-symbol adaptation.
    (c) run entry when coeff a==b==d; predictor med(a,b,c); s_param persists plane-wide. The four
    planes interleave ROW-MAJOR into the RGGB mosaic (plane 0/1/2/3 -> quad (0,0)(0,1)(1,0)(1,1)),
    then crop to IAD1. Black self-calibrated PER-CHANNEL from the masked columns; white=(1<<nBits)-1
    refined by the per-model table; WB from the CMT3 makernote ColorData (0x4001, same offset table
    as CR2); matrix from the per-model table; pattern RGGB. VERIFIED: full-sensor mosaic is
    byte-identical (0 diffs) to the rawpy oracle on EOS R5 (46.65M px) and R6 II (25.49M px), all
    four planes. LIMITATIONS: level-0 (no wavelet) single-tile CRAW only -- current full-frame
    bodies; leveled/multi-tile or non-4-plane CRAW returns false (caller falls back to the embedded
    preview). CR3 HDR-PQ preview handling is unchanged (see the loadFromImageIO section above).

    TiffWalk (ImageFormats/Raw/tiffwalk.*): a tiny shared TIFF/EP IFD reader (header, IFDs +
    SubIFDs, tag value accessors: scalar / u32 array / real array / bytes / ascii, handling the
    inline-vs-offset rule and endianness). DngRaw uses it; other vendor unpackers can adopt it
    to stop re-implementing the IFD-walk boilerplate (SonyRaw still has its own inline walk).

    SHARED LOSSLESS-JPEG (ImageFormats/Raw/losslessjpeg.*): the compression behind Canon CR2,
    most compressed Adobe DNG, and lossless NEF/ARW. NOT DCT JPEG -- the lossless mode (SOF3):
    each sample is Huffman-coded as the difference from a spatial predictor of its neighbours.
    LosslessJpeg::Decode(bytes) -> Image{width,height,components,precision,samples} with samples
    interleaved by component (samples[(y*width+x)*comps + c]). It is camera-agnostic; each vendor
    decoder extracts the bytestream from its container and reassembles components/tiles/slices
    into the CFA (that mapping is format-specific, lives in the caller). SCOPE: single scan,
    Huffman, predictors 1-7, point transform; NO restart intervals yet (returns false on DRI>0).
    VERIFIED: the C++ output is byte-identical to a reference decoder that was validated
    end-to-end on a real lossless-JPEG DNG (Leica M10: 14-bit, 2 components, predictor 1) by
    demosaicing/colour-converting it to a coherent, correctly-coloured image.

    Phasing: 1 scaffold (done) -> 2 first uncompressed format UnpackCfa: Sony ARW (done)
    -> 3 real colour: per-model matrix + matrix-derived WB + Sony as-shot WB/black (done); TODO
    more model matrices -> 4 better demosaic (AHD) -> 5 compression: shared lossless-JPEG decoder
    (done); wire into DNG/CR2 UnpackCfa next; compressed NEF/ARW later.

    NOTE: an earlier (2025) unused scaffold (ImageFormats/Base/rawbase.* +
    ImageFormats/Tiff/tiffdecoder.*) used the rejected demosaic()-as-override axis and has
    been removed.

    NOTE: RawColor now stops at LINEAR working-space RGB (no tone/gamma/ICC baked in); the
    display conversion is owned by the shared OutputTransform stage (see Develop section).


==========================================================================================
OPERATION MODE (Preview vs Develop)
==========================================================================================

    Winnow's top-level mode is G::OperationMode (global.h): Preview | Develop (more may follow).
      Preview  fast image review -- embedded preview JPGs + a large forward read-ahead cache.
      Develop  view/edit a SINGLE image at BEST quality (scene-linear raw decode). A large forward
               cache is a LOW priority here, so Develop suspends read-ahead and, on a WorkingImage-
               cache miss for a raw, RE-DECODES to the scene-linear image rather than falling back
               to the tone-mapped display image (that fallback is only correct for non-raw files).
    UI: a standout (Apple-blue) dropdown at the EXTREME LEFT of the status bar (createStatusBar,
    width pinned via stylesheet min/max-width to beat the global "QComboBox { min-width:6em }"), and
    the D shortcut (operationModeAction) -- both call MW::setOperationMode / toggleOperationMode.
    (D previously enabled/disabled the Develop panel via developAction; that action is now
    shortcut-less in the Utilities menu.)

    The mode also OWNS whether the Develop panel is usable: it is enabled only when the user's
    Develop toggle (developAction) is on AND the mode is Develop -- Preview always greys it out.
    MW::syncDevelopPanelEnabled() computes that AND and is called on both inputs (developAction
    toggled, setOperationMode). In Preview the panel is not kept in sync with the selected image
    either: fileSelectionChange skips developProperties->setCurrentImage (no edits are possible, so
    none are pending to flush), and setOperationMode re-points the panel at the current image when
    entering Develop / flushes edits when leaving it.

    The mode OWNS the raw/preview decode (G::useRaw): setOperationMode calls toggleUseRaw(on) for
    Develop and toggleUseRaw(off) for Preview -- so Preview shows embedded previews and Develop
    decodes raw sensor data. Within Develop the "Edit: Raw / Embedded Preview" selector can still
    OVERRIDE to embedded preview (it drives the same toggleUseRaw); the override lasts until the next
    mode switch, which re-applies the mode default. The standalone status-bar useRawBtn is REMOVED
    (commented out; button still created/wired in case it returns).

    Behaviour implemented: (1) setTargetRange targets ONLY the current image in Develop mode (no
    forward read-ahead -> the current image's scene-linear WorkingImage is not evicted); (2)
    renderDevelopPreview re-decodes the raw on a WorkingImage miss OR when the cached one is
    display-referred (a prior develop-miss fallback), via ImageDecoder::decodeIndependent (which
    caches the scene-linear WorkingImage). Non-raw files still build from the display image.

    OPEN QUESTION (decide later): should THUMBNAIL generation ALWAYS use the embedded preview
    (useRaw=false) for raw files, even in Develop mode? Rationale: in Develop you edit ONE image;
    the thumb strip is for orientation and does not need the slow full-raw decode. Currently
    G::useRaw is global, so Develop=true also makes the strip decode raws (bounded by the Develop
    read-ahead suspension, but still). A dedicated "thumbnails always preview" path would decouple
    the loupe (raw) from the strip (preview).

DEVELOP / IMAGE EDIT (PARAMETRIC ADJUSTMENTS)
==========================================================================================

------------------------------------------------------------------------------------------
Architecture
------------------------------------------------------------------------------------------

    Lightroom-style non-destructive adjustments (denoise, white balance, exposure, contrast,
    highlights, shadows, whites, blacks, texture, dehaze ...). They apply to ALL image types
    (JPEG/TIFF/HEIC/RAW), so they are a shared stage AFTER decode and BEFORE the display
    tone/gamma/ICC transform -- NOT part of RawFormat.

    Decisions (settled): adjustments run on a FLOAT LINEAR working buffer (exposure/tone/
    dehaze need headroom), and apply to ALL image types (not raw-only).

    Unifying representation -- WorkingImage (float linear scene-referred RGB + working colour
    space + white reference). Both decode paths converge on it:
        RAW:      ... -> Demosaic -> camera-RGB -> working space   (RawColor stops at LINEAR)
        NON-RAW:  QImage decode -> un-gamma to linear -> working space
    Consumer:
        WorkingImage -> Develop(EditParams) -> OutputTransform(tone+gamma+ICC) -> QImage

    EditParams -- one versioned struct, identity defaults (absent = no-op = no behaviour
    change). The single source of truth for all adjustment values; every stage reads from it.
    Persisted per-image (datamodel column + sidecar; deferred). The slider UI binds to it.

    Develop -- stateless/reentrant processor, constructed per decode (same discipline as
    Demosaic/RawFormat). FIXED, hard-coded operation order:
        1. Denoise (luma/chroma)        earliest; for RAW ideally on the CFA mosaic
        2. White balance                linear channel scale (RAW: camMul, from temp/tint)
        3. Exposure                     linear gain 2^EV
        4. Contrast                     S-curve about mid-grey
        5. Highlights/Shadows/          luminance-masked parametric tone
           Whites/Blacks
        5b. Colour RGB                  per-channel linear gain (red/green/blue), folded into #2
        5c. Colour HSL                  hue rotation + saturation + vibrance + luminance,
                                        after the tone curve
        6. Texture                      mid-frequency local contrast
        7. Dehaze                       local contrast + black-point, haze estimate
        8. Vignette                     radial corner exposure falloff (exposure/feather)
        9. Grain                        monochromatic film grain (amount/size/roughness)
        (room left for Clarity/Sharpen)

    Two ops are special for RAW: white balance and denoise give better results applied inside
    the raw pipeline (WB at the camera-RGB stage; denoise on the CFA mosaic) rather than
    generically post-demosaic. EditParams is shared, so RawFormat::Decode() takes an optional
    const EditParams* and consumes those two fields at the right stages; Develop handles the
    rest. (Scaffold may start with everything in Develop and push those two down later -- a
    quality-only refactor, no interface change.)

    WIRING (done): the decode path now runs through these classes.
        NON-RAW: ImageDecoder::load() decodes to QImage as before; then ImageDecoder::
                 applyDevelop() (after rotate(), before colorManage()) does
                 InputTransform -> Develop -> OutputTransform. It is a NO-OP and is skipped
                 entirely when EditParams is identity, so the unedited browsing path is
                 unchanged and never round-trips through the float buffer. When it does run it
                 goes through the WorkingImage cache (reuses / populates the pre-develop image).
        RAW:     RawFormat::Decode() develops in its native linear float (RawColor::ToWorking
                 -> Develop -> OutputTransform), hands its pre-develop WorkingImage back via
                 outWork (cached by ImageDecoder), and ImageDecoder sets developApplied so
                 applyDevelop() does not run a second time. See PERF / WORKINGIMAGE CACHE below.

    ABORT: RawFormat::Decode() takes the decoder's abort atomic and polls it between stages and
        once per row inside the demosaic loop, returning false (lastError "Aborted") if set.
        This matters because ImageCache::stop() (quit) uses a BlockingQueuedConnection to each
        decoder; without polling, an in-flight 24MP demosaic would run to completion while the
        main thread blocks waiting -> beachball. ImageCache::stop() now also calls
        decoders[id]->abortProcessing() (sets the atomic, non-blocking) BEFORE that blocking
        wait so the in-flight decode actually sees the flag and bails.

    G::useRaw TOGGLE: ImageDecoder::load() only attempts the RAW path when G::useRaw is true;
        when false it skips RawFormat entirely and shows the embedded preview/JPG (prior
        behaviour). A status bar button (MW::useRawBtn, between reverseSortBtn and
        panToFocusToggleBtn) toggles it -- colored raw.png icon when on, grayscale raw_bw.png
        when off. MW::toggleUseRaw rebuilds the full-size image cache so the change is visible
        immediately. The value persists across sessions via QSettings ("useRaw", default
        false). NOTE: the raw decode is still also gated by RawFormat::Create() returning a
        decoder for the extension (arw/dng/cr2/nef/orf/rw2/raf today; others -> embedded JPG).
        Saved Develop edits are also gated on useRaw: MW::applyDevelopPreviewIfEdited() overlays
        a non-identity EditStack onto the loupe only when useRaw is on, since the edits are
        calibrated against the demosaiced RAW render. In preview mode (useRaw off) the embedded
        JPG is shown untouched; toggling useRaw on rebuilds the cache and the developed look returns.
    InputTransform (Develop/) is the non-raw "un-gamma to linear" step, the symmetric
    counterpart of OutputTransform.

    BASELINE TONE CURVE: a scene-referred raw render is radiometrically linear and lands dark
    and flat next to a camera JPEG (measured ~+0.7 EV / much less contrast on the A9 II). So
    WorkingImage carries a sceneReferred flag: RawColor sets it true (sensor data, with
    highlight headroom -- RawColor now clamps only negatives, not >1), InputTransform sets it
    false (a JPEG already has the camera tone curve baked in). OutputTransform applies a fixed
    baseline tone curve ONLY when sceneReferred: exposure x kBaselineExposure (=1.6, ~+0.68 EV)
    then the ACES (Narkowicz) filmic shoulder, before the sRGB transfer. This lifts midtones and
    rolls highlights off smoothly (vs hard-clipping), landing close to the embedded preview
    (mean luma 0.434 vs 0.445; validated in numpy against the preview). It is a fixed default
    look, not per-image auto-exposure; a future Develop "exposure" can override it. Non-raw
    output is unchanged (tone curve skipped), so the unedited-JPEG round trip stays identity.

    PERF (keeps Winnow fast): WORKINGIMAGE CACHE (Develop/workingimagecache.h, DONE). The
    expensive part of a RAW is UnpackCfa -> Demosaic -> RawColor, which produces the pre-develop
    WorkingImage; Develop + OutputTransform that follow are cheap. So that WorkingImage is cached
    (keyed by absolute file path) and an interactive slider change re-renders via only
    Develop + OutputTransform -- never decode or demosaic again.

        Storage: WorkingImageCache::instance().put(fPath, work). RAW caches the pre-develop
            WorkingImage that RawFormat::Decode() now hands back through its outWork out-param
            (a shared_ptr -- no pixel copy; the snapshot is always pre-develop, so a non-identity
            edit develops on a private copy and leaves the cached image pristine). Non-raw caches
            the post-InputTransform WorkingImage the first time applyDevelop() builds one.
        Re-render: WorkingImageCache::get(fPath) + WorkingImageCache::render(work, edit, out).
            render() is static/stateless: identity edit -> OutputTransform only (no copy);
            non-identity -> copy + Develop + OutputTransform. ImageDecoder::applyDevelop() now
            uses this fast path (cache hit skips InputTransform; miss builds + caches once).
            The editor (slider UI, deferred) calls get()+render() directly to re-render without
            a decode; a cache miss falls back to a full decode (which re-populates the entry).
        Memory: entries are shared_ptr<const WorkingImage> (large -- ~4x the 8-bit QImage), so
            the cache is byte-budgeted (default 768 MB, setMaxBytes()) with LRU eviction; the
            most-recently-used entry is never evicted (the active edit always hits). It is a
            small edit accelerator for recently-viewed images, NOT a second image cache.
            ImageCache::initialize() clear()s it on every folder change.
        Done (2026-06): downscaled proxy for live drag + full-res settle render run async off the
            GUI thread (see "Load Responsiveness" / renderDevelopFullResAsync).
        Still TODO: explicit pin of the active image so heavy ahead-caching can't evict it before
            the user edits.

    Phasing: 1 WorkingImage + split RawColor at linear + InputTransform/OutputTransform +
    Develop skeleton (identity), wired no-op through decode (DONE) + WorkingImage cache hook
    (DONE) -> 2 implement ops (exposure/WB/contrast/tone first; texture/dehaze/denoise later)
    -> 3 push RAW white balance/denoise pre-demosaic for quality -> 4 persistence + slider UI.
    NOTE: the single-EditParams render above is the SCAFFOLD. The real editor generalises it
    to a LAYER + MASK stack (next subsection); EditParams stays the per-layer unit, render()
    grows to take an EditStack, and the Develop op math is unchanged.

------------------------------------------------------------------------------------------
Panel focus & slider arrow-nudge
------------------------------------------------------------------------------------------

    The Develop dock is a PropertyEditor (QTreeView) whose value cells are persistent editor
    widgets (SliderEditor -> Slider). Global image navigation (Left/Right = prev/next image)
    is handled in MW::eventFilter. The disambiguation between "navigate images" and "nudge
    this slider" is Qt keyboard focus:

        - Slider has StrongFocus and takes focus on click (Slider::mousePressEvent). SliderEditor
          sets setFocusProxy(slider), so a click or Tab into the row lands focus on the slider.
        - Slider::keyPressEvent nudges on Left/Right (Up/Down too; PageUp/PageDown = the larger
          step) and ACCEPTS the key. Accepting matters: MW's arrow handler is an APPLICATION event
          filter (qApp->installEventFilter), and application filters run once per object as an
          UNconsumed key propagates up the parent chain -- so an arrow the slider did not accept
          reaches MW::eventFilter with obj == "MWWindow" and moves the selection. Consuming it in
          the slider stops that propagation. (Relying on QAbstractSlider's own stepping was not
          enough in practice, hence the explicit override.)
        - Belt-and-suspenders: MW::eventFilter's Left/Right nav is additionally guarded to no-op
          when QApplication::focusWidget() is a value editor (QAbstractSlider / QAbstractSpinBox /
          QLineEdit / QComboBox). When focus leaves the panel, arrows navigate images again.

    Focus cue (which slider owns the arrows): SliderEditor::paintEvent draws an accent left bar
    plus a subtle translucent fill when slider->hasFocus(). Slider::focusInEvent/focusOutEvent
    call parentWidget()->update() so the cue tracks focus. This behaviour is generic to every
    PropertyEditor slider (Develop, Embellish, Preferences), not just Develop.

    Not yet done: a panel-LEVEL active-border cue across all docks (thumb/grid/table/folders/
    Develop). DockWidget notes it has not solved drawing a border around a QDockWidget; the
    control-level cue above already shows where slider shortcuts land.

    Develop diagnostics report (Help > Diagnostics > Develop diagnostics)
        MW::developDiagnostics() dumps everything the Develop pipeline depends on for
        the currently selected image, in one read-only snapshot: the operation-mode /
        raw-engine globals (G::operationMode, G::useRaw, G::decodeRawEngine); the current
        file (raw?, video?, ISO, camera model); the cached scene-linear WorkingImage base
        (dims, sceneReferred, white, MB) -- the first thing to check, since a
        sceneReferred=false base, or dims that differ from the sensor active area,
        means Develop is editing the display-referred embedded JPG, not the sensor
        image; the rawInfo (active area, bits, compression, CFA, black/white, matrix);
        the effective EditParams for the Base layer and each enabled non-Base layer
        (+ mask counts); the crop/warp Geometry; and the interactive develop caches
        (PMRID "Denoise raw" base + keys, developWorkTriedPath / in-flight guards,
        proxy + full-res render state, mask reference caches). The DENOISE section also
        reports the PMRID (k,b) noise-model resolution FOR THE CURRENT IMAGE -- the tier
        that fired (DNG NoiseProfile / model table / blind / generic), the resolved k,b,
        and whether the file carried a DNG NoiseProfile -- the first thing to check if a
        file denoises wrong (e.g. source=generic on a Nikon means its modelMatch missed).
        The snapshot (developPmridResSource/K/B/HadNP) is captured in ensureRawDenoise
        on a PMRID decode and keyed to developPmridKey, so it is NEVER stale: it shows
        only while developPmridFull is this image's base, else "(no current PMRID base)"
        while the base is (re)building. PMRID::LastResolution() is the worker-side
        hand-off the snapshot is captured from. Wired in menusand-
        actions.cpp (diagnosticsDevelopAction) via MW::diagnosticsDevelop.
        RENDER VERIFICATION has TWO axes, so the two failure modes seen (edit silently
        dropped; demosaic/decode not triggered) are each caught:
          [A] Develop DISPLAY vs the Preview (embedded) image. The Preview image is
              captured (downscaled) on entering Develop (MW::setOperationMode, before
              the re-decode); each develop display -- edited OR not -- is grid-sampled
              against it in MW::updateDevelopScopes (cheap, no scaling of the 50MP
              shown). maxAbs>0 confirms the decode change (demosaic) and/or edits took
              effect; maxAbs==0 on a raw in Edit=Raw means the demosaic did NOT apply
              (display still the embedded preview). This axis populates for an unedited
              image on mode entry.
          [B] recipe vs the un-developed base. On each full-res settle the worker
              renders the CLEAN base with identity params vs the actual base (incl. raw
              denoise) with the recipe at ~256px (geometry suppressed so sizes match).
              maxAbs==0 with a non-identity recipe = a dropped edit (the "denoise does
              nothing" class). Populates only on edited settle renders.
        Both are sub-ms and stored on MW (developVerify* / developVerifyVsPreview*).

    Entering Develop must re-decode the current image (MW::setOperationMode)
        ImageDecoder::load() picks the decode by (G::operationMode==Develop &&
        G::useRaw): Preview shows the embedded preview, Develop demosaics the raw.
        Switching mode therefore changes the decode even when useRaw does not flip.
        setOperationMode drives the re-decode via toggleUseRaw WHEN useRaw changes; but
        if useRaw already matches the target (e.g. it was left true from a prior
        session / settings), toggleUseRaw is skipped and NOTHING re-decoded -- the
        loupe kept showing the Preview embedded image until the user toggled the
        Edit-source selector. FIX: the useRaw-unchanged branch now also sets
        imageView->currentImageHasChanged and emits imageCacheColorManageChange()
        (-> reloadImageCache -> refreshViewsOnCacheChange -> loupe), matching the
        toggleUseRaw refresh, so entering Develop always renders through the mode's decode
        path.

------------------------------------------------------------------------------------------
Layer & masking model (planned)
------------------------------------------------------------------------------------------

    GOAL. Apply any edit either globally or through one or more masks, with the SAME edit set
    shared across a layer's masks, performance critical. The user-facing concept is a LAYER;
    it maps to exactly one internal object. This is the Lightroom MASKING model (a mask group
    carrying one edit set), NOT a Photoshop pixel-layer stack -- that distinction is what keeps
    it fast (see PERFORMANCE).

    A LAYER = one EditParams (the existing struct, unchanged) + a COLLECTION of mask components
    (radial + brush + linear ...) combined into a single per-pixel alpha + an opacity. The edit
    set applies wherever that combined alpha > 0, so "the same edits across more than one mask"
    falls out for free (the masks are unioned). A layer with NO mask components is GLOBAL
    (applies everywhere) -- so "globally OR on masks" needs no special case.

    NOISE REDUCTION is split across the two stages (the decode pipeline and Develop):
        • "DENOISE RAW" (Colour + luminance NR) is GLOBAL-ONLY, driven by EditParams
          denoiseLuma/denoiseChroma (two Base-layer 0..1 sliders,
          "Denoise Lum"/"Denoise Color").
          UI: the two sliders are root-level SIBLINGS of the Base "Demosaic" row (not
          children), always shown with no collapse arrow; they appear only on the WINNOW
          engine (Apple omits them -- see DevelopProperties::addCoreItems). UR_ExtraIndent
          lines their captions up one tree level so they align with the Basic sliders.
          The Demosaic row is a plain leaf (no children, no UR_isDecoration), so it draws
          in the ordinary caption colour like "Edit:", not the teal header colour; the
          "Edit:" source row carries a small UR_ExtraRowHeight (8px) for breathing room.
          "Denoise Lum" defaults to 75 and "Denoise Color" to 100 -- a fresh raw carrying
          only those defaults still counts as identity (EditParams::kDefaultDenoiseLuma /
          kDefaultDenoiseChroma, mirrored in isIdentity). A saved recipe overrides them
          per image (paramsFromJson keeps the default only for keys absent from the JSON).
          The denoise rows share the Demosaic row's per-raw visibility (hidden when not
          editing raw or when the layer is collapsed -- applyCoreVisibility).
          RUN MODE: a row sits between the Demosaic row and "Denoise Lum"
          (DevelopProperties::addCoreItems; also hidden by applyCoreVisibility) with two
          checkboxes hosted in the two columns so they line up with the Demosaic row:
          "Denoise" in the CAPTION cell (aligned with the "Demosaic" caption) and "Auto
          run" in the VALUE cell (aligned with the Demosaic dropdown).
            - "Auto run" persists to QSettings Develop/autoRunDenoise (mirrored in the
              runtime flag MW::developAutoRunDenoise). When ON (default) the heavy PMRID
              decode runs automatically -- the three ensureRawDenoise auto call sites
              (render settle in renderDevelopFullResAsync, image-select, and entering
              Develop) gate on developAutoRunDenoise. When OFF they are skipped so the
              render uses the CLEAN base. Turning it ON also runs the denoise immediately
              (MW::onAutoRunDenoiseToggled -> runRawDenoiseNow).
            - "Denoise" is the manual trigger + completion state. Checking it emits
              runRawDenoiseRequested -> MW::runRawDenoiseNow (forces one ensureRawDenoise
              regardless of the flag); unchecking emits clearRawDenoiseRequested ->
              MW::clearRawDenoiseNow (drops BOTH developDenoised and the full-strength
              developPmridFull base, re-renders clean). Its label is "Denoise" until the
              PMRID base is ready, then "Denoised".
          MW::rawDenoiseReadyForCurrent tests developPmridFull (the full-strength base,
          keyed path+iso and so AMOUNT-INDEPENDENT). It drives both the checkbox label
          and the amount-slider enabled state (DevelopProperties::updateDenoiseRunState,
          called on ensureRawDenoise completion and clear; seeded on buildTree). Because
          the base is amount-independent, once it exists a "Denoise Lum"/"Denoise Color"
          change is only a cheap re-blend: the render gate re-runs ensureRawDenoise when
          developAutoRunDenoise OR rawDenoiseReadyForCurrent (NOT auto-only), so a
          manual-mode slider change stays denoised instead of reverting to clean. The
          amount sliders are DISABLED (greyed caption + value) until the base exists (they
          only scale its blend), enabling when the denoise completes. applyItemsEnabled
          re-asserts that disable when the panel is (re-)enabled, so a panel-wide enable
          can't un-grey them.
          Signals (autoRunDenoiseToggled / runRawDenoiseRequested /
          clearRawDenoiseRequested) are wired in initialize.cpp.
          DEMOSAIC PROGRESS: with Auto run OFF the "Denoise raw" decode (which shows the
          "Raw Denoise" row) does not run on open, so the plain Winnow sensor demosaic
          that produces the image would otherwise show no feedback. It now shows a blue
          "Demosaic" row (progressDemosaicRow) for the CURRENT image. That demosaic runs
          in the ImageCache decoder threads (ImageDecoder::load, the Develop+useRaw
          raw-sensor branch -> RawFormat::Decode), so progress is routed as a SIGNAL, not
          a direct call: RawFormat::Decode drives its progress sink from Demosaic::Run's
          per-tile callback (a clean, non-denoise decode with a sink now drives the whole
          bar); ImageDecoder::load emits demosaicProgress(sfRow, fPath, done, total);
          ImageCache relays it as demosaicProgress(fPath, done, total);
          MW::onDemosaicProgress updates the row, gated to the current file + Develop +
          useRaw + Winnow + Auto run off, and clears it on setCached for the current row.
          (The rarely-used scene-linear fallback MW::ensureDevelopWork ->
          ImageDecoder::decodeIndependent instead passes its own decodeProgress callback,
          updating the same row directly.)
          IMPLEMENTED as PMRID (MegVii, Apache-2.0), a PRE-demosaic (CFA/Bayer-domain)
          learned denoiser: it runs on the raw mosaic BEFORE demosaic (ImageFormats/
          Raw/pmrid.{h,cpp}, PMRID::Apply) through the unified inference layer
          (Utilities/inference/, ORT -- CoreML/ANE on macOS, DirectML on Windows). Needs a
          WINNOW_ENABLE_ORT build (default OFF -> no-op stub) plus pmrid.onnx
          (ReleaseExtras/, committed, warn-if-absent), else it no-ops. Global / not
          maskable (a Base op before Develop). Because it needs the
          CFA mosaic it is WINNOW-engine + BAYER only: inert on the macOS Apple decode
          engine (no CFA) and on X-Trans, where "Denoise raw" no-ops. Conditioned by ISO
          (ImageMetadata::ISONum); applied as an amount-scaled BLEND (see WHY TWO
          DEMOSAICS below), luma by denoiseLuma, chroma by max(luma,chroma). (History: an
          earlier post-demosaic RawRefinery "TreeNet" path via the ORT inference layer,
          plus an unwired NAFNet export, were removed 2026-07-12 when PMRID superseded
          them; recover from git if a post-demosaic engine is ever wanted -- e.g. to cover
          X-Trans/Apple.) LIVE PREVIEW: the cached pre-develop
          WorkingImage is kept CLEAN (RawFormat::Decode caches it BEFORE denoise; denoise runs only on
          the private develop copy, i.e. the non-interactive export path). The interactive develop
          render derives a raw-DENOISED base from the clean cache
          (MW::developRawDenoisedBase / ensureRawDenoise), cached by path+amounts+ISO and
          computed OFF the GUI thread (developRenderPool) -- so a denoise-amount change
          re-runs the model ONCE and later tone/exposure edits reuse the denoised base;
          both the proxy and the full-res render start from it. ISO for the conditioning
          comes from ImageMetadata::ISONum (populated in cache mode from G::ISOColumn;
          live preview reads it via MW::currentImageIso).
          NOISE MODEL (k,b): the KSigma normalization needs a per-frame Poisson-Gaussian
          model (var = k*x + b, white-normalised green plane), resolved by a priority
          chain (PMRID::resolveKB): (1) a DNG NoiseProfile (tag 51041) baked into the
          file, read by DngRaw::UnpackCfa -- exact, no calibration; (2) a per-camera-model
          K(ISO)/B(ISO) table (pmrid_calib_tables.h) -- MEASURED by a photon-transfer
          sweep (calibrate_pmrid.py) or DERIVED from published sensor data
          (gen_pmrid_tables.py, photonstophotos/Bill Claff) -- ISO-interpolated; (3) blind
          estimation from the CFA (var-vs-mean over flat blocks, PMRID::estimateKB) so an
          uncalibrated body still denoises with its own model; (4) a generic full-frame
          Sony table as a last-ditch guard. The chosen tier is logged by PMRID::Apply.
          Only DNG carries a NoiseProfile, so proprietary raws (ARW/NEF/CR2/CR3...) use
          tier 2 if known, else tier 3.
          ONE DECODE, TWO BASES + PREVIEW-FIRST: ensureRawDenoise is triggered on image
          SELECT (MW::fileSelectionChange) AND on entering Develop (MW::setOperationMode),
          not only on settle, so its progress row (progressRawDenoiseRow) appears
          immediately in both the "select while already in Develop" and the "in Preview
          then switch to Develop" flows. Its single decode returns BOTH the PMRID base and
          the CLEAN base (RawFormat::Decode outClean = the same mosaic demosaiced before
          PMRID = one UnpackCfa, not two) and publishes the clean base to the cache.
          The ImageCache decode then REUSES that clean base (ImageDecoder::load
          short-circuits UnpackCfa+Demosaic when a scene-linear entry is cached), removing
          the redundant second decode. Meanwhile the loupe shows the embedded JPG
          immediately (ImageView::loadImageInterim on a develop-raw cache miss) and the
          developed image pops in when the decode lands.
          STAGED PROGRESS: the denoise progress bar spans the WHOLE decode, not just the
          PMRID stage: RawFormat::Decode maps the pre-PMRID (clean) demosaic to [0,25%],
          PMRID to [25,75%], and the post-PMRID (denoised) demosaic to [75,100%] of one
          0..1000 bar (Demosaic::Run gained a per-row progress callback). So the bar
          moves as the clean demosaic runs and continues through PMRID -- no empty gap.
          Inert for a normal (non-denoise) decode, which passes no progress sink.
          ZOOM/PAN KEPT ACROSS THE POP: loadImageInterim captures the outgoing zoom/pan
          (developCapture*); the developed image restores it via applyDevelopCapturedView,
          called from both the clean-image loadImage and setDevelopPreview, instead of
          re-fitting -- so a zoomed inspection stays put through interim -> developed. The
          crop case still re-fits: setDevelopPreview keeps the view only when the aspect
          ratio is unchanged (a resolution change); an aspect change = crop -> refit.
          WHY TWO DEMOSAICS: "Denoise raw" is not all-or-nothing -- it is a BLEND scaled
          by the luma/chroma amount sliders, so the decode needs BOTH endpoints, and both
          come from demosaicing the SAME CFA mosaic, differing only in whether PMRID ran
          first:
            - clean base (outClean) = mosaic demosaiced BEFORE PMRID = the noisy,
              un-denoised RGB = the 0% endpoint.
            - PMRID base (*work)    = mosaic demosaiced AFTER PMRID = full-strength
              denoised RGB = the 100% endpoint.
          PMRID itself is a PRE-demosaic (CFA-domain) denoiser, NOT a joint
          demosaic+denoise; the demosaic is a separate bilinear pass on each.
          Develop::BlendRawDenoise then does shown = clean + amount*(pmrid - clean)
          (luma/chroma split). The progress bar (progressRawDenoiseRow) tracks ONLY the
          PMRID stage, so it sits empty through the demosaics and fills through the PMRID
          tiles. Caching both bases is what lets a denoise-slider drag re-blend instantly:
          the model re-runs only on an image or ISO change.
          BASE MUST BE SCENE-LINEAR: the denoise blend (Develop::BlendRawDenoise)
          subtracts the clean cached base from the denoised re-decode, so the two must
          share geometry + space. The clean base comes from MW::ensureDevelopWork ->
          ImageDecoder::decodeIndependent, which selects the in-house RAW decoder off
          ImageMetadata::ext. DataModel::imMetadata() populates m.type but NOT m.ext,
          so ensureDevelopWork sets m.ext from the file suffix; without it the raw
          decode is skipped, the base falls back to the display-referred embedded JPG
          (active-area size, not scene-linear), and the blend silently rejects the size
          mismatch (denoise appears dead). See the Develop diagnostics report to confirm
          the base is sceneReferred with matching dims.
        • LOCAL luminance NR is a MASKABLE Develop op: an ADDITIONAL luminance-only smoothing
          applied on top of the baseline, per layer/mask. It is a SPATIAL op (below), operating
          on the already-decoded WorkingImage -- so it needs no second decode and is independent
          of which decode engine ran. It is therefore a PORTABLE OpenCV pass (mac + Windows),
          not Core Image. Algorithm: ratio-preserving luma denoise -- Y = dot(rgb, lumaWeights);
          Yd = edge-preserving denoise(Y, strength) (bilateral / guided, NLM as a slower
          higher-quality option); rgb' = rgb * (Yd / Y) so only luminance changes and chroma
          ratios are untouched.

    DATA MODEL (Develop/, planned):
        struct MaskComponent {
            enum Type { Linear, Radial, Brush };
            enum Op   { Add, Subtract };          // union / cut-out WITHIN a layer
            // Linear/Radial: parametric geometry in normalised 0..1 image coords (a few floats)
            // Brush:         a list of stamps {x,y,radius,flow,hardness} -- NOT a bitmap
            bool inverted; bool feather; float featherAmount;
        };
        struct Layer {
            QString name;
            EditParams params;                    // unchanged per-layer unit
            float opacity = 1.0f;  bool visible = true;
            std::vector<MaskComponent> mask;      // empty => global
        };
        struct EditStack {
            EditParams base;                       // global edits (or model as layers[0])
            std::vector<Layer> layers;
            int version = 1;
            bool isIdentity() const;
        };

    COMPOSITING (sequential-on-accumulator). Each layer develops the result of the stack BELOW
    it, blended back by opacity x mask alpha:

        acc = Develop(workingImage, base.params)                 // global foundation
        for each layer i (bottom -> top):
            a   = opacity_i * maskAlpha_i(x)                     // 0 where the layer doesn't act
            acc = lerp(acc, Develop(acc, params_i), a)           // per pixel

    This makes edits sequentially ADDITIVE (each layer sees the result below it) while keeping
    opacity and masks well defined per pixel. Layer ORDER therefore matters and REORDER changes
    the result -- that is correct and is surfaced in the UI. Base layer is always opacity 1, no
    mask.

    PERFORMANCE (the reason for the masking model over a pixel-layer stack). Ops are split by
    cost and the cheap ones are FUSED:
        • POINT ops -- exposure, contrast, highlights/shadows/whites/blacks, temp, tint, the
          Colour RGB + HSL sliders, and (for now) texture + dehaze -- are pure per-pixel
          functions. ALL layers' point ops run
          in a SINGLE fused pass over the image: one read, one write, no per-layer intermediate
          buffers; N layers cost N cheap evaluations in registers per pixel (bandwidth-bound).
          This is the slider-drag hot path and is independent of layer/mask count.
          (DECISION 2026-06-25: texture + dehaze are POINT ops for now to keep the fused engine
          simple; revisit a SPATIAL local-contrast version later -- see RawDecode_ToDo #15.)
        • SPATIAL ops -- local luminance NR (and a future spatial texture/dehaze) -- need a
          neighbourhood, so they cannot fuse: one pass each, blended by the layer's mask, CACHED
          and recomputed only when their own param or mask changes (never on a point-slider
          drag). The pass output depends only on its strength param, not the mask VALUES, so it
          is computed over the mask BOUNDING BOX + filter-radius halo (not the whole frame) and
          the cached result is reused while only the blend re-reads the mask alpha.

    THREADING. The fused point pass is data-parallel WITHIN one render() (row-tiles), NOT
    spread across the ImageDecoder worker pool. The decoder pool is task-parallel ACROSS images
    (one decoder per image, to fill the browse cache); an interactive edit is a SINGLE image, so
    the pool leaves it on one core -- only splitting that one image's rows uses all cores.
    IMPLEMENTED (phase 1, Develop::applyPointOps) with QtConcurrent::run over the global
    QThreadPool -- the same idiom FocusStack already uses (fs.cpp) -- rather than
    cv::parallel_for_ (which appears nowhere in the tree). The global pool is fixed-size, so a
    background decoder that also calls render() shares it and oversubscription is bounded
    automatically (no N x N blow-up, no setNumThreads juggling). Rows split into <= maxThreads
    chunks of >= 64 rows; small images run flat. (cv::parallel_for_ remains an option if a later
    spatial op is implemented in OpenCV and wants to stay on OpenCV's own pool.)

    MASKS store GEOMETRY, never bitmaps. Parametric masks (linear/radial, ~6 floats) are
    evaluated analytically inline in the fused loop -- zero storage, resolution-independent.
    Brush masks are stored as the STROKE/STAMP LIST the user painted (not a rasterised alpha),
    rasterised to a cached alpha buffer on load and after each edit: tiny sidecar, resolution-
    independent (rasterise at proxy vs full res), and serialisable as text in the XMP sidecar. A
    layer's combined alpha (the add/subtract union of its components) is rasterised ONCE and
    cached, recomputed only when mask geometry changes -- never when sliders move.

    CACHE TIERS (most already present):
        1. pre-develop WorkingImage          -- Develop/workingimagecache.h (DONE)
        2. per-layer combined mask alpha      -- recompute on geometry change only
        3. spatial-op outputs (local luma NR ...) -- recompute on their param/mask change only
        4. fused point pass                   -- always re-run on a point-slider drag (cheap)
    Plus a downscaled PROXY developed during an active drag, full-res async on release (now
    implemented: renderDevelopFullResAsync, see "Load Responsiveness") -- sub-frame with the
    fused engine.

    PERSISTENCE -- XMP SIDECAR, per image. Winnow already writes per-image .xmp sidecars
    (Metadata/xmp.cpp Xmp::writeSidecar, DOM-based); the EditStack serialises there (parametric
    geometry + brush strokes as numbers, Lightroom-style). This REPLACES the current global
    QSettings "Develop/Layers/<name>/..." persistence (which is per-machine, not per-image -- the
    scaffold's main correctness gap). EditStack is versioned for forward migration.

    BUILD PHASING (supersedes the single-EditParams phasing above):
        1. [DONE] Fused point engine, global only: white balance (temp/tint), exposure, contrast
           and the tone regions (highlights/shadows/whites/blacks) implemented for real
           (Develop::buildPointCoeffs + applyPointOps, develop.cpp); render() unchanged (still
           one EditParams) but Develop now does a single parallel per-pixel pass. Wired live:
           DevelopProperties::paramsChanged -> MW::developParamsChange() pulls the cached
           pre-develop WorkingImage (WorkingImageCache), re-renders Develop + OutputTransform,
           and pushes straight to the loupe via ImageView::setDevelopPreview() -- no decode, no
           file read. A non-raw cache miss builds the WorkingImage once from the displayed image
           (InputTransform). Orientation: a scene-referred (raw, sensor-native) render has EXIF
           orientation applied to match the loupe (mirrors ImageDecoder::rotate()). NOT yet
           persisted or written back to the image cache -- navigating away drops the preview
           (phase 4). White balance = per-channel SCENE-LINEAR gain (temp lifts red / lowers
           blue, tint lowers green toward magenta; sliders -100..100 normalised to +/-1, full
           scale = +/-50% on a channel via kTempGain/kTintGain). WB and exposure are both linear
           multiplies, so buildPointCoeffs folds them into one per-channel gain (PointCoeffs::
           channelGain) applied before the perceptual tone curve. Exposure = 2^EV scene-linear
           gain.

           TONE CURVE (contrast + highlights/shadows/whites/blacks) -- all four tone regions plus
           contrast are pure 1-D functions of a channel's white-normalised value, applied in a
           PERCEPTUAL (~gamma 2.2) domain, NOT scene-linear (a linear-space curve about 0.18
           explodes highlights, which sit several stops above the pivot; encoding to gamma first
           puts mid-grey near 0.46 so tones shape evenly). buildPointCoeffs BAKES the whole chain
           into a per-render LUT (PointCoeffs::toneLut, indexed by s=(v/white)^(1/gamma), returning
           white-normalised linear), so the hot loop does ONE encode pow + one interpolated lookup
           per channel instead of several pow/exp -- the tone pass is now cheaper than the old
           contrast-only path. Contrast = gentle slope (1 + 0.4*amount, [0.6,1.4]) about mid-grey;
           the -100..100 slider is NORMALISED to amount -1..1 (without this the first slider unit
           already hits the extreme). Each tone region adds a Gaussian-weighted lift in perceptual
           space (centres: blacks 0.0, shadows 0.25, highlights 0.75, whites 1.0; kToneSigma 0.18,
           max lift kToneShift 0.20 at full slider; + brightens / - recovers, Lightroom-like).
           The LUT spans up to ~3 stops above white (kToneLutMaxN) so highlight headroom is shaped,
           not clipped.

           COLOUR (RGB + HSL, under the Color header) -- all seven are POINT ops, so they
           ride the same fused pass at ~no extra cost. RGB (red/green/blue, -100..100) are
           per-channel scene-linear gains folded straight into channelGain alongside WB +
           exposure (kRgbGain 0.5 -> full slider = +/-50% on a channel). HSL
           (hue/saturation/vibrance/luminance, -100..100) is a cross-channel block applied
           AFTER the tone curve (it mixes channels, so unlike the tone curve it cannot be
           a per-channel LUT): hue = a Rodrigues rotation about the neutral (1,1,1) axis
           so greys stay grey (kHueMaxRad = 60deg at full); saturation = chroma scale
           about Rec.709 luma (-100 -> grey, +100 -> 2x); vibrance = the same chroma
           scale but weighted per pixel by (1 - HSV saturation), so muted pixels move
           more than already-saturated ones (protecting skies/skin from clipping);
           luminance = uniform gain (-100 -> black, +100 -> 2x). hslActive gates the
           block; identity sliders leave the pass untouched.

           SPATIAL ops (Texture #6, Dehaze #7, Vignette #8, Grain #9) run AFTER the fused
           point pass, each owning a full-image pass (like Denoise) -- they need a
           neighbourhood (or, for Vignette/Grain, pixel coordinates / a noise field) so
           cannot join the per-pixel kernel. Texture and Dehaze are ratio-preserving on LUMINANCE (chroma
           untouched) and scale their blur radius to the image's longer edge so the
           screen-res proxy and the full-res settle render shape the same frequency band.
           Texture = mid-frequency local contrast: a Gaussian base (kTextureSigmaFrac
           ~0.0015*edge) splits a detail band that + amplifies (kTextureGain) and -
           smooths. Dehaze = APPROXIMATION (not dark-channel-prior; user-chosen): a
           large-radius local-contrast boost (box blur, kDehazeSigmaFrac ~0.02*edge -- box
           because its running-sum cost is ~radius-independent) + a contrast pull about a
           low pivot (kDehazePivot 0.3, deepens shadows) + a saturation lift (kDehazeSat);
           + clears haze, - adds it. Vignette = a radial exposure falloff about the
           centre: mask = pow(rn, k), rn the elliptical radius (0 centre, 1 corner), k
           from the Feather slider (high feather -> low k -> gradual/reaches inward; low
           feather -> corner-hugging); corner gain = 2^Exposure (- darkens, + brightens),
           scaled by the mask. Off-centre / shaped vignettes use radial masks, so this
           global op is just Exposure + Feather. Grain = monochromatic film grain added to
           perceptual luminance (ratio-preserving, like Texture): a fixed-seed noise field
           on a coarse grain-cell grid (Size sets the cell, scaled to the long edge so the
           proxy and full res match) is bilinearly upsampled, its amplitude modulated by a
           lower-frequency Roughness field (0 uniform, 1 patchy), weighted toward the
           midtones (fades to 0 at black/white) and scaled by Amount. The fixed seed keeps
           the pattern stable across re-renders (no shimmer). These are global (no-mask)
           now; the layer compositor will bound them to a mask bbox later.
           Costs land only on the full-res settle render (proxy filters ~3MP). The Effects
           "Denoise" is two 0..100 sliders (mapped to 0..1) into Develop::Denoise: "Denoise"
           (localDenoiseLuma) = luminance NR via a perceptual bilateral (ratio-preserving), and
           "Denoise Color" (localDenoiseChroma) = chroma NR via a downscaled opponent-chroma
           (R-Y, B-Y) blur that keeps luma exact. Luma runs first, then chroma. All ten Basic
           sliders are now live.

           SLIDER-DRAG LATENCY: the naive path re-rendered the full image synchronously on the
           GUI thread every tick. For a 50MP RAW that measured ~520 ms/tick (probe below),
           dominated by OutputTransform::ToImage (~420 ms) -- it was the only serial stage while
           Develop::applyPointOps was already parallel. Three fixes (DevelopProperties::
           paramsChanged -> MW::developParamsChange -> MW::renderDevelopPreview):
             - PROXY: a drag renders a screen-resolution proxy WorkingImage
               (WorkingImageCache::downscaled, INTER_AREA in scene-linear, sized ~1.5x the loupe
               viewport, cached per path in MW::developProxy) and FAST-upscales the result to the
               displayed dimensions so ImageView::setDevelopPreview's "same dimensions" contract
               holds (zoom/fit/scene untouched). Raw orientation is applied while the proxy is
               still small (cheap). ~6-20x less pixel work per tick.
             - PARALLEL ToImage: OutputTransform::ToImage now splits rows across the global pool
               (same idiom as applyPointOps), cutting the per-pixel pow cost ~Ncore.
             - COALESCE + SETTLE: developParamsChange does no rendering itself -- it arms a 0ms
               single-shot (developProxyRenderTimer) that collapses a burst of ticks into one
               proxy render, and (re)arms developFullResTimer (~160 ms) so the crisp FULL-res
               render runs once the slider stops.
             - ASYNC FULL-RES (2026-06): the settled full-res render is ~1.3 s on a 50MP RAW and
               was run synchronously on the GUI thread, freezing the drag. Worse, the freeze
               drained queued slider events in bursts whose gaps re-fired the settle timer, so the
               1.3 s render ran REPEATEDLY mid-drag. [DevTime] confirmed: proxy ticks ~45 ms
               (render 25 / rotate+scale 18 / preview 2), but interleaved `full 8640x5760 render
               1300-1440 ms`. Fix: developFullResTimer now calls MW::renderDevelopFullResAsync,
               which drives the render on a dedicated 1-thread pool (developRenderPool) OFF the GUI
               thread; the proxy keeps the drag live meanwhile. The result is marshalled back via
               QMetaObject::invokeMethod -> MW::onDevelopFullResReady and applied only if still
               current. STALENESS GUARD: developParamsGen is bumped on every paramsChange; a render
               whose captured gen != developParamsGen (or whose path != currentFilePath) is
               discarded, and onDevelopFullResReady re-arms the settle timer so the latest value
               still gets a crisp render. IN-FLIGHT GUARD: developFullResInFlight allows only one
               full-res render at a time. Orientation is computed on the GUI thread
               (MW::developOrientationDegrees, reads dm->sf) before launch; the worker touches only
               the const WorkingImage (held alive by a shared_ptr) and value-copied params.
               Compositing (render + rotate + proxy upscale) is shared by both paths via the
               file-local developComposite() helper.
             - SPATIAL-OP COST (2026-06, follow-up): with texture/dehaze sliders left non-zero
               (they carry over between images until per-image params land), the async full-res
               render was still ~1.45 s. Per-stage [DevTime] (WorkingImageCache::RenderTimings +
               Develop::StageTimings) pinned it on the spatial ops, NOT the point pass:
               point 64 / texture 899 / dehaze 399 ms. Two fixes: (a) the per-pixel luminance/
               output loops in Develop::Denoise/Texture/Dehaze were SINGLE-THREADED -- now split
               across the global pool via the file-local parallelFor() (same idiom as
               applyPointOps/ToImage; identical math, bit-for-bit output). (b) Texture's large-
               sigma cv::GaussianBlur dominated what remained (~600 ms): the base is low-frequency,
               so it is now computed on a luminance image downscaled by kDown (=clamp(sigma/3,1,4),
               INTER_AREA down / INTER_LINEAR up) and the cost drops ~kDown^2. Net: full-res render
               ~1.45 s -> ~360 ms (texture 899->72, dehaze 399->120) on an M4. Spatial ops still
               recompute every full-res render because they run AFTER the point pass (an exposure
               change alters their input); the real win for an untouched image is per-image params
               so a 0 slider early-returns -- see item 4 (XMP sidecar). GENERAL RULE: any new
               per-pixel Develop loop must use parallelFor; any new large-kernel blur should use a
               downscaled base (or an O(1) box/integral approach) -- files keep growing.
           PROBE: set G::isReportDevelopTime to log [DevTime] lines (proxy/full/full(async), dims,
           proxyBuild / render+compose / preview ms; the async line adds the develop sub-stage
           breakdown denoise/point/texture/dehaze). Off in production.
        2. Layer object + compositor: add EditStack, run the fused loop over layers with opacity;
           convert the dock's "layers-as-alternatives" into a real stack (no masks yet).
        3. Parametric masks (linear/radial): analytic inline + per-layer alpha cache.
        4. XMP sidecar: serialise/deserialise EditStack; retire the QSettings per-layer store.
           DONE (2026-06, Increment 1) -- PER-IMAGE DEVELOP STATE:
             - DATA MODEL: Develop/editstack.h. EditStack { version; QVector<EditLayer> }, each
               EditLayer { name; mask (reserved -- empty = global, mask editing deferred); opacity
               (reserved); enabled; EditParams params }. This is the layer = mask + settings shape
               so the format does NOT change when masks/compositor land. Serialized to JSON then
               base64 (raw JSON quotes are awkward in an XML attribute). VERSIONED and FORWARD-
               TOLERANT: unknown keys ignored, missing keys default to identity. Header-only and
               free of any render-hot-path include (develop.cpp does not include it).
             - SIDECAR: stored as the winnow:Develop rdf:Description attribute (Xmp::initialize
               registers definedElements["develop"]; the winnow namespace already existed).
               Metadata::writeDevelopSidecar / readDevelopSidecar mirror writeOrientation (open
               sidecar, one Xmp item). An identity stack writes "" which CLEARS the attribute (no
               sidecar is created just to store nothing).
             - DOCK (Develop/Properties/developproperties.cpp): the sliders now reflect the CURRENT
               IMAGE's EditStack, not app-global QSettings. setCurrentImage(fPath) flushes the
               previous image then loads+caches this image's stack and pushes its params into the
               editors (populateSlidersFromStack, guarded by isPopulating so the programmatic
               setValue->itemChanged does not re-mark dirty). itemChange writes into the current
               image's active-layer params, marks it dirty, drives the live preview, and (if gated)
               arms the debounce write. editParams() returns the ACTIVE layer's params (the
               renderer shows the active layer -- no mask/opacity compositing yet).
               LAYER HEADER (Develop/Properties/layerheader.cpp): the layer dropdown lives in a
               gradient band WIDGET above the tree (the same header gradient as TransformPanel; the
               old in-tree "Core" and "Layers" headers are gone). Layout:
               > Layer [dropdown v] [...] [E]. The '>' collapse arrow ->
               setTreeCollapsed hides/shows ONLY the layer's top items (the Core rows
               for Base, else the mask-tool rows -- the root rows above BasicHeader;
               applyLayerItemsCollapsed, re-asserted after each buildTree since a
               rebuild resets row visibility). The Basic/Color/Effects sections stay
               visible. [E] -> onLayerPreviewToggled (whole-layer eye). The DROPDOWN
               carries ONLY the layers (LayerHeader::setLayers): the image's layers,
               the active one marked with a checkmark -> onLayerSelected (switch
               activeLayerIndex; then reveal the layer's OWN section -- un-collapse its
               mask rows (setTreeCollapsed(false), bulk-guarded so Solo leaves
               Basic/Color/Effects as-is) and select its first mask so the coverage
               overlays the image, selectedMaskIndex 0 (Base has none, -1)). The layer
               ACTIONS live on a separate ellipsis menu button [...] (layerMenuBtn)
               between the dropdown and [E], patterned on EmbelProperties::effectNewBtn
               -> effectContextMenu: clicking it pops a QMenu (LayerHeader::showLayerMenu,
               built on demand) with "Add new layer" -> newLayer (name dialog, default
               "Layer n"); then, below a separator, the per-layer actions "Add mask to
               <layer>" -> showMaskMenu and Reset/Remove/Rename <layer> ->
               resetActiveLayer / deleteLayer / renameActiveLayer. The per-layer group
               is OMITTED for Base (index 0) -- LayerHeader::setBaseActive, driven by
               updateMaskMenuBtn. MASK ENTRY POINTS: a layer's first mask tool is offered
               automatically when the layer is created (newLayer -> showMaskMenu); further tools are
               added via the dropdown "Add mask to <layer>" or the [+] glyph on each mask-tool row in
               the tree (UR_AddBtn, hit-tested in mousePressEvent -> showMaskMenu). The dropdown entry
               covers the case where the layer has no masks at all (skipped on create, or all deleted).
               DevelopProperties::bindLayerHeader connects these signals.
               TREE = ACTIVE LAYER: the tree is REBUILT for the active layer by buildTree() (on image
               change, layer switch and mask add/remove/select): the layer's top items first (Base =>
               the Edit-source selector + Demosaic + Denoise raw rows [formerly the Core header]; else
               the mask tool rows) followed by the Basic / Color / Effects sections.
               EDIT SOURCE (RAW files only, Base layer -- addCoreItems): the first Base top row is
               "Edit:" hosting an A/B radio pair "Raw" / "Embedded Preview" (a plain QWidget in the
               value cell of a DT_None row, recreated each buildTree). It is kept in sync with G::useRaw
               and the status-bar useRaw button BOTH WAYS: picking a radio -> onEditSourceChanged ->
               useRawRequested signal -> MW::toggleUseRaw (a private slot, hence the hop); the button ->
               toggleUseRaw -> DevelopProperties::syncEditRaw sets the radio (signals blocked) back.
               currentIsRaw() (MW::isFileRaw = suffix in metadata->hasJpg) gates the whole Core block --
               non-raw files get no Core rows (they ARE the developable image). The Demosaic + Denoise
               raw rows show only when editing raw: applyCoreVisibility() (hide = !useRaw ||
               layerItemsCollapsed) runs LAST in buildTree (after applyLayerItemsCollapsed, which would
               otherwise re-show them) and again from setTreeCollapsed/syncEditRaw. Demosaic/Denoise raw
               are root leaves, so they carry UR_ExtraIndent (one extra indent level in the
               delegate's paint + sizeHint) to line up with the Basic sliders ("Temp"), which sit a
               level deeper as BasicHeader children; "Edit:" is left-aligned (not indented). buildTree close()s + removes all rows then
               re-adds them, preserving the sections' expand-state within a session (read from the
               current rows on rebuild). ACROSS SESSIONS the Basic/Color/Effects expand-state also
               persists in QSettings under "Develop/SectionExpanded/<Header>": the first build (no
               rows yet) seeds each header's default from sectionExpanded() (first-ever run => Basic
               open, Color/Effects closed), and QTreeView::expanded/collapsed -> persistSectionExpanded()
               writes the state whenever the user toggles one of the three headers (mask tool rows,
               which also expand/collapse, are filtered out by name). SECTION HEADERS: the Basic, Color
               and Effects headers append the active layer's name (e.g. "Base: Basic", "Layer 1:
               Color") via updateSectionHeaderCaptions(). PREVIEW / RESET: each section header
               (Basic/Color/Effects) carries an eye BarBtn that toggles that group's Preview flag on
               the active layer; the whole-layer eye + reset live in the LayerHeader widget
               (EditLayer::showBasic/showColor/showEffects/showLayer, all default true, persisted in
               the sidecar only when false). Preview is
               NON-DESTRUCTIVE: effectiveLayerParams() returns a COPY of the layer's params with any
               previewed-off group folded to identity (EditParams::resetGroup) -- stored values are
               kept, so re-showing restores them; showLayer folds all three. The fold is applied at
               every render-capture site (EditStack::effectiveParams, DevelopProperties::editParams
               and stackJob). Right-clicking a header (contextMenuEvent) offers Preview (toggle) +
               Reset <group>; Reset is DESTRUCTIVE -- EditParams::resetGroup on the
               STORED params, then repopulate. The same menu (shown anywhere in the
               tree, not only on a header) also carries the tree-wide items Expand all /
               Collapse all / Solo mode, mirroring the Embellish dock: Expand all /
               Collapse all go through DevelopProperties::setAllSectionsExpanded,
               which calls QTreeView expandAll/collapseAll AND drives the Layer row
               (its collapse arrow lives in the LayerHeader band, not the tree --
               LayerHeader::setCollapsed + setTreeCollapsed). Solo mode is a checkable
               toggle (persisted as Settings "Develop/isSolo", restored in initialize
               via setSolo) that, when on, collapses the other top-level sections
               whenever one is expanded (PropertyEditor::collapseAllExcept in
               mousePressEvent). The Layer row is folded into Solo too: expanding an
               adjustment section (Basic/Color/Effects) collapses the layer
               (DevelopProperties::onSectionExpanded) and expanding the layer collapses
               those sections (setTreeCollapsed). The Demosaic row is a layer item, NOT
               a Solo peer, so expanding it never collapses the layer. A bulk guard
               (isBulkExpandCollapse) keeps Expand all from tripping the Solo handler.
               Group->keys mapping follows the UI (texture/dehaze are Basic; tone
               splits are Basic for Reset only; localDenoiseLuma -- the Effects "Denoise" (local NR,
               key "localDenoise") -- is in Effects; denoiseLuma/denoiseChroma are the Base "Denoise
               raw" (decode-time global NR, key "denoise") and in no group). eye.png/eye_off.png live
               in images/icon16. Transform has its
               own Preview eye + Reset in TransformPanel, wired to MW (previewToggled/resetRequested);
               its eye is a live result toggle, not a group fold (see the Transform section below).
               NAMING: index 0 is always
               the "Base" layer (normalized on load in setCurrentImage); extra layers added with +
               are numbered "Layer 1", "Layer 2", ... (the new layer's number = its index). The Base
               layer CANNOT be removed -- deleteLayer refuses when the active layer is
               index 0 (popup "The Base layer cannot be removed.").
               RENAME: the header's [R] button -> renameActiveLayer pops a QInputDialog
               seeded with the current name; uniqueLayerName resolves clashes with a
               numeric suffix; Base (index 0) is rejected with a popup "The Base
               layer cannot be renamed." An all-identity stack is
               never persisted, so empty layers added to an unedited image are not written (EXCEPTION:
               a layer that has any mask component counts as non-identity so masks survive
               navigation). The old QSettings layer helpers (readLayerList/setCurrentLayer/
               layerRootPath/layerValue) are left defined but unused; presets (Increment 2) will
               repurpose a QSettings store.
             - MASK (UI increment; compositor + canvas overlay deferred): each NON-Base layer has ONE
               mask, built from an ordered list of tools -- editstack.h EditLayer.masks is now that
               tool list (MaskComponent: tool/op/inverted/feather + opaque paramsJson for per-tool
               geometry). Each tool ADDS or SUBTRACTS area; the first tool must Add (nothing to
               subtract from an empty mask). The combine field is legacy/unused. A layer's first mask
               tool is offered automatically when the layer is created (newLayer -> showMaskMenu); Base
               (index 0) carries no mask. showMaskMenu pops a menu of "Add <tool> Mask" and (once a tool
               exists) "Subtract <tool> Mask" entries -- every tool name ends in "Mask" and the AI
               tools drop the "Select" prefix (maskToolName): Linear/Radial Gradient, Brush, Color/
               Luminance Range, Subject/Sky/Background, Depth Range. ADD-LAYER FLOW: newLayer pops
               the "New layer" name dialog at the cursor (the [+] button just clicked, clamped to the
               screen) instead of screen-centre, and on accept immediately calls showMaskMenu -- a new
               layer exists to carry a masked adjustment, so choosing the mask tool is the next step
               (Escape leaves an empty layer). ADD-MASK PLACEHOLDER: a non-Base layer with no mask yet
               (e.g. Escape was pressed) shows a single "Add mask" row at the top of the tree with a
               [+] glyph -- a header-style UR_LeafSingleLine spanned row carrying only UR_AddBtn (no
               [-], no expand arrow), added by addAddMaskRow in addMaskItems. Clicking anywhere on it
               (it has no UR_MaskIndex; mousePressEvent identifies it by UR_Name "addMask") pops
               showMaskMenu. It disappears as soon as a tool is added -- the layer then shows its tool
               rows, and adding a tool selects it, so the placeholder is present precisely when no mask
               is selected. Each tool is a top-level tree
               row (caption "Add/Subtract <tool>" with a [-] remove button; the tools sit ABOVE the
               Basic/Color/Effects sections, added by addMaskItems in buildTree). The rows are
               header-style for the single-line full-width caption + expand arrow, but flagged
               UR_LeafSingleLine (new role) so the delegate paints them in the LEAF colour, not
               category teal. UR_ShowDecoration forces the delegate to draw the expand arrow even when
               the row has no children (an UNSELECTED tool carries no settings children), so every
               mask tool shows a closed disclosure arrow (open when selected and its settings show).
               Each tool row is a single FULL-WIDTH spanned cell
               (setFirstColumnSpanned) and the [-] remove glyph is DRAWN BY THE DELEGATE at the
               row's right edge (UR_DeleteBtn role), not a value-column button widget: a column-1
               widget over the caption gets clipped/covered on expand (the caption's overflow into
               column 1 is outside the expand repaint's dirty region, and a covering index widget
               composites stale until a full repaint). mousePressEvent hit-tests the glyph rect
               (mirroring the delegate geometry) to delete; clicks elsewhere on the row toggle it.
               [Delegate general behaviour] PropertyDelegate::paint spans EVERY header caption
               (UR_isHeader) across the full row (caption + value columns). The delegate painter is
               NOT clipped to the cell (confirmed by probe: QPainter::hasClipping() is false during
               item paint), so a single column-0 pass draws the caption straight across both columns
               on a full repaint. The catch is PARTIAL repaints: dragging the dock splitter resizes
               the (stretch) value column, which marks ONLY the value-column cells dirty. QTreeView
               then repaints just those cells' background, erasing the caption overflow that was
               drawn during column 0's pass (column 0 is not re-run) - so a long header caption
               truncated back to the column boundary after a resize even though hasClipping()==false
               and the elided text was the full string. Fix: the caption is drawn in BOTH passes -
               the value-column pass fills its own background (gradient, or the bottom separator for
               non-gradient headers) and then redraws the SAME elided caption at the SAME viewport
               rect, so a value-column-only repaint restores it. capLeft must be identical in both
               passes; for a decoration row it is the column-0 cell left (view->visualRect(capIndex)
               .x()), reproducible without column 0's rect. The caption right edge is pulled in and
               the text elided when the value field is occupied: before the delegate-drawn [-] glyph
               (UR_DeleteBtn), or before a value-column widget (value sibling's UR_Editor geometry +
               childrenRect; e.g. the Layers/Templates -/+ buttons, which are index widgets
               composited on top and so still show). setFirstColumnSpanned is still used by mask-tool
               rows (so column-1 is not painted at all) but is NOT required for plain headers.
               Clicking a tool toggles it: the SELECTED tool expands to show its
               settings as ITS OWN children (Feather slider, Invert checkbox) -- no duplicate
               tool-name row; only one tool is expanded at a time. Clicking the tool caption again
               collapses it (setSelectedMask(-1)), so there is no separate Done row. NB
               PropertyEditor::mousePressEvent does NOT select rows (only expand/collapse), so
               DevelopProperties overrides mousePressEvent to read the clicked row's UR_MaskIndex and
               toggle it (and returns without the base expand/collapse). All mask UI is isolated in
               developproperties.cpp (showMaskMenu/newMask/deleteMask/rebuildMaskTools/addToolRow/
               setSelectedMask).
             - SPATIAL EDITING (Linear + Radial Gradient): edited directly on the loupe, not the
               dock. Geometry lives in MaskComponent.paramsJson as normalized coords: Linear
               {x1,y1,x2,y2}; Radial {cx,cy,rx,ry,angle} (cx/cy by W,H; rx by W, ry by H; angle deg).
               Resolution/zoom independent; survives proxy/full re-render. Handshake (wired in MW,
               mirroring paramsChanged): selecting a spatial tool fires DevelopProperties::maskEditBegin
               (tool,op,inverted,paramsJson,feather) -> ImageView::beginMaskEdit; deselecting fires
               maskEditEnd. updateMaskEdit() (end of rebuildMaskTools) tracks every image/layer/
               selection change; developDockVisibilityChange() ends/re-asserts it so the overlay never
               leaks into the plain loupe. ImageView::drawForeground dispatches by tool to
               drawLinearMask / drawRadialMask, shown only while the cursor is over the view (maskHover
               via enter/leaveEvent). Linear: tint ramp + centre line + 0%/100% perpendicular guides +
               endpoint/centre handles; endpoint drag rotates+scales about the fixed centre, centre/line
               drag translates. Radial: elliptical tint (a unit-circle QRadialGradient mapped through a
               brush transform to the rotated image ellipse) + outline + centre + 4 axis handles; centre
               handle moves, axis handles resize each semi-axis (centre fixed), dragging the outline
               rotates. Tints/handles draw in viewport coords for constant on-screen size; a miss falls
               through to pan/zoom. Drag emits maskGeometryChanged LIVE (and on release) ->
               DevelopProperties::setActiveMaskParams (stores into the component, debounce-persists, no
               tree rebuild). Feather/Invert live-update via maskFeatherChanged/maskInvertChanged; tint
               colour conveys op (Add=red, Subtract=blue); Invert flips (Radial: effect outside).
             - WHOLE-LAYER MASK TINT (composite overlay): while ANY mask tool is expanded (its settings
               shown, i.e. selectedMaskIndex >= 0 on a non-Base layer), the loupe shows the ENTIRE layer
               mask -- all the layer's Add/Subtract tools composited -- as a single red coverage tint,
               and hides it when all tools are collapsed. MW::updateMaskOverlayTint (wired to
               maskEditBegin/maskEditEnd + paramsChanged) reuses the render-path buildMaskBuffer (so the
               tint is pixel-consistent with the developed result) at a capped resolution (~1600 px long
               edge; geometry is normalized so any size is faithful), builds the AI/range refs it needs
               (cached), converts coverage -> premultiplied red (alpha up to 150), orients it to the
               displayed pixmap, and hands it to ImageView::setLayerMaskTint (clearLayerMaskTint when no
               tool is expanded / DevelopProperties::maskOverlayActive() is false). ImageView::
               drawForeground paints this composite tint UNDER the active tool's handles, NOT hover-gated
               (unlike the per-tool handles); when it is present the per-tool draw takes drawTint=false
               so it draws only its handles/guides/cursor/swatches (its own tint would double up). The
               composite reflows live during a geometric drag (setActiveMaskParams -> paramsChanged ->
               rebuild). "O" (toggleMaskOverlayAction -> MW::toggleMaskOverlay -> ImageView::
               toggleMaskTint; a Develop mode local shortcut, Open Folder in
               Preview mode -- see DEVELOP MODE LOCAL SHORTCUTS) HIDES/SHOWS
               this overlay while editing -- flips ImageView::maskTintHidden,
               which gates BOTH the whole-layer composite and the per-tool preview in drawForeground
               (handles/cursor keep drawing so editing continues); reset to shown on each beginMaskEdit,
               no-op outside mask editing. Changing ANY adjustment slider (Basic/Color/
               Effects, or any future key via applyKeyToParams) while the overlay is shown
               also hides it (DevelopProperties::maskTintHideRequested ->
               ImageView::hideMaskTint) so the effect on the masked pixels is visible;
               re-shown by "O" or by re-selecting a mask tool. Mask-only edits (Feather/
               Invert/Range/Brush settings) keep the overlay shown.
             - RENDER COMPOSITING (Stage 4b + N-layer stack): the render composites the WHOLE stack
               in SCENE-LINEAR before the output transform, INDEPENDENT of which layer is active for
               editing -- so a saved mask is visible the moment the image loads (no need to select
               its layer). DevelopProperties::stackJob() captures it as plain values (Base params +
               each enabled non-Base layer's params/masks/combine) so it is safe for the off-thread
               full-res render. WorkingImageCache::renderStack starts from Base (applied globally),
               then for each layer develops the RUNNING ACCUMULATOR `acc` with that layer's params (an
               identity side aliases acc -- no copy/Develop) and blends back over acc by its mask
               (acc = acc*(1-m)+layer*m; an empty/global mask applies the params globally on top); one
               final OutputTransform. MASK BLEND SPACE is an A/B flag (kPerceptualMaskBlend in
               workingimagecache.cpp): A = lerp in scene-linear (physically faithful, but a STRONG
               masked adjustment's visible falloff is front-loaded -- display gamma steepens it near
               m=1, so a linear feather reads abrupt at the size edge); B (current) = lerp in a
               PERCEPTUAL (gamma 2.2) space so DISPLAYED brightness tracks m ~linearly and a feather
               reads evenly (== the coverage tint). Only feather pixels (0<m<1) pay the pow; m<=0 skip,
               m>=1 copy. Flip the flag to compare/revert. Because each layer develops the accumulated result (NOT the
               original work), where two layers' masks OVERLAP their adjustments COMPOUND rather than
               the top layer replacing the one below -- e.g. two overlapping +1-stop layers give ~+2
               stops in the overlap (in scene-linear the per-op composition is natural: exposure
               multiplies, etc.). Layer ORDER therefore matters for non-commuting ops. buildMaskBuffer
               (mainwindow.cpp) rasterizes a layer's component(s) at WorkingImage (pre-orientation)
               resolution; each pixel maps work-normalized -> output-normalized (output = work
               rotated CW by degrees) so geometry edited on the oriented loupe lines up after the
               EXIF rotation. parseMaskComp/evalMaskComp branch by tool: Linear = projection ramp;
               Radial = distance in the ellipse's local frame (computed in OUTPUT-PIXEL space so the
               ellipse keeps aspect + rotation), 1 inside -> 0 at the boundary. The BRUSH component is
               not analytic: its strokes are RASTERIZED (Develop/brushstamp.h, shared with the
               ImageView preview so they match) into a work-space coverage buffer, cached
               (brushRasterCached, paramsJson+dims+degrees key, mutex-guarded since the GUI proxy and
               full-res worker both build it; only proxy-size buffers are cached -- full-res is ~w*h*4
               bytes and built once per settle). Components combine in order: brush samples its buffer
               (then component Invert), parametric uses evalMaskComp. Falloff smootherstep,
               Add=union(max), Subtract=*(1-c), Invert flips. developComposite remains the fast
               single-pass path when there are no non-Base layers. Mask add/delete/geometry/feather/
               invert and a committed brush stroke emit paramsChanged() to re-render. Deferred: true
               STACKED adjustments (each layer builds on the result below, not the original), the
               combine field (Union only today), and AI tools.
             - BRUSH (freehand mask): vector strokes in paramsJson {size,flow,autoMask,autoMaskMode,
               strokes:[{pts,size,feather,flow,erase,autoMask,autoMaskMode}]} (each stroke snapshots
               its settings, incl. the auto-mask mode; current
               settings drive the next stroke). DEFAULTS: feather 0 (crisp), flow 100. Painted on the
               loupe (ImageView): a two-ring cursor -- the OUTER ring is the brush SIZE (the total
               extent; coverage reaches 0 there), and when feather>0 an INNER ring at radius*(1-f/2)
               (the half-coverage radius) marks the feather, matching Lightroom's cursor. left-drag
               paints, Alt-drag erases; dabs = smootherstep dabs accumulated per stroke by MAX then
               composited with flow (build-up across strokes). FEATHER = LIGHTROOM MODEL (softens
               INWARD): coverage is a full-strength core out to radius*(1-feather/100), then
               smootherstep to 0 at radius (BrushStamp::coverage) -- so feather does NOT change the
               brush footprint (size = outer extent, fixed); it shrinks the solid core inward
               (feather 0 = hard edge at the size ring, feather 100 = fully soft, core -> centre).
               Pairs with the perceptual mask blend (kPerceptualMaskBlend) so the displayed effect
               tracks the coverage the way Lightroom's does. Live red/blue tint
               preview rebuilt only on the dirty segment bbox; the develop re-composites on stroke
               release (maskGeometryChanged -> setActiveMaskParams). WHILE a stroke is being swiped the
               whole-layer composite tint (maskLayerTint, rebuilt only on release) is SUPPRESSED
               (drawForeground brushStroking guard) so the per-brush live preview shows the stroke
               building up in real time instead of the tint only appearing after release. Dock rows: Size/Feather/Flow/Auto
               mask/Invert. Size also via [ / ] keys or a two-finger drag (ImageView wheelEvent, gated
               to brush+hover) -> maskBrushSizeRequested -> setActiveBrushSize syncs the dock; "A"
               toggles auto-mask (-> maskBrushAutoMaskRequested -> setActiveBrushAutoMask); Cmd/Ctrl+Z
               removes the last stroke. ImageView needs focus (set on brush enter) and claims [ ] A
               from their global action shortcuts via QEvent::ShortcutOverride. Each cursor ring is
               drawn twice -- a dark halo under the coloured line -- so it stays visible on light AND
               dark images (Level-tool halo idiom). Cursor hint: dashed
               blue = AI auto-mask, green = luminance auto-mask, pink = erase, white = plain. AUTO-MASK has TWO MODES
               (per-stroke, chosen by the "AI edge (SAM)" dock checkbox; stored as autoMaskMode
               "lum"|"ai" in the stroke + brush paramsJson; the "A" key just toggles auto-mask on/off,
               keeping the current mode). Both attenuate each dab by an edge factor
               (BrushStamp::edgeFactor) evaluated in the SHARED rasterizer, so preview == render:
                 * LUMINANCE ("lum", default): a luminance guide (BrushStamp::Guide, ~1024 long edge,
                   output-oriented) is built once from the loupe pixmap when the brush activates
                   (ImageView::ensureAutoGuide) and registered by path (BrushStamp::putGuide); the
                   render samples the SAME guide (getGuide(fPath), threaded through
                   developCompositeStack/buildMaskBuffer/brushRasterCached). A flagged stroke paints
                   only where |lum - lumRef| < tol (lumRef = guide luminance at the stroke start;
                   smootherstep falloff).
                 * AI ("ai"): confines the stroke to the SAM 2 object UNDER ITS START POINT. The
                   encoder is PRE-WARMED on entering brush-AI (MW::onAiMaskEditBegin when an AI-mode
                   brush is selected/added; MW::warmBrushSamEncoder via maskBrushAiEnabled when the
                   checkbox is ticked) so the first stroke pays only the ~40ms decode. On stroke start
                   ImageView emits maskBrushSamFieldRequested(seed) -> MW::onBrushSamFieldRequested
                   -> ensureBrushSamField: the shared Object-Mask encoder (ensureObjectEncoder, cached
                   ~1s/image) plus a POINT-prompt decode (ObjectMaskPredictor::refinePoint: one +ve
                   point label 1 + one pad label -1, has_mask_input=0 -- fits the fixed 2-point decoder,
                   no re-export) yield a 0..1 object coverage field, stored as a BrushStamp SAM field
                   keyed by path+seed (putSamField/getSamField, samFieldKey). edgeFactor with aiField=1
                   returns that coverage directly, so the dab is clipped to the object even across a
                   same-luminance background. The render pre-pass (MW::ensureBrushSamFields, called in
                   updateMaskOverlayTint + the two develop render paths) repopulates fields the store
                   dropped. LIMITATION: a truly cold render (reopen / evicted store) before the field
                   is decoded paints that stroke UNCONFINED until the mask is re-entered. This "ai" mode
                   is a 2nd auto-mask option for A/B testing against "lum" -- shares the SAM 2 encoder
                   with the OBJECT MASK tool (below).
             - CONTENT-RANGE MASKS (Luminance Range + Color Range): coverage depends on the pixel
               VALUE, not its position, so unlike the geometric tools these need the image's pixels.
               Both measure on a SHARED display-referred reference (Develop/rangemask.h,
               RangeMask::RangeRef -- interleaved RGB 0..1, output-oriented, capped ~1024), built and
               registered by path (putRef/getRef, mirroring BrushStamp's guide) so the loupe overlay
               and the develop render sample the IDENTICAL map (preview == render). The reference is
               the developed BASE layer only (Base params + OutputTransform + orientation, MW::
               ensureRangeRef via developComposite on a downscaled work) -- "what you see", so the
               selection tracks the base tones -- and BASE-ONLY on purpose: measuring on the composite
               would let a range mask that shifts its own tones feed back on its own selection and
               oscillate. ensureRangeRef is cached (keyed on path + a base-params signature; run from
               renderDevelopPreview and renderDevelopFullResAsync only when a range mask exists), so a
               range-slider drag or a colour sample never rebuilds it -- they just re-threshold the
               cached reference. buildMaskBuffer gains a third component kind (isRange) beside brush /
               parametric: it fetches getRef(fPath) once (absent -> the component yields 0, like a
               brush with no guide) and per pixel calls RangeMask::lumCoverage / colorCoverage at the
               mapped output coords, combining with the same Add=max / Subtract=*(1-c) / Invert logic.
               LUMINANCE RANGE: paramsJson {lo,hi} (0..1 display luminance, Rec.601); coverage 1 inside
               [lo,hi] with a smootherstep band from MaskComponent.feather. Dock rows Range Min / Range
               Max / Feather / Invert; lo/hi changes emit maskRangeChanged -> ImageView::
               setMaskRangeParams (live tint). No canvas interaction. COLOR RANGE: paramsJson
               {refine (0..100, higher = tighter), samples:[[r,g,b],...] 0..1 display}; coverage from
               the nearest sample's distance in a cheap luma/chroma opponent space (luma down-weighted,
               like Lightroom) within a refine-derived tolerance, smootherstep-feathered. EYEDROPPER:
               with the Color Range tool active, a loupe click samples RangeRef under the cursor (cross
               cursor), shift-click ADDS another sample; sampled colours draw as on-image swatches
               (bottom-left, viewport coords) and clicking a swatch removes it (pointer cursor). Sample
               edits go back through the existing geometry handshake (ImageView::maskGeometryChanged ->
               DevelopProperties::setActiveMaskParams), so no new persist path. Dock rows Refine /
               Feather / Invert. The tint (ImageView::drawRangeMask / buildRangePreview) is red (Add) /
               blue (Subtract) like the other tools and rebuilds on any dock param change (maskRange
               Changed / setMaskFeather / setMaskInverted) or eyedropper edit; it heals if the
               reference lands after the overlay began (buildRangePreview on a null preview). Deferred:
               a dual-handle luminance range slider, per-layer "image below" reference for stacked
               range masks, and an in-dock swatch strip (swatches live on the loupe for now).
             - INFERENCE LAYER (Utilities/inference/): the unified model-inference seam that new
               neural features use. InferenceSession (front door) owns an InferenceBackend; the only
               concrete backend today is OrtBackend (ONNX Runtime). Per-platform execution providers
               are selected at load: CoreML EP on macOS (dispatches to the Apple Neural Engine / GPU),
               DirectML on Windows, CPU everywhere as the fallback. All ORT contact is confined to
               ortbackend.cpp behind a PIMPL, and the whole layer is guarded by WINNOW_HAVE_ORT: built
               without ORT (CMake option WINNOW_ENABLE_ORT=OFF, the default until the runtime is
               vendored on both platforms) it compiles to a stub -- IsLoaded()==false, Run() no-ops --
               so features degrade like a missing model. This DELIBERATELY supersedes the old cv::dnn /
               "no onnxruntime" approach for NEW models (it reaches the ANE and loads architectures
               cv::dnn cannot, e.g. attention/diffusion); the existing cv::dnn predictors (u2net/skyseg/
               midas/focus/SAM2, below) keep working unchanged and migrate opportunistically. The
               InferenceBackend abstract class is an ESCAPE HATCH: a native CoreMLBackend can be added
               for a model that needs maximal ANE utilisation (generative fill) without touching
               callers. FUTURE: we may move to a full native-per-platform solution (native Core ML on
               Mac + DirectML/native on Windows) if ORT's CoreML EP underuses the ANE.
               DEVICE SELECTION (InferenceDevice pref, ortbackend.cpp): the pref biases
               WHICH compute units the CoreML EP targets. Auto/NPU append the CoreML EP
               with flags 0 = the full set (ANE -> GPU -> CPU), the ANE-first default. GPU
               appends it with COREML_FLAG_USE_CPU_AND_GPU | COREML_FLAG_CREATE_MLPROGRAM,
               i.e. Apple GPU (Metal / MPSGraph) with the ANE EXCLUDED -- for graphs whose
               ops the ANE cannot service (e.g. FFT-based FFC blocks in LaMa-style
               generative fill); BackendName() then reports "CoreML/GPU" so a log line
               confirms the GPU units took. CPU forces the CPU EP. CAVEAT: this only picks
               the target DEVICE -- ops the CoreML EP cannot map are still partitioned
               back to the CPU EP regardless of the flag, so an end-to-end-GPU LaMa also
               depends on the ONNX export (FFT decomposed to matmul); the GPU flag alone
               does not guarantee it. Note there is NO ONNX Runtime "MPS execution
               provider" -- MPS-as-a-backend is a PyTorch concept; on ORT the GPU is
               reached THROUGH the CoreML EP's CPUAndGPU compute units as above. Only
               PMRID requests a device today (Auto), so the GPU path stays inert until a
               caller asks for it.
               Active consumer: PMRID "Denoise raw" (ImageFormats/Raw/pmrid.cpp, see
               "DENOISE RAW" above) -- it took over when the earlier RawRefinery "TreeNet"
               denoiser was removed 2026-07-12. Models resolve via applicationDirPath()
               + a ReleaseExtras/ .onnx, warn-if-absent, like the cv::dnn models.
             - SELECT SUBJECT MASK (AI): an AI salient-object mask that selects the main subject.
               Like the content-range masks it is CONTENT-derived and shares their preview==render
               design, but the coverage is a FIXED saliency map produced once by the U^2-Net ONNX
               model rather than a per-tick threshold. The model runs through OpenCV's DNN module
               (cv::dnn::readNetFromONNX -- the SAME runtime FocusPredictor uses; no onnxruntime
               dependency), wrapped in Utilities/subjectpredictor (SubjectPredictor): U^2-Net
               preprocessing (RGB, 320x320, /maxpixel, per-channel ImageNet mean/std). GOTCHA:
               U^2-Net has 7 outputs (d0..d6) and OpenCV's forward() with no name returns the LAST-
               registered layer, which here is a COARSE side output (d6 -- a fuzzy blob, the original
               "rough selection" bug); we fetch outputs by name (getUnconnectedOutLayersNames) and use
               outs[0] = d0, the fused full-res saliency, min-max normalized to a 0..1 map. The model
               file u2net.onnx (~176 MB) sits
               next to focus_point_model.onnx (executable dir on Windows, .app/Contents/MacOS on mac;
               CMake bundles/copies it, warning rather than failing if absent so the mask just
               no-ops). MW::ensureSubjectMask (mirroring ensureRangeRef) develops the downscaled BASE
               layer (output-oriented, "what you see"), runs the model synchronously on the GUI thread
               with a busy cursor (~200-400 ms, one-shot per image on the user's add/select action),
               and registers the coverage by path in Develop/subjectmask.h (SubjectMask::SubjectRef,
               single-channel). The raw 320x320 saliency is EDGE-REFINED before storage: upsampled to
               the reference (~1024) size and guided-filtered (He et al.) against the developed base's
               luminance so the cutout snaps to the real subject boundary instead of a blocky low-res
               ramp (subjectpredictor guidedFilterGray -- ~9 box filters, OpenCV core only, no
               ximgproc). It is CACHED BY PATH ONLY -- subject
               detection is independent of the develop sliders, so slider drags never re-run
               inference. buildMaskBuffer gains an isSubject component kind: it fetches getRef(fPath)
               once (absent -> yields 0, like a range mask with no reference) and per pixel calls
               SubjectMask::coverage at the mapped output coords, combining with the same Add / Subtract
               / Invert logic. paramsJson is empty (content-derived, no geometry); the dock shows only
               Feather (a smootherstep threshold band around the 0.5 saliency cutoff -- softens the
               cutout edge) and Invert (selects the background instead). The saliency is edge-tuned in
               subjectpredictor (small guided-filter radius = long/256 + eps 1e-6, then a contrast
               around 0.5) so the matte hugs the subject without a halo; Feather softens from that
               tight base. The loupe tint (ImageView::buildSubjectPreview, sharing drawRangeMask / the
               maskRangePreview buffer via rebuildContentPreview) is red (Add) / blue (Subtract) like
               the other tools, rebuilds on Feather / Invert changes, and heals if the saliency lands
               after the overlay began. CONTENT-MASK TINT PERSISTENCE: unlike the geometric tools
               (hover-gated -- their handles are pointer-relative), the Range/Subject coverage tint
               stays drawn as long as the tool is active (maskEditMode), even when the pointer leaves
               the view, so it is visible while adjusting Feather/Refine in the dock (drawForeground
               content branch + the setMaskFeather/setMaskInverted repaint no longer require maskHover).
               ensureSubjectMask is called from renderDevelopPreview / renderDevelopFullResAsync only
               when a Subject mask exists (stackHasSubjectMask).
             - SELECT SKY MASK (AI): the sky twin of Select Subject, built as a PARALLEL of it
               (Utilities/skypredictor SkyPredictor, Develop/skymask.h SkyMask store, MW::ensureSkyMask
               / stackHasSkyMask, an isSky component in buildMaskBuffer, ImageView::buildSkyPreview,
               tool==6). It runs a single-channel sky-segmentation ONNX (skyseg.onnx, next to
               u2net.onnx; CMake bundles/copies it, warning rather than failing if absent so the mask
               no-ops) through the SAME OpenCV DNN runtime. Preprocessing is the generic seg-net
               convention (RGB, /255, per-channel ImageNet mean/std); output is [1,C,H,W] with the sky
               channel taken (C==2 -> channel 1, else 0). The coarse map is edge-refined by the SHARED
               SegRefine pipeline (Utilities/segrefine.h -- min-max -> upsample -> guided filter vs the
               photo's luma -> contrast), the identical matting the Subject mask uses (that code was
               extracted into segrefine.h so both stay in sync). A SEPARATE path store from SubjectMask
               so a Subject and a Sky mask can coexist on one image. Dock rows Feather + Invert; the
               loupe tint, persistence and add/select build (MW::onAiMaskEditBegin handles the AI
               tools) all mirror Subject.
             - SELECT BACKGROUND MASK (AI): the INVERSE of Select Subject -- no new model or store. It
               reuses the SubjectMask saliency (ensureSubjectMask; stackHasSubjectMask + the subjRef
               fetch + onAiMaskEditBegin all treat Subject and Background the same) and simply selects
               1 - subject. In buildMaskBuffer the subject CompDesc gains subjectBaseInvert (true for
               Background), and coverage is sampled with (inverted ^ subjectBaseInvert) so Background
               with Invert-off = background, Invert-on = subject; the loupe tint (buildSubjectPreview)
               XORs the same way (maskIsBackground, tool==7). Dock rows Feather + Invert.
             - DEPTH RANGE MASK (AI): a HYBRID -- the MODEL produces a depth field (like Subject/Sky)
               but the SELECTION is a [near,far] BAND (like Luminance Range over depth). Model is MiDaS
               v2.1 small (midas.onnx, ~64MB, conv-based -- an official OpenCV DNN sample; ViT DPT /
               Depth-Anything won't load) via Utilities/depthpredictor: input [1,3,256,256], /255 +
               ImageNet mean/std, single output [1,256,256] inverse depth. The map is min-max'd and
               INVERTED to 0=near..1=far, then guided-UPSAMPLED against the photo (SegRefine::
               guidedUpsample -- continuous field, guided-filter joint upsampling, NO binary contrast).
               Stored in Develop/depthmask.h (DepthMask::DepthRef, its own path store) and built by
               MW::ensureDepthMask (stackHasDepthMask + a depthRef fetch + onAiMaskEditBegin, all
               mirroring Sky). buildMaskBuffer's isDepth CompDesc carries the band [rlo,rhi] from
               paramsJson {lo,hi}; per pixel it calls DepthMask::coverage = a smootherstep band over
               the sampled depth (the exact analogue of RangeMask::lumCoverage). Dock rows Near / Far /
               Feather / Invert (Near/Far reuse the maskRangeLo/maskRangeHi sources -> maskRangeChanged
               -> ImageView::setMaskRangeParams, which now accepts Depth; live tint buildDepthPreview).
               paramsJson is non-empty {lo,hi} so it is NOT content-derived (survives stackJob's empty-
               params skip normally). AI masks Subject/Background/Sky/Depth are fully implemented;
               Object Mask (below) is the one AI mask currently at scaffold stage.
               KNOWN LIMITATION (validated, not a bug): skyseg is strong on horizon-type skies (offline
               test on a wide winter forest/sky photo gave a near-perfect mask, bright snow correctly
               rejected) but weak on sky FRAGMENTED through dense canopy behind a large subject + bokeh
               -- the raw model output itself only fires on scattered patches there, so no post-
               processing recovers it. Workaround for such shots: a Luminance/Color Range mask + brush.
               FUTURE WIP (deferred): benchmark alternative sky models offline (onnxruntime) and swap
               only if one clearly beats skyseg on the hard case AND loads in OpenCV DNN.
             - OBJECT MASK (AI, PERIMETER-PAINT): a PROMPTABLE mask. The user traces the
               object BOUNDARY with a solid brush (multiple strokes; Opt/Alt erases); when
               the traced perimeter forms a CLOSED loop the enclosed region is filled and
               that silhouette is refined to the edge by a SAM 2 encoder+decoder pair via
               OpenCV DNN (NO onnxruntime -- feasibility proved by an offline spike:
               cv::dnn 4.11 loads+runs both graphs). Unlike Subject/Sky (one-shot per
               image) it is TWO-PHASE, matching SAM's
               heavy-encoder / light-decoder split: Utilities/objectmaskpredictor ObjectMaskPredictor::
               setImage() encodes the developed base ONCE (~1s CPU, caches image_embed + high_res_feats
               + guide), then refine() runs the decoder PER brush edit (~40ms) feeding the painted fill
               as the dense mask_input prompt (has_mask_input=1, +/-8 logits) plus the stroke bbox as a
               box (labels 2/3), picks the best of 3 candidate masks by predicted IoU, morphologically
               cleans it (open + keep-largest-CC + hole-fill, kills occluder specks), then shares
               SegRefine::refine for the edge matte. Output ObjectRef (Develop/objectmask.h, its own
               path store; coverage() identical to SubjectMask). Preprocessing = SAM 2 reference: RGB,
               1024^2, (px/255-mean)/std, ImageNet stats; point_coords scaled to the 1024 space.
               DEPLOYMENT: the DECODER must be the FIXED-SHAPE export -- cv::dnn's importer rejects the
               stock samexporter decoder's dynamic num_labels/num_points ("parseShape: dynamic 'zero'
               shapes"); pin them (1 label / 2 points) + onnxsim constant-fold offline (build-time tool
               only, nothing extra ships). Encoder loads as-is. Models (sam2 encoder/decoder .onnx) live
               next to u2net.onnx in the executable dir; warn-if-absent like the other AI masks.
               WIRED: MaskTool::Object (appended to the enum); MW::ensureObjectMask (two-phase --
               encode cached by path in objectMaskPredictor, decode the component brush + putRef under
               objectRefKey = path+brushHash so multiple object masks coexist); stackHasObjectMask; and
               the four triggers (onAiMaskEditBegin warms the encoder + decodes if a stroke exists;
               updateMaskOverlayTint per-component; renderDevelopPreview + renderDevelopFullResAsync
               loop the job's object components). Perimeter-paint contract (the
               UI emits it): paramsJson {"size":N,"strokes":[{"pts":[x,y,...],
               "size":N,"erase":bool},...]}, output-normalized 0..1 boundary
               strokes (like the Brush tool). parseObjectBrush rasterizes the
               strokes with BrushStamp::rasterize, then ObjectMask::fillEnclosed
               (Develop/objectmask.h) does a morphological close + border flood-
               fill: it thickens the wall by bridgePx (a fraction of the long
               edge, so preview/render agree), floods the background inward from
               the border, and any unreached non-wall pixel is ENCLOSED. Returns
               the filled silhouette ONLY when a closed loop is found; an open
               trace -> no coverage (matches the amber/green UI). No opencv in
               mainwindow.
               buildMaskBuffer WIRED: CompDesc.isObject + a PER-COMPONENT objRef
               (object refs are per-brush, not shared by path like Subject/Sky/
               Depth, so each CompDesc carries its own ObjectMask::getRef(
               objectRefKey(fPath,paramsJson))); the per-pixel loop calls
               ObjectMask::coverage. objectRefKey lives in the first anon
               namespace (before buildMaskBuffer) so both it and ensureObjectMask
               share it. Absent ref => no effect.
               PERIMETER-PAINT UI WIRED (ImageView): the Object tool is a solid
               BOUNDARY brush (NOT a lasso, NOT a fill brush). It REUSES the
               Brush buffers: strokes accumulate into maskBrushMain (the
               perimeter wall), maskBrushStroke holds the in-progress stroke,
               maskBrushSize is the diameter, maskBrushStrokesJson the committed
               list. Press begins a stroke (Alt = erase); move stamps segments
               (BrushStamp) + folds them into maskObjPerim (wall+stroke) and runs
               objRecomputeFill (ObjectMask::fillEnclosed) -> maskObjClosed +
               maskObjFill; objRebuildPreview tints the overlay AMBER while open,
               GREEN + a faint fill once closed. Release composites the stroke
               into the wall and emits {"size","strokes"} via maskGeometryChanged
               -> setActiveMaskParams -> paramsChanged, which runs
               ensureObjectMask (encode once + fill + decode) when closed and
               refreshes maskLayerTint. Size: [ / ] keys, a two-finger drag /
               wheel (adjustBrushSize -> maskBrushSizeRequested ->
               setActiveBrushSize), or the dock Size slider; Cmd/Ctrl+Z =
               objUndoStroke. State: maskObjPerim/maskObjFill/maskObjClosed/
               maskObjDrawing (imageview.h); helpers objEnsureBuffers/
               objRecomputeFill/objRebuildPreview/objUndoStroke; beginMaskEdit
               restores strokes+size, endMaskEdit clears; maskHitTest returns -1
               for Object; drawMask suppresses the stale layer tint while a
               stroke is in flight (brushStroking) so the amber/green shows live.
               Object is NOT in maskIsContent() and needs no content preview.
               DOCK ROWS WIRED (developproperties.cpp): maskToolName/FromName
               know Object; showMaskMenu offers Add/Subtract Object Mask (loops
               bound to MaskTool::Object); the maskEditBegin dispatcher includes
               Object so selecting it activates the perimeter brush AND fires
               onAiMaskEditBegin (warms the encoder); the settings rows are Size
               + Feather + Invert (its own row-builder branch); setActiveBrushSize
               accepts Object; defaultMaskParams returns {"size":8,"strokes":[]}.
               NOT added to the stackJob contentDerived list on purpose -- an
               undrawn/open Object mask yields no coverage and is dropped from the
               render like an unplaced gradient; once a loop closes it fills and
               composites. Full loop closes via existing signals: stroke release
               -> maskGeometryChanged -> setActiveMaskParams -> paramsChanged ->
               {updateMaskOverlayTint -> ensureObjectMask -> buildMaskBuffer tint}
               + {developParamsChange -> masked render}.
               MODELS + EXPORT WIRED: ReleaseExtras/tools/export_sam2.{py,sh} produce sam2_encoder.onnx
               (as-is) + sam2_decoder.onnx (fixed-shape: pins prompt dims 1 label/2 points + onnxsim
               constant-fold; prints Shape=0 on success). CMake stages both like u2net/skyseg/midas
               (warn-if-absent; bundled into MacOS/ on mac, copied beside Winnow.exe on Windows). The
               produced pair was verified to load+run under cv::dnn (enc ~1s, dec ~43ms). See
               ReleaseExtras/tools/README.md. Loader expects the names sam2_encoder/decoder.onnx.
               PENDING: full-app build verify; live end-to-end test; Windows confirm. NOTE buildMaskBuffer
               runs off the GUI thread; ObjectMask get/putRef are QMutex-guarded.
               Model quality validated offline (box + brush) on a real photo; the brush dense-prompt
               path fixes the box-only occluder weakness (IoU 0.92->0.99).
             - LIFECYCLE / FLUSH: MW::fileSelectionChange calls setCurrentImage; after the loupe
               pixmap is shown (both the cache-hit path and MW::refreshViewsOnCacheChange) MW::
               applyDevelopPreviewIfEdited renders the saved edits over an edited image (no-op for
               identity, so an unedited image stays the normal decode -- and is never written).
               Sidecars are flushed on navigate-away (setCurrentImage), quit (closeEvent), and
               before ingest/copy (MW::ingest / copyFiles); NEVER during a drag. Writes are
               synchronous (a few-KB text file). The OPTIONAL debounce-while-editing write (~2 s
               after edits settle) is gated by G::isDevelopDebounceWrite (default true); the other
               flushes always run.
             - VIDEOS: the Develop module operates on decoded still frames, so it is gated off for
               video selections. MW::currentIsVideo() (the sf VideoColumn flag) guards every entry
               point: fileSelectionChange hands developProperties->setCurrentImage an EMPTY path for a
               video (still flushing the image being left, then pointing the dock at nothing), and the
               three render entries -- applyDevelopPreviewIfEdited, renderDevelopPreview,
               renderDevelopFullResAsync -- early-return, so armed proxy/settle timers or Transform
               refreshes never try to develop a video.
             - PERF NOTE: setCurrentImage reads the sidecar synchronously on first touch of each
               image (cheap: no sidecar => fast empty read). Folding the develop blob into the
               existing metadata sidecar read (a datamodel column) is a fast-follow if navigation
               over many edited images shows latency.
           Increment 2 (presets): snapshot an EditStack (or subset) to a QSettings presets
           collection; apply to a selection (write each image's sidecar). Then masks/compositor.
        5. Brush masks (stroke list + rasterise) -> spatial ops with cache tier 3
           -> proxy-on-drag.

------------------------------------------------------------------------------------------
Fill Replace (spot / fill / object heal)
------------------------------------------------------------------------------------------

    "Fill Replace": heal a clicked spot, a brushed area, or a brushed-over object,
    parametrically and non-destructively. SHIPPED SCOPE (decision 2026-07-17): SPOT
    CLEANUP ONLY -- the spot button / "F" arms a click-to-heal spot tool; the Fill
    (paint an area) and Object (remove an object) modes and the Fill Replace panel are
    SHELVED behind G::useReplaceFillModes=false (global.cpp). All the code below is
    kept and working; flip the flag to bring the panel + modes back. The primary engine
    is an exemplar CLONE heal (real neighbouring pixels, Lightroom heal-brush style --
    see CLONE HEAL below); clone fallbacks (and Object-kind spots) use LaMa
    regenerative inpainting via the unified inference layer on the Apple GPU (see
    ENGINE SELECTION / LAMA SPOT-FILL below).

    ------------------------------ WHAT WAS TRIED -------------------------------------
    Intent: grow "spot removal" into a full Lightroom-class Fill Replace tool -- Spot
    (click), Fill (paint an area, clone-filled) and Object (brush an object, generative
    fill) -- entirely offline. Chronology and honest results:

    1. MI-GAN inpaint (27 MB, ~76 ms/512-tile on the ANE): fine on tiny spots; on
       high-ISO / noisy or defocused content the fill is a coarse colored mosaic that
       reads as a smudge. Kept only as the G::useLamaSpotFill=false fallback.
    2. LaMa inpaint (208 MB, ~272 ms/512-tile on the Apple GPU via CoreML CPUAndGPU;
       the ANE cannot run its FFC blocks): better than MI-GAN, but any 512-net fill
       upscaled to a large tile is soft and its low-frequency level drifts (measured +5
       luma dome). Corrections (Poisson / low-freq transfer / harmonic membrane) plus
       grain matching (quilted real residual -- synthetic RNG noise measured wrong in
       sigma AND character) narrowed but never closed the gap; a ground-truth bench
       (heal clean areas, compare to the untouched original) proved any generative fill
       leaves a visible patch on grainy/mottled surrounds.
    3. Exemplar CLONE heal (no model; masked template match + Poisson blend + chunking
       for long strokes + pinned sources + thickness-based feather + coverage-aware
       source rules): EXCELLENT on spots and thin-stroke removals (branch tests healed
       invisibly, user-confirmed) -- this is the shipped spot engine. On LARGE fills it
       hits an information limit: a 200px-wide region's true content is not inferable
       from its surround (ground-truth RMSE ~15 for every method tried), and patch-
       scale colour drift remains perceptible; candidate fixes (low-freq harmonization,
       smaller chunks) benched WORSE and were rejected.
    4. Diffusion inpaint (DreamShaper-8-inpainting, SD1.5-class, ~2 GB fp16, ~15 s per
       512 fill via torch/MPS on the M4): clearly the best large-area quality (bench:
       fat-fill RMSE 9.1 vs 15.8 for clone; stalk+leaf removal visually superior), but
       only with ALL of: a complete object mask + margin (else it REGENERATES the
       object from leftover fragments -- this is what SAM2 selection would be for), a
       background prompt plus a negative prompt (else worse than clone; the word
       "bokeh" literally paints bokeh discs), and the grain quilt on top. Integration
       would demand a ~2 GB model, a new runtime (stable-diffusion.cpp or Core ML),
       heal-result caching (15 s cannot run per render), and prompt plumbing.

    CONCLUSION (2026-07-17): spots are solved and shipped; large-area fill is not worth
    the model size / runtime / complexity for Winnow's scope. Fill + Object modes
    shelved, code retained. If revisited, the path is: SAM2 click-select (full-object
    mask) -> diffusion tier with negative prompt + grain quilt + cached results.
    -----------------------------------------------------------------------------------

    HISTORY (detail): the tool began as MI-GAN "spot removal" (MI-GAN beat LaMa in the
    first bake-off: 27 MB and ~76 ms on the ANE vs 208 MB and seemingly CPU-bound;
    scope was SPOTS only). 2026-07: LaMa was found NOT CPU-bound after all -- it runs
    on the Apple GPU via the CoreML EP's CPUAndGPU compute units at ~272 ms/tile, 4.2x
    faster than CPU; the ANE genuinely can't take its FFC blocks but the GPU can (see
    project_lama_coreml_gpu). Then measurement showed ANY generative fill reads as a
    patch on grainy/mottled surrounds, so the clone heal became the primary engine and
    LaMa the regenerative path; MI-GAN remains only as the G::useLamaSpotFill=false
    fallback engine.

    DATA MODEL (Develop/editstack.h): per-image like Geometry -- EditStack::spots is an
    ordered list of FillSpot{enabled, paramsJson}. paramsJson is opaque brush geometry
    (FillSpotGeom: feather 0..100 + "kind" = the Replace mode, and EITHER the
    single-stroke form -- size = diameter/long-edge, pts = output-normalized stroke
    points (Spot click / Object drag / legacy spots) -- OR the painted-area form
    "strokes": an ordered add/erase stroke list, each with its own brush size, from Fill
    mode; rasterize() composites them in paint order, add = max, erase = multiply by
    (1 - alpha), per-segment so long painted strokes stay cheap), serialized under the
    "fill" key (forward-tolerant). PARAMETRIC and non-destructive: only the strokes are
    stored; the heal is re-synthesized at render (the engines are deterministic) and is
    stable under later slider changes (no re-inference on an exposure tweak).

    RENDER: heals run BEFORE the geometry stage, on the developed oriented full frame,
    so spots stay glued to content when it is later cropped/straightened.
    developCompositeStack (Main/mainwindow.cpp) calls MiganFill::apply(out, job.spots)
    just before CropTransform::applyGeometry; StackRenderJob.spots is filled by
    DevelopProperties::stackJob. Engine (Utilities/inference/miganfill.*): per spot,
    take a square context tile (~4x the spot), resize to 512, pack [keep-0.5, im*keep]
    (keep=1 outside the hole), run, composite im*keep + out*(1-keep). The hole is
    DILATED a few px for the model (hard edges seam); the ORIGINAL soft brush alpha
    composites the patch back, so the seam is feathered. Spots apply in order (each
    heals over prior ones). Shared FillSpotGeom rasteriser (Develop/fillspot.h) is used
    by BOTH the ImageView preview and the render, so preview == render (cf. objectmask.h).

    UI (discoverability): a spot tool BarBtn in the Develop dock title bar
    (initialize.cpp, images/icon16/spot.png; dims off, brightens armed) arms the mode.
    It calls DevelopProperties::onSpotToolToggled; DevelopProperties owns the armed
    state (spotMode) and drives the button icon back via the spotActiveChanged signal.
    Armed, ImageView shows a brush-size circle cursor ([ / ] resize); a swiped stroke
    paints one spot on release;
    Escape disarms. Each spot drops an on-canvas PIN; click a pin to remove it. Stroke ->
    spotStrokeCommitted -> DevelopProperties appends a FillSpot -> re-render (heal); pins
    come from spotPinCenters via spotPinsChanged. Model: ReleaseExtras/migan.onnx
    (gitignored, EXISTS-guarded bundling); warn-if-absent -> the tool no-ops like the AI
    masks. FUTURE: the "Fill" pseudo-layer can be promoted to a real EditLayer (format is
    forward-tolerant); larger creative fills need a diffusion model behind the
    InferenceBackend seam.

    FILL REPLACE PANEL (Develop/Replace/replacepanel.* -- SHELVED: hidden while
    G::useReplaceFillModes is false, see WHAT WAS TRIED; the description below applies
    when the flag is re-enabled): the tool's UI surface, a
    TransformPanel-style strip below the Transform panel ("Fill Replace" gradient header
    with [?] tips and [E] preview eye; the eye renders with/without every committed heal
    -- non-destructive bypass via DevelopProperties::spotsShown gating stackJob's spots).
    Three mode rows, each shortcut letter bold in G::header2Color:
      Spot   (S)  click a blemish -- single click, no dragging; clone heal
      Fill   (F)  PAINT the area to replace (resizable brush; strokes accumulate as a
                  teal tint, Opt/Alt strokes ERASE from it), then Enter commits the
                  painted area as ONE heal; Esc clears the pending paint (a second Esc
                  exits the tool). Uncommitted paint is discarded on mode switch / tool
                  exit. Clone heal.
      Object (O)  brush over an object to remove; regenerative model fill
    The brush has NO user-facing feather: the cursor is one ring (= exactly what gets
    replaced), the tint shows exactly what was brushed, and rasterization is HARD (1px
    anti-alias edge). The seamless transition is the ENGINE's job: at composite time the
    alpha is feathered by a band sized from the stroke THICKNESS (featherFromHole =
    clamp(0.7 x max inscribed radius, 2..12px) -- NOT the bbox: a thin diagonal stroke
    has a huge bbox, and a bbox-sized feather smeared content far outside the brushed
    area; the clone extent is dilated by the same band so the ramp blends cloned content
    over its whole width). The stored per-spot "feather" JSON field is legacy metadata,
    no longer rasterized.
    The title-bar spot button or "S" toggles the panel; visible == armed
    (DevelopProperties::spotMode, panel visibility tracks spotActiveChanged so Escape
    closes it too). S is a Develop mode local shortcut (Slideshow in Preview mode) -- see
    DEVELOP MODE LOCAL SHORTCUTS. The tool must NOT claim S back from that table while
    armed, or S could arm it but never disarm it.

    S/F/O select the mode while the panel has focus (ReplacePanel::eventFilter, gated on
    G::useReplaceFillModes). The loupe's S/F/O mode keys, and the A/B "F" tool toggle,
    "B" correction cycle and "N" grain toggle, were RETIRED when S became the Develop mode
    spot toggle -- G::spotFillCorrectMode / G::spotFillGrain keep their defaults and are
    no longer keyboard-settable. The mode is
    stored per stroke as "kind" in FillSpotGeom paramsJson (legacy spots parse as fill),
    so each heal re-renders with the engine it was made with; mode persists to QSettings
    "Develop/Replace/mode".

    ENGINE SELECTION (lamafill.cpp LamaFill::apply, per spot by kind): spot/fill heal by
    the exemplar CLONE (cloneHealOne, below) and fall back to the model path when no
    clean source region exists (e.g. the hole touches a frame edge); object goes straight
    to the model path (a removed object leaves no clean source to clone). Clone heals
    need NO model, so lama.onnx-absent builds still heal spots/fills; only the model path
    no-ops warn-if-absent.

    LAMA SPOT-FILL (Utilities/inference/lamafill.*, LamaFill::apply, mirrors MiganFill's
    tile-context heal + soft-alpha composite). developCompositeStack uses it when
    G::useLamaSpotFill (default true), else falls back to MiganFill;
    flip the flag in global.cpp to revert. Runs on the GPU (InferenceDevice::GPU ->
    CoreML CPUAndGPU) because the ANE can't service LaMa's FFC. LaMa I/O differs from
    MI-GAN: two inputs image[1x3x512x512] RGB in [0,1] + mask[1x1x512x512] where 1 =
    inpaint region; output[1x3x512x512] RGB in [0,255] (verified by probe). Model is
    ReleaseExtras/lama.onnx (gitignored, EXISTS-guarded mac bundling like migan.onnx --
    Windows deploy copy not added, matching MI-GAN). The lama.onnx export MUST be the
    fixed-shape (1x3x512x512) + Einsum("ij,jk->ik")->MatMul rewrite, which cut CoreML
    partitions 405->76; a raw dynamic-batch export is far slower. See
    project_lama_coreml_gpu for the export recipe.

    HEAL LUMINANCE-MATCH A/B (large fills leave a low-frequency "patch" offset against a
    smooth surround, since the alpha composite feathers only the SEAM, not the interior
    level): applyFillCorrection (lamafill.cpp) corrects the model fill at net-res before
    compositing, selected by G::spotFillCorrectMode -- 0 none (raw model), 1 Poisson
    (cv::seamlessClone NORMAL_CLONE, boundary-gradient match; no-ops if the hole is empty
    or touches a frame edge; preserves the model's WRONG gradient so it still patches),
    2 membrane transfer (model texture + push-pull surround level; INERT for this
    lama.onnx -- probe-verified 2026-07-16 that the export composites the input over the
    keep region IN-GRAPH, so orig == model outside the hole, the membrane of their
    difference is identically zero and mode 2 == mode 0 byte-for-byte; any correction
    anchored on the keep-region model-vs-orig difference is dead for this export),
    3 pure harmonic (push-pull surround only, discard the model). The surround is
    reconstructed by pushPull() -- a Gaussian-pyramid push-pull (Gortler et al.) that
    fills the hole with a smooth harmonic membrane; this REPLACED a single-Gaussian
    normalized convolution that collapsed to a flat average deep in a large hole (leaving
    a residual luminance blob). Mode 1 Poisson is the DEFAULT: the model path serves
    Object fills (and clone fallbacks), which must keep the model's content, and Poisson
    keeps it with the level solved from the boundary; mode 3 discards the model, only
    right for smooth surfaces. **B** cycles the mode while the replace tool is armed
    (ImageView::keyPressEvent -> spotFillModeChanged -> MW::developParamsChange re-heals
    the same image live -- proxy tick + full-res settle; a bare
    renderDevelopPreview(false) left the loupe stuck on the upscaled PROXY, whose
    grainless surround invalidated the first A/B round; status bar shows the mode).
    TESTING-only key, not in shortcutsform.ui.

    CLONE HEAL (cloneHealOne, Lightroom heal-brush style, the spot/fill engine): a
    masked template match on the context ring finds the best source patch near the spot
    -- coarse-to-fine (match at reduced resolution so the hole is ~<=48px, refine +-q px
    at full res), search window 3x the hole so lighting agrees -- then cv::seamlessClone
    (NORMAL_CLONE) Poisson-blends the patch over the hole (texture AND gradients from
    the source, level solved from the destination boundary) and the engine-feathered
    alpha blends it in. WHY POISSON: ground-truth benchmarked (scratch heal_bench.py --
    heal strokes over CLEAN background, compare against the untouched original) it beat
    every alternative on every content type (trunk RMSE 5.8 vs 7.5 membrane-split /
    5.9 xphoto SHIFTMAP / 7.0 pure membrane; bokeh 5.4 vs 10.3 / 8.7 / 7.4). Harmonic
    membranes obey the maximum principle and can NEVER reconstruct a local brightness
    maximum, so any membrane-based low-freq dims the bokeh blobs a stroke crosses (a
    visible "dark sausage"); SHIFTMAP shows graph-cut patch seams on defocused
    backgrounds; Poisson keeps the source's own blobs and only tilts them to fit the
    boundary. No model inference, full resolution; copied pixels carry the surround's
    true texture at EVERY frequency (grain, mid-freq mottle), which was the remaining
    visible tell of every generative fill. Holes reaching a FRAME EDGE still clone:
    seamlessClone needs its mask strictly inside the patch, so the tile is REFLECT-
    padded 8px for the solve and cropped back. Returns false when no clean source
    exists -> the caller falls back to the model path.

    PINNED SOURCES: the template match runs on DEVELOPED pixels, so re-matching every
    render could flip the chosen source mid-slider-drag (heal-content shimmer). The
    first successful match per (image path, render size, spot geometry, chunk) is
    pinned in a small mutex-guarded cache (full-res renders run off the GUI thread) and
    reused; the Poisson blend still re-runs on the current pixels, so exposure/colour
    edits track while the heal GEOMETRY stays stable. "No source" is pinned too, so
    failing chunks don't re-search every render. Also removes the per-render match
    cost. Keyed by qHash(paramsJson), so editing a spot re-matches naturally.

    CHUNKED CLONE (large/long painted regions): ONE source offset for a whole stroke
    leaves a stroke-shaped ghost when the background structure doesn't repeat at a
    single shift (measured on the branch-removal test), so when the coverage bbox
    exceeds ~1.5x the chunk size the heal splits into a grid of chunks: each chunk
    clones its own windowed share of the coverage (ChunkWin: weight 1 on the core cell,
    linear ramp over core/4 px; cores tile the bbox so every covered pixel is at full
    weight in exactly one chunk) from its own LOCAL source, and the ramps blend
    neighbouring chunks' different sources into each other. The chunk core = 3x the
    painted THICKNESS (6 x max inscribed radius, clamped 96..384 px) -- like the
    feather, NOT the brush diameter: a big brush previously made 640px chunks whose
    interiors sit far from any boundary, and nothing matches a smoothly varying
    background at that scale (stalk test: colour drift, 2000px source offsets). The
    coarse match also carries a DISTANCE PENALTY (score x (1 + (d/R)^2)): a source at
    the search edge must be ~2x better than a near one, keeping heals inside the local
    colour/lighting zone. Two coverage-aware rules keep sources clean, both against the
    WHOLE blemish, not just the chunk's hole: (1) the template-match ring ignores all
    coverage (else it seeks sources that LOOK like the remaining blemish), and (2) a
    candidate source is rejected if the pixels it would copy -- the chunk's hole SHAPE,
    padded past the feather dilation -- overlap ANY coverage; the overlap for every
    offset comes from ONE cross-correlation (cv::matchTemplate TM_CCORR) of the
    coverage with the hole shape. The shape matters: a bounding-RECT test rejected
    nearly every candidate for a diagonal stroke (its bbox is a huge, mostly-empty
    square), silently sending all chunks to the model path -- which WAS the visible
    stroke-shaped smudge (lab-verified on the branch image). A chunk with no valid
    source falls back to the model fill for just that chunk.

    GRAIN MATCH (applyGrainMatch, G::spotFillGrain, default on, **N** toggles): measured
    root cause of the residual "patch" on smooth walls is NOT luminance (level corrections
    land within ~1/255) but MISSING sensor grain -- a smooth fill reads as plastic against
    a grainy wall (harmonic fill measured at 0.17x the surround's grain). Synthetic RNG
    noise also measured WRONG in character (white, lag-1 autocorrelation ~0.05 vs the real
    grain's ~0.38; channel-independent ~0.04 vs ~0.92 -> chroma confetti that attenuates
    more than real grain under display resampling), so the grain is QUILTED: random
    high-frequency residual (sigma-4 highpass) blocks from the tile's kept surround,
    screened to near-median energy so structure is never copied, overlap-blended across
    the hole with random flips -- copied real grain carries sigma, spatial and channel
    correlation and fine mottle for free. Per channel the amplitude tops the fill's
    existing grain up to the surround's in quadrature; falls back to channel- and
    spatially-correlated synthetic noise when the surround has too few clean source
    blocks (fixed RNG seed either way so re-renders don't shimmer). MODEL-path only --
    clone heals carry real grain already. Diagnosis was instrument-first (measured the
    A/B outputs in Python) after level-only fixes kept failing.

------------------------------------------------------------------------------------------
Develop dock (UI)
------------------------------------------------------------------------------------------

    The Develop panel is a dock (View menu > Develop Editor Panel, shortcut F9), built like
    the Embellish dock: MW::createDevelopDock() in initialize.cpp wraps a DevelopProperties
    widget (Develop/Properties/), a PropertyEditor subclass mirroring EmbelProperties. It is
    tabbed with the Embellish dock in the right dock area and its visible / collapsed state
    persists exactly as the other docks (settings isDevelopDockVisible + DevelopDock, plus the
    workspace struct field isDevelopDockVisible).

    The dock's widget is a container, top to bottom: a fixed-height SCOPES strip, the TRANSFORM
    (crop + perspective) panel, then the DevelopProperties tree taking the remaining (stretch)
    space. The scopes and transform strips are each independently toggled from the editor bar.

Develop scopes (histogram + vectorscope)
------------------------------------------------------------------------------------------

    A live histogram (left) and vectorscope (right) sit above the property tree, in
    Develop/Scopes/: HistogramView (256-bin R/G/B with additive blend + a faint luma outline),
    VectorscopeView (YCbCr Cb/Cr 2-D accumulation on a circular graticule, log-scaled and
    hue-coloured), wrapped by ScopesView (a QHBoxLayout, histogram given the larger stretch).

    They reflect the image CURRENTLY SHOWN in the loupe. MW::updateDevelopScopes(const QImage&)
    does ONE strided sample pass (~180k-pixel budget, so a 50MP RAW costs the same as the proxy;
    constScanLine so no detach) that fills both scopes from the same downsample. It is called
    after each develop preview render (the post-Develop `out`, both setDevelopPreview sites) and,
    for an unedited image, from the decoded image in applyDevelopPreviewIfEdited; the scopes are
    cleared at the imageView->clear() sites. No new timers -- it piggybacks the existing coalesced
    render path. The pass is a cheap no-op while the scopes are hidden.

    A button on the Develop editor bar (graphic.png) toggles the strip via MW::toggleDevelopScopes;
    the choice persists as Settings "Develop/scopesVisible". (A standalone widget above the tree was
    chosen over an in-tree spanned row so the scope stays pinned rather than scrolling away; see
    notes/Develop_Scopes_Plan.txt for the rejected alternative.)

    Tone-region slider (ToneRegionSlider, under the histogram, sharing its x-axis): a black->white
    track with three draggable handles that split the tonal range into blacks | shadows |
    highlights | whites (like the Lightroom tone-curve region splits). The handles move WHERE the
    shadows/highlights tone controls act and how far each reaches; blacks (0) and whites (1) stay
    pinned. They map to three EditParams fields (toneShadowCenter / toneCrossover /
    toneHighlightCenter, perceptual 0..1, defaults 0.25/0.50/0.75). In Develop::buildToneLut the
    shadows/highlights Gaussian centres come from the two outer handles and each region's sigma is
    derived from its gap to the crossover (kToneSigmaSpan, so the default gap reproduces the old
    fixed kToneSigma) -- i.e. the defaults are an exact no-op. A drag writes the active layer's
    params and emits paramsChanged() (live re-render, coalesced like the sliders); the splits only
    change pixels when a tone slider is non-zero, so they are NOT part of EditParams::isIdentity().
    DevelopProperties::bindToneSlider() connects the slider (drag -> onToneSplitsChanged) and
    populateSlidersFromStack() pushes the saved positions back on image switch.

    Cursor readout: hovering the loupe marks the pixel under the cursor on both scopes -- vertical
    R/G/B ticks on the histogram, a ring at its Cb/Cr on the vectorscope. ImageView (mouse tracking
    on) emits cursorImagePos (normalized) / cursorLeftImage; MW::onImageCursorPos samples that one
    pixel from developShownImage (the cached QImage last handed to updateDevelopScopes == what the
    loupe shows; implicitly shared, so O(1) per move, no re-bin, no pixmap->image copy) and calls
    ScopesView::setMarker / clearMarker. Cleared when the cursor leaves the image. No-op while the
    scopes are hidden.

    Vectorscope zoom: a right-click menu (100/150/200%) magnifies the plotted trace + marker
    outward from the centre to reveal low-saturation detail; the graticule circle stays as the
    100% reference and a small "%"-label shows when zoomed. VectorscopeView::setZoom +
    zoomChanged; ScopesView re-emits as vectorscopeZoomChanged; MW restores/persists it as
    Settings "Develop/vectorscopeZoom".

    Skin-tone line: the same right-click menu toggles a flesh-tone reference line -- the YIQ
    I-axis, which from our BT.601 Cb/Cr coefficients is (Cr-128) = -1.946*(Cb-128); skin of every
    ethnicity falls along it (hue is constant; only saturation/brightness vary). Drawn centre ->
    edge into the warm upper-left ray, independent of zoom (it is an angle guide). setSkinLine +
    skinLineChanged -> ScopesView::vectorscopeSkinLineChanged -> Settings
    "Develop/vectorscopeSkinLine".

Develop Transform panel (crop + perspective)  [UI PROTOTYPE]
------------------------------------------------------------------------------------------

    TransformPanel (Develop/Transform/) is a compact control strip below the scopes and above
    the property tree -- the sibling of ScopesView. It is the UI surface for a non-destructive
    crop/straighten/perspective edit; it carries NO geometry or pixel state. The crop rectangle,
    straighten angle and perspective homography will live in the edit stack and apply LAST in the
    pipeline (after the develop ops), so they stay non-destructive and masks map through them. The
    image-side overlay and the warp engine are wired to the panel's signals in a later increment;
    for now each control persists its own state and emits a signal (the integration hook).

    Layout (redesigned): a property-style gradient HEADER band ("Transform", drawn by an internal
    GradientHeader QWidget using the same backgroundShade +5 -> -15 gradient as PropertyDelegate)
    with three trailing BarBtns: [?] tips ("Crop and transform tips"), [E] preview eye (show/ignore
    the whole transform, previewToggled), [R] reset (clear crop/straighten/warp to identity,
    resetRequested; icon reset.png -- the "chasing arrows" glyph, replacing the old refresh.png).

    Below it a three-way MODE toggle down the left (Crop / Level / Warp -- a mutually exclusive
    QButtonGroup; the selected button gets a teal (#1b8a83) background + border). Each mode row
    carries that mode's controls and its own [R] reset (resetModeRequested, all reset buttons kept
    in the last grid column so they align vertically):
        Crop : an Aspect combo (As shot / 1x1 / 3x2 / 4x3 / 5x7 / 4x5 / 8.5x11 / 16x9 / 16x10 /
               persisted custom aspects / "Add custom aspect..."; userData per item is the w/h
               ratio, 0.0 = As shot/free) + a padlock that locks the ratio + a flip button
               (swap.png, "F") that swaps the crop between landscape and portrait. Locking
               "As shot" constrains drags to the image's native aspect
               (ImageView::cropLockedAspect); flipping honours 1/aspect.
               Flip (ImageView::setCropAspectFlip) rotates the crop box 90 deg by swapping
               its pixel width and height (new width = old height, etc.), keeping the
               centre and scaling down only if the rotated box would not fit the image; a
               locked crop already sits at the ratio so the swap lands on the inverted
               ratio, a free crop just swaps its own dimensions.
               Flip STASH: a straight flip-back must restore the exact pre-flip rect,
               else the scale-to-fit clamping shrinks the crop a little on every
               landscape<->portrait round trip. cropFlipPrevN holds the rect from before
               the last flip and cropFlipResultN what that flip produced; if the crop is
               unchanged since (cropN still == cropFlipResultN, epsilon compare) the next
               flip restores cropFlipPrevN verbatim instead of recomputing. Any manual
               crop edit (resize/draw/pan) moves cropN off cropFlipResultN, so the next
               flip recomputes from the edited rect. The stash is discarded in
               beginCropEdit (new session) and setCropAspect (aspect/lock change breaks
               the pairing).
        Level: a straighten-angle field (-45..45 deg, editingFinished -> levelAngleEntered, applied
               as an absolute straighten). Selecting Level also arms the draw-a-level-line tool.
        Warp : selecting it seeds the 4-point quad (ImageView::beginWarp) for corner dragging.
               Commit the traced quad with Enter/Return or a double-click (ImageView claims those
               keys/clicks via ShortcutOverride while cropWarp is active, emits warpCommitRequested
               -> MW::rectifyDevelopCrop, which stores the warp + suggested crop and returns the
               toggle to Crop). There is no separate Rectify button.
    The Aspect combo/lock/flip are enabled only in Crop mode, the angle field only in
    Level mode.

    MW wiring: modeChanged -> setDevelopTransformMode arms the matching ImageView tool
    (beginCropEdit / beginLevel / beginWarp); levelAngleEntered -> setDevelopLevelAngle;
    resetModeRequested -> resetDevelopTransformMode clears just that mode's geometry
    contribution. The CROP reset (and the header reset) also calls setAspectAsShot()
    first: beginCropEdit re-fits a full-frame crop to a LOCKED aspect (cropAspect > 0), so
    without freeing the aspect the reset to the full frame would just re-inscribe the
    locked ratio and appear to do nothing.

    Shortcuts: "R" toggles the panel (MW::toggleDevelopTransform, developTransformAction, editor-
    bar button rectangle.png; persists Settings "Develop/transformVisible"). R is a Develop mode
    local shortcut and does nothing in Preview mode -- see DEVELOP MODE LOCAL
    SHORTCUTS. Closing the panel has two OPPOSITE outcomes: R / Enter / the editor-bar
    button all COMMIT (via toggleDevelopTransform -> exitDevelopCrop: write the overlay
    crop into the edit stack, show the cropped result); Esc CANCELS via
    MW::cancelDevelopTransform -- restores the Geometry snapshot taken at enterDevelopCrop
    (developCropGeometryBackup), drops the overlay WITHOUT reading its rect, and hides the
    panel. The snapshot/restore is needed because straighten and warp are written to
    geometry LIVE during a session (applyDevelopLevel / rectifyDevelopCrop), so a cancel
    must revert them, not merely skip the pending crop. (Enter is special-cased while a
    WARP quad is being traced: there it commits the quad -- rectify -- not closes; see
    the contextual-keys note in DEVELOP MODE LOCAL SHORTCUTS.)
    Whenever Transform is active, single letters select/act: "C" Crop, "L"
    Level, "W" Warp, "A" toggles the aspect lock, "F" flips the aspect between
    landscape and portrait -- and must beat the window-level shortcuts on those keys
    (e.g. "A" Run Droplet, "C" Compare, "F" the spot/fill tool, which stays available
    outside a transform session). Because the crop overlay
    (ImageView), not the panel, usually holds focus during a transform, these are
    dispatched by MW::developShortcutIntercept (the qApp-level Develop-mode arbiter that
    also routes "S" to the spot tool), gated on developCropEditing: it accepts the key's
    ShortcutOverride (freeing it from the global QAction) and, on the following KeyPress,
    calls TransformPanel::handleTransformShortcut(key). It runs before Qt's shortcut
    system and independent of focus, so it works wherever focus sits; the arbiter's
    value-editor guard excludes text fields (aspect combo, angle field) so typing an
    aspect/angle still works. TransformPanel::eventFilter still claims the same keys when
    the panel or one of its non-text controls holds focus (the same handler). A
    panel-scoped letter QAction is not used because it would be an ambiguous overload with
    the window-level shortcuts. Custom aspects + lock state + selected mode persist under
    Settings "Develop/Transform/".

    Crop rubber-band tool (ImageView) -- IMPLEMENTED:
    Showing the Transform panel (R / button) calls ImageView::beginCropEdit; hiding it calls
    endCropEdit. KEY RULE (user-specified): the crop tool NEVER changes the view transform -- no
    zoom, no auto-pan. The user keeps full control of zoom/pan; begin/end only toggle the overlay
    and leave the image exactly where it is. cropN (normalized image coords) is the source of truth;
    cropFrameVp (the on-screen frame, viewport px) is DERIVED from it (cropNToVpRect) and tracks the
    image under the current zoom/pan. Interaction (all guarded by cropActive()):
        - drag a HANDLE (cropHitTest 0..7: 4 corners + 4 edge mids) -> resizes the frame over the
          STATIC canvas (cropResizeFromHandle anchors the opposite corner/edge, clamps to the
          image's on-screen rect); cropN := cropVpRectToN(frame). No re-stage, no zoom change.
        - while the crop is still the WHOLE frame (cropIsFull), the cursor is a crop crosshair and a
          drag on empty area rubber-bands a brand-new crop (cropDrawNewFrom, cropDrag==kCropDrawNew)
          -- so you draw the crop directly instead of shrinking the full frame by its handles. Over
          a handle it reverts to the resize cursor/behaviour.
        - Alt/Opt over a rectangle crop shows the "transform" rubber band (red border, circular
          corner handles, no side handles); an Alt-drag of a corner pulls the free perspective quad.
        - drag elsewhere (a sub-crop exists) -> a NORMAL canvas pan (the existing scrollbar pan); the frame stays fixed
          on screen and the image slides under it, so cropN is recomputed from the unchanged frame.
          This is how the crop is repositioned -- you move the canvas, not the crop rectangle.
        - aspect lock (from the panel, setCropAspect) constrains the frame ratio; "As shot"/ratio 0
          is free.
        - when the USER zooms (scale()) or the window resizes, the frame is re-derived from cropN
          (cropSyncFrameFromN) so the crop stays on the same image content.
    Overlay (cropDrawOverlay, viewport coords in drawForeground): dimmed exterior, rule-of-thirds
    grid, frame border, corner/edge handles. ImageView emits cropChanged(x,y,w,h) for persistence.
    The wheel is swallowed during crop (wheel = image navigation, which would strand the overlay).

    Warp (4-point perspective) sub-mode -- IMPLEMENTED (interaction only):
    Holding Alt/Option while dragging a crop CORNER breaks the rectangle into a free quadrilateral
    (cropWarp=true; cropEnterWarp seeds cropQuadN/cropQuadVp from the rectangle, corners TL,TR,BR,BL).
    In warp mode only the 4 corners are handles (each drags independently, clamped to the image);
    cropHitTest / cropResizeFromHandle / cropDrawOverlay / cropSyncFrameFromN all branch on cropWarp.
    The overlay draws the quad with a bilinear 3x3 guide grid + 4 corner handles and dims outside the
    polygon. Panning still works (the quad is fixed on screen, recomputed from the moving image; the
    pan-clamp uses the quad's bounding box, cropFrameBBoxVp). Choosing an aspect, re-entering, or
    Rectify leaves warp mode.

    Perspective-warp PIXEL engine -- IMPLEMENTED (Develop/Transform/croptransform.{h,cpp}):
    CropTransform::rectifyPerspective(src, quadPx[4], &outCropNorm) warps the whole image so the
    quad (TL,TR,BR,BL image-pixel coords) becomes an axis-aligned rectangle and returns the
    corrected QImage (transparent where the warp leaves the original) plus the largest inscribed
    axis-aligned rectangle (normalized) as the default crop. Math: cv::getPerspectiveTransform
    (quad -> WxH rect sized by averaged opposite-edge lengths) -> translate into a positive canvas
    bounded by the warped image corners -> cv::warpPerspective (BGRA, transparent border) ->
    validity mask (alpha>=250) -> largest-rectangle-in-binary-matrix (histogram+stack, on a
    downscaled mask). Self-contained (OpenCV only) so it can later be called from the render
    pipeline. Validated offline vs the same algorithm in cv2 (keystone quad -> exact axis-aligned
    rect; inscribed rect avoids the transparent corners). ImageView::rectifyCrop drives it on the
    DISPLAYED image: warp -> setPixmap -> resetFitZoom -> re-enlarge scene -> crop continues on the
    rectified image (cropN = outCropNorm). This is a DISPLAY-LEVEL preview (the develop pipeline
    still holds the original; a develop edit re-renders un-warped).

    Persistent crop (geometry applied in the render pipeline) -- IMPLEMENTED (Phase A, rectangle
    crop):  EditStack now carries a per-image Geometry (cropX/Y/W/H, straighten, hasWarp+quad[8],
    fillCanvas, show), serialized in the sidecar (toJson/fromJson, folded into isIdentity). The
    Transform panel's Preview eye is a LIVE RESULT TOGGLE (the panel is only on-screen while the crop
    tool is active, and that view deliberately shows the full frame + overlay, so a plain show/ignore
    flag would have nothing to toggle): eye ON commits the overlay's crop, drops the overlay
    (ImageView::endCropEdit) and renders with the geometry applied (MW::developCropShowResult gates
    the crop-suppression in renderDevelopPreview + the async full-res path); eye OFF re-renders the
    full frame and re-arms the overlay (beginCropEdit). enter/exitDevelopCrop reset the flag +
    setPreviewShown. Transform Reset writes an identity Geometry and returns to editing. (Geometry::
    show is retained in the model/sidecar for a persistent ignore but is not what the eye drives.)
    The renderer
    applies it LAST -- developCompositeStack runs CropTransform::applyGeometry(out, job.geometry)
    after the develop ops + EXIF orientation, on the full-output-dimension image, in both the proxy
    and async full-res paths (StackRenderJob.geometry from DevelopProperties::stackJob).
    Commit model (Lightroom): MW::developCropEditing gates it -- while the crop tool is being edited
    the render NEUTRALIZES the geometry so the FULL frame shows for the overlay; committing applies
    it. MW::enterDevelopCrop (set flag -> render full -> ImageView::beginCropEdit(storedCrop)) and
    MW::exitDevelopCrop (read cropRect -> endCropEdit -> DevelopProperties::setCurrentGeometry ->
    clear flag -> render cropped) are driven from toggleDevelopTransform + developDockVisibilityChange.
    ImageView::setDevelopPreview re-fits when the image dimensions change (crop applied/removed); the
    same-size path (a develop-slider drag) still just swaps pixels.

    Persistent warp (Phase B) -- IMPLEMENTED: the 4-point perspective is a STORED geometry param
    (hasWarp + quad[8]), non-destructive (no pixmap bake). Two-phase display via the render gate:
    while the crop tool is active the render suppresses ONLY the crop (resets it to full) but KEEPS
    the warp, so before Rectify the full frame shows (trace the quad) and after Rectify the corrected
    canvas shows (set the final crop). MW::rectifyDevelopCrop stores g.hasWarp + g.quad (= the traced
    corners, normalized in the developed+oriented frame = applyGeometry's input space) + g.crop (the
    suggested largest-inscribed rect from ImageView::computeRectifyCrop), then re-renders the
    corrected canvas and restarts the crop overlay on it. exitDevelopCrop keeps the stored warp and
    updates only the crop. KNOWN LIMIT: re-warping an already-warped image is wrong (the new quad is
    traced on the warped canvas, but applyGeometry warps the un-warped frame) -- there is no
    warp-reset UI yet, so fresh-image warp is the supported path.

    Level / straighten tool -- IMPLEMENTED: the panel Level button arms ImageView::beginLevel (draw a
    line along something that should be horizontal/vertical). On release the line's tilt is reduced to
    the nearest H/V and emitted (levelAngleChanged); MW::applyDevelopLevel adds it to g.straighten
    (clamped +/-45), auto-fits the crop to the rotation wedges via CropTransform::straightenCropNorm
    (analytic rotatedRectWithMaxArea, pixel-validated to sit inside the valid region), then re-renders
    the straightened canvas and restarts the crop overlay -- same two-phase pattern as Rectify.
    applyGeometry's straighten converts to ARGB32 so the wedges are transparent.

    NOT yet done (next increments): a warp-reset (to re-warp); confirm export applies the geometry;
    content-aware fill. The panel's Crop button is unwired.

    Sections (built via PropertyEditor::addItem):
        Layers   header with -/+ buttons (same idiom as Embellish Templates) + a "Select
                 layer" combo. A layer is one set of adjustments; switching rebuilds the
                 Basic/Effects rows from that layer's saved values. Layers persist under
                 Settings "Develop/Layers/<name>/..." with "Develop/Layers/current".
                 SCAFFOLD ONLY -- this is the "layers as ALTERNATIVES" model (only the selected
                 layer is active, persisted per-machine in QSettings). It is superseded by the
                 "Layer & masking model (planned)" subsection above: all layers active as a
                 composited stack, the combo selects only which layer the sliders EDIT, opacity +
                 masks per layer, persisted per-image in the XMP sidecar.
        Basic    Temp, Tint, Exposure, Contrast, Highlights, Shadows,
                 Whites, Blacks, Texture, Dehaze (sliders). Integer -100..100 except Exposure
                 (EV, -5.00..5.00). All default to 0 (identity).
        Color    Red, Green, Blue (per-channel gain); Hue, Saturation, Vibrance, Luminance
                 (global HSL).
                 Integer -100..100 sliders, all default 0 (identity).
        Effects  Denoise (localDenoiseLuma, luminance NR) + Denoise Color
                 (localDenoiseChroma, chroma NR) -- two 0..100 sliders (mapped to 0..1),
                 default 0. Local, maskable, post-demosaic. Distinct from the Base layer's
                 "Denoise raw" (PMRID pre-demosaic, denoiseLuma/denoiseChroma). Also
                 Vignette (vignetteExposure, EV -5.00..5.00, default 0) + Vignette Feather
                 (vignetteFeather, 0..100 -> 0..1, default 50): a global radial
                 corner-exposure falloff (see #8 above); shaped vignettes use masks. Also
                 Grain (grainAmount, 0..100 -> 0..1, default 0) + grain Size (grainSize,
                 0..100 -> 0..1, default 25) + grain Roughness (grainRoughness, 0..100 ->
                 0..1, default 50): monochromatic film grain (see #9 above); size and
                 roughness are inert until amount is non-zero.

    Each layer maps onto one EditParams (DevelopProperties::editParams()). Binding a
    value change to a re-decode is deferred: itemChange() persists the value to settings
    and emits paramsChanged() so the hook is ready.


Develop presets (save)
-----------------------

    Save the current image's develop state as a named, reusable preset (Lightroom's
    "New Develop Preset"). SAVE ONLY for now -- applying a preset is a later task.
    SAVING a preset is reached two ways: Cmd+Shift+N (Develop mode only) or the Develop
    dock context menu ("Save Develop Preset...", in the same menu as Expand all /
    Collapse all / Solo). APPLYING (running) a saved preset is the colour-wheel Preset
    button in the Develop panel title bar, or the "P" key -- a STUB for now (the
    picker/apply path is not built; MW::developRunPreset). The title-bar Preset + Export
    buttons are colourful BarBtn glyphs in the same style as the Scope and Transform
    buttons (Export -> lightning, also a stub).

    Active border. The Develop title-bar buttons that toggle a panel/tool -- Scopes,
    Transform and Spot -- carry a blue (G::appleBlue) border while their panel is shown /
    the tool is armed, like a conventional checked toolbutton (BarBtn::setActive). MW
    keeps them as members and calls setActive from the toggle handlers
    (toggleDevelopScopes, toggleDevelopTransform + cancelDevelopTransform,
    spotActiveChanged) so the border tracks the real state even when toggled by the
    keyboard (H / R / Esc / S). The Preset + Export buttons are momentary (no panel), so
    they show no persistent border yet.

    Dialog (Dialogs/savedeveloppresetdlg.{h,cpp}, SaveDevelopPresetDlg). A preset-name
    field plus a collapsible, scrollable checklist of every settable item. Items that
    differ from their default on the current image are pre-checked and their group
    pre-expanded, so the user can Save immediately or fine-tune. Buttons: Select all /
    Select none / Cancel / Save (Save is disabled until the name is non-empty; an
    existing name prompts to overwrite). Right-clicking the checklist pops Expand all /
    Collapse all.

    The dialog is UI-only: it renders a model of PresetGroup { title, checkable, leaves }
    and reports back the preset name + the checked leaf keys per group. It knows nothing
    of the develop data model or the settings layout. Groups:
        Global settings   a non-checkable container. Leaves: Raw demosaic (the decode
                          engine, G::decodeRawEngine), Raw noise reduction (Base
                          denoiseLuma+denoiseChroma), Histogram tonal ranges (the three
                          tone splits), Crop, Level (straighten) and Warp (each a SEPARATE
                          leaf, from the per-image Geometry), Spots (all spot heals).
                          Raw demosaic + Raw noise reduction only apply to raw files, and
                          raw NR (PMRID) is the Winnow engine only; both stay listed but
                          are pre-checked only when relevant (demosaic when the file is
                          raw; raw NR when raw AND not decoded by Apple).
        Base layer /      one checkable tri-state parent per layer. Leaves are the Basic /
        Layer N           Color / Effects adjustments (temp..grain). Denoise folds in its
                          Color (chroma); Vignette folds in its feather; Grain folds in
                          size + roughness. Mask geometry is content-specific and is NOT a
                          preset item.

    Write (DevelopProperties::saveDevelopPreset / writePreset / writeLayerLeaves). The
    caller snapshots the current image's EditStack and persists the checked values to
    QSettings under the requested hierarchy:
        Develop Presets/<name>/Global/<key>       = value
        Develop Presets/<name>/Base layer/<key>   = value
        Develop Presets/<name>/Layer 1/<key>      = value
    Leaf keys reuse the EditParams JSON field names and store the RAW param values (the
    source of truth, e.g. denoise 0..1), so a future "apply preset" can assign them
    straight back (mirroring EditStack::paramsFromJson). Geometry is stored as crop
    ("x,y,w,h"), straighten and warp ("q0..q7"); spots as a QSettings array of paramsJson.
    Writing a preset first clears any prior group of the same name (overwrite).

    Shortcut wiring. Two separate actions:
      - developSavePresetAction (SAVE, Cmd+Shift+N). Unlike the bare-key Develop actions
        (dispatched by developShortcutIntercept, which arbitrates only unmodified keys),
        this is a modified combo owned by no other action, so it carries a REAL
        QKeySequence ("Ctrl+Shift+N"). It is enabled only in Develop mode --
        setOperationMode tracks the mode (a disabled QAction's shortcut does not fire) and
        syncDevelopMenuEnabled greys the menu item in Preview.
      - developRunPresetAction (RUN/apply, "P" + the title-bar Preset button). P is
        globally Pick, so like the other bare Develop keys it is dispatched by
        developShortcutIntercept via the developShortcuts table (loadDevelopShortcuts),
        NOT a real QKeySequence -- QKeySequence("P") would clash with Pick. Stub for now.
